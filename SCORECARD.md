# CareDroid Quality Scorecard

**CareDroid — Emergency Department operations platform** (Vite + React frontend, NestJS backend, embedded clinical copilot). This file tracks a long-running, cycle-by-cycle quality-improvement program: one well-scoped, evidence-backed lever per cycle, never a fabricated score.

**Last updated: 2026-07-28 (Cycle 213 — closed out the 2 `__cta` candidates Cycles 207-208 left honestly unverified (`reception-job-profile-card__cta` on `/profile`, `reception-skill-strip__cta` on `/emergency/reception`), and found this was blocked by something more fundamental than the demo-auth issue Cycle 208 diagnosed. Root-caused the demo-auth failure precisely first: `scripts/capture-reception-screenshots.mjs`'s seeded `localStorage` payload had no `authMode`/`demoPersona` marker, so `demoPersonaModel.ts`'s `isDemoPersonaUser()` never recognized it as a demo session — `hydrateStoredDemoUser()` silently discarded the whole payload (including the `role` field the script was trying to set) and fell back to the app's default role every time, for every role the script ever requested. Proved this precisely with a standalone reconstruction of the real hydration logic (not guessed) and fixed the script by adding `authMode: 'open-access'` to its seeded payload. But then found the fix alone still couldn't render either CTA: `ReceptionJobProfileCard.tsx`/`ReceptionSkillStrip.tsx` were both added 2026-07-24 — 5 days after `dist/`'s last build (2026-07-19) — so the served static bundle doesn't contain this code *at all* (confirmed: their class names appear nowhere in `dist/assets/*.js`). This is a harder blocker than dist staleness elsewhere in this program (which affects only whether a *fix* is reflected, not whether a *feature* exists at all), so verification switched to a new technique: a hand-built HTML harness loading the real, current source CSS files directly via native browser `@import`/`<link>` (no bundler needed — sidesteps the blocked Vite/esbuild path entirely) against markup matching each component's real JSX, run through the same Playwright+system-Edge pipeline used throughout this program. Measured a real, severe WCAG failure — **~1.02:1** (worse than every prior instance of this bug) — confirming the identical sweep-rule footgun a third time: `color-normalization.css`'s `[class*='cta']` sweep and `text-normalization.css`'s `__cta`/plain-anchor rules both don't recognize either class as an already-solid, already-white-text button. Fixed with the same targeted `:not()` exclusions as Cycles 207-208 (re-measured live: 4.10:1, still short of AA). That surfaced a second, genuinely separate, pre-existing defect once the sweep-rule masking was removed: both components' own intended background token, `--cdl-brand-600`, resolves to `#0284c7` in light mode (4.10:1 with white text) and `#0ea5e9` in dark mode (2.77:1) — neither passes WCAG AA's 4.5:1, and dark mode is worse than light. Fixed by switching both components' CTA background to a fixed, theme-constant `#075985` (this codebase's existing `--cdl-info-text`/`--medical-sky-600` value) rather than a theme-swapping token — verified live in both themes: **7.56:1** for both, unaffected by the reception-skill-strip's existing `--critical` red variant (already-passing 4.83:1, untouched). Added a new regression suite section (`fjCriticalBannerCta.contrast.test.ts`, 5 more assertions) proven red-without/green-with via a standalone script. `tsc --noEmit` and `eslint` (frontend + the fixed script) all 0 errors. New verification technique added to the toolkit: a raw-HTML-plus-real-source-CSS harness, for exactly the case where source has outpaced the only available compiled artifact enough that no amount of dist-patching can substitute. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 212 — closed out roadmap item #22's route-audit thread: screenshotted the final 24 previously-unaudited routes (`/start`, `/workspace-dependency-graph`, `/tools/catalog`, `/plugins`, `/dependency-map`, `/dependency-graph`, `/data-lineage`, `/self-diagnostics`, `/saas-health`, `/ai-governance`, `/regulatory`, `/artifacts`, `/memory`, `/fleet/predictive-maintenance`, `/fleet/route-optimizer`, `/ai-evaluation`, `/platform-learning-engine`, `/business-brain`, `/asset-packs`, `/expansion-opportunities`, `/integrations-marketplace`, `/integration-readiness`, `/solution-builder`, `/configuration-studio`) — **all 179 of the app's distinct real routes have now been looked at through this lens at least once**, across 17 cycles (196-212). `/start` re-confirmed the same already-fixed, dist-staleness-only crash first found on `/workspace` in Cycle 207 (`PlatformEntryHub`'s Cycle 154 bug, fixed in source 2026-07-22, simply not reflected in the never-rebuilt `dist/`) — no new investigation needed, already thoroughly documented. `/dependency-graph` showed another live instance of the `CategoryBarChart` axis-label-clipping bug Cycle 211 investigated in depth and deliberately left unfixed (same shared 42-consumer component, same failure shape: only the longest label clips) — recorded as a further confirmed instance, not re-investigated.

Found and fixed a real, small acronym-casing bug on `/saas-health`: health-check cards read "Frontend Health," "Backend Health," ... but "**Api** Health" and "**Ai** Health" — both missing their acronym's correct capitalization. Traced to `src/services/saasHealthApi.ts`'s `SAAS_HEALTH_FALLBACK` constant (rendered whenever the real `/api/saas-health` backend is unreachable, which is unconditional in this static-serve environment and a real, non-rare production scenario too), which derived each label with a blind `${id.charAt(0).toUpperCase()}${id.slice(1)} Health` transform over a small, fixed set of 7 check ids (`frontend`, `backend`, `api`, `integrations`, `tenant`, `ai`, `simulation`) — correct for 5 ordinary-word ids, wrong for the 2 that are themselves acronyms. The exact same bug *shape* Cycle 200 found in `hospitalReadinessAssessment.ts` (a blind `.toLowerCase()` destroying "AI"/"IoT"), just the inverse transform hitting the same 2-acronym blind spot. Fixed the same way Cycle 200 did — explicit, correctly-cased labels per id rather than a smarter-but-still-fragile case-detection heuristic, since the set is small and fully enumerable. Verified via a standalone Node script computing all 7 labels both before and after: confirmed byte-for-byte only the 2 broken ones (`api`, `ai`) changed, the other 5 stayed identical. This component had zero prior test coverage; added `saasHealthApi.test.ts` (3 tests). `tsc --noEmit` and `eslint` both 0 errors, full frontend scope. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 211 — continued roadmap item #22's route-audit batch (running total 141 of 179 going in), screenshotting a fresh batch of 14 previously-unaudited routes (`/auth/forgot-password`, `/auth/magic-link`, `/auth/invite`, `/surveillance/nexus`, `/digital-twin-intelligence`, `/customer-portal`, `/knowledge-base`, `/enterprise-readiness`, `/trackmind`, `/trackmind-maturity`, `/enterprise-platform`, `/platform-intelligence`, `/platform-admin`, `/workflow-mining` — running total now **155 of 179**). Most fell back cleanly to the shared reception/settings/tools pages (expected, consistent with every prior cycle's findings on not-yet-role-wired routes); `/workflow-mining` is a real, distinct page.

Found a real, clearly-reproducible bug on `/workflow-mining`'s "Journey frequency" chart: its diagonal (rotated -20°) x-axis category labels were visibly clipped, e.g. "ED triage calculator to dashboard" rendered as "...ulator to dashboard." Investigated this thoroughly since the shared `CategoryBarChart` component (`src/components/dashboard/DashboardCharts.tsx`) it comes from is used by **42 real pages** across the app — worth understanding precisely before touching something this widely shared. Root-caused to a genuinely multi-layered interaction, not a single simple bug: **(1)** the chart's `margin={{..., left: 0}}` gives rotated, `textAnchor="end"` labels no reserved space to extend into as they swing left; **(2)** independently, the shared `.dashboard-visual-panel` container (already touched once before, Cycle 205) also clips via `overflow: hidden`; **(3)** the app shell's own top-level scroll containers (`app-shell-main-content`, `.emergency-app-shell`) contribute a further, non-negotiable clip boundary that cannot safely be touched at all. Attempted two fix directions and verified each empirically rather than assuming success: increasing the chart's internal `margin.left` (tested via the compiled-JS-literal-patch-and-restore technique, mirroring the established CSS dist-patch pipeline) shifted the plot's internal layout as expected but had non-linear, hard-to-predict side effects — at a large enough value to meaningfully help, category bands compressed enough that adjacent rotated labels started visually overlapping each other, a new problem trading one clipping bug for another. Separately, overriding the shared panel/chart container's `overflow: hidden` to `visible` (tested via an injected style, not the compiled bundle) fixed 2 of the 3 labels completely with no visible side effects, but the 3rd (leftmost, longest-reaching) label still clipped against the untouchable app-shell boundary. Concluded a *safe*, fully-verified fix isn't achievable this cycle: this component's 42-consumer blast radius means a fix needs verification across a real, representative sample of very differently-shaped label data — something this sandbox's blocked JS rebuild path can't support with confidence beyond literal-value bundle patching, and the interacting-but-partial nature of both attempted fixes (each solving part of the problem while risking a different regression) is exactly the kind of finding that shouldn't be shipped half-understood. Documented the full root-cause chain and both partial fix attempts for a future, dedicated cycle rather than force a partial or risky change — the same discipline as Cycle 208's 2 honestly-left-unverified `__cta` candidates. No source files changed this cycle; `git status` confirmed clean before and after besides the two pre-existing untracked concurrent-session directories. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 210 — continued roadmap item #22's route-audit batch (running total 127 of 179 going in), screenshotting a fresh batch of 14 previously-unaudited routes (`/auth`, `/auth-callback`, `/reset-password`, `/verify-email`, `/legal/privacy-policy`, `/legal/terms`, `/legal/gdpr`, `/legal/hipaa`, `/legal/consent-history`, `/medical-iot`, `/fleet/command`, `/fleet/map`, `/digital-twin`, `/version` — running total now **141 of 179**), finally covering this app's untouched auth/legal category. Confirmed `/auth`, `/auth-callback`, `/reset-password`, `/verify-email` all redirect cleanly to `/emergency/reception` — consistent with this app's documented "no exposed login/registration UI" design (per prior project memory), not a bug. `/digital-twin` deep-links into `/emergency/tools?...&open=digital-twin`, an intentional non-ED-workspace redirect. `/version`, `/legal/privacy-policy`, `/legal/terms`, `/legal/gdpr`, `/legal/hipaa` all render clean, real content.

Found and fixed a genuine, previously-uncaught crash on `/legal/consent-history`: the page threw to the app's ErrorBoundary whenever consent status couldn't be loaded (which is unconditional in this static-serve environment, and would also be real whenever a live backend's `/api/compliance/consent` endpoint is unreachable or the `complianceConsent` capability is disabled). Root-caused precisely with a real React server-render reproduction, not guessed: `ConsentHistory.tsx`'s error-state and empty-state branches both called the shared `EmptyState` component with `action={{ label: 'Try Again', onClick: fetchConsentHistory }}` — a plain JavaScript object — but `EmptyState` (confirmed both real implementations in this codebase, `components/ui/EmptyState.tsx` and `components/data-display/EmptyState.tsx`, share the identical contract) renders `action` directly as a child (`{action}`), and React throws `"Objects are not valid as a React child"` for a plain object rather than a `ReactNode`. Also found a second, quieter bug in the same two call sites: `message={error}` was passed instead of `EmptyState`'s real `description` prop, meaning even once the crash is fixed, the actual error text would have silently never appeared (spread onto the DOM as an unrecognized attribute instead). Fixed both call sites by passing a real `<button>` element for `action` and renaming `message` → `description`. Verified with a genuine `react-dom/server` reproduction (not a description of the bug, an actual render): the exact pre-fix JSX shape throws the exact real error message seen live; the exact post-fix shape renders clean HTML with the error text now genuinely visible. Found this component had zero prior test coverage at all — added `ConsentHistory.test.tsx` (3 tests: the disabled-capability error path, the fetch-failure error path, and a real successful render with 4 consent records) via `@testing-library/react`, mocking only the 2 real service/config dependencies. `tsc --noEmit` and `eslint` both 0 errors, full frontend scope — the new test file typechecking cleanly is itself a partial confirmation the fix's prop shapes are correct, on top of the SSR proof. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 209 — resumed roadmap item #22's route-audit batch (running total 113 of 179 distinct routes going in), screenshotting a fresh batch of 14 previously-unaudited routes (`/discover`, `/recommendations`, `/automation`, `/automation-audit`, `/automation-analytics`, `/assistant`, `/ai-command-center`, `/tools`, `/operations`, `/integrations/hub`, `/cosmos`, `/knowledge-graph`, `/3d-viewer`, `/live-map` — running total now **127 of 179**). Confirmed 2 recurring dist-staleness artifacts rather than treating them as new findings: `/ai-command-center` and `/cosmos` both fall back to the shared command-center dashboard and show the exact `.cdl-zone--primary-actions` dark rectangle Cycle 199 already fixed (confirmed via DOM inspection finding the identical class + a source check confirming the `:not(.cdl-zone--primary-actions)` exclusion is present in current `color-normalization.css`); `/integrations/hub` falls back to `/emergency/settings` and shows the same pre-`align-content:start` tall risk card Cycle 206 already fixed (confirmed the fix is present in current `BottleneckPanels.css`). Both are the stale, never-rebuilt `dist/` (2026-07-19) simply predating fixes made after that date — no action needed, no new investigation required since both fixes were already directly re-confirmed present in source this session.

Found and fixed 2 real, substantial bugs on `/live-map` (Live Tracking Map): **(1)** every card in the "Tracked assets" list ran its title, meta line, and subtitle together with zero space (`.live-map__marker-item`'s children — `<strong>`/`<span>`/`<small>`, all inline by default — had no stacking layout at all), reading as "North routeFLEET · occupiedoccupied · CareDroid North Clinic · ETA 18m"; fixed with `display: flex; flex-direction: column; gap: 2px` on the item, confirmed live via dist-patch that the 3 lines now render as intended, separate rows. **(2)** the map canvas's SVG marker labels ("F"/"I") rendered as enormous letters roughly 7x the diameter of their own marker dot — traced to `<text x="2.8" y="1">{...}</text>` in `LiveTrackingMap.tsx` never having had a `font-size` set anywhere, so it fell back to the browser's ~16px default text size interpreted directly as SVG user units inside a 0-100 `viewBox`, next to a radius-2.2 circle; fixed with an explicit `font-size: 2.4px` (SVG user units) on `.live-map-canvas__marker text`, confirmed live via the same dist-patch pipeline that markers now render as small, legible glyphs beside their dots. Separately found the *word*-level half of the concatenation bug — "occupiedoccupied" isn't just missing a space, `occupied` is genuinely repeated — traced to `liveTrackingMapModel.ts`'s `buildFleetTrackingMarkers`: `subtitle: [FLEET_STATUS_TONE[vehicle.status] ? vehicle.status : vehicle.status, ...]` is a tautological ternary (both branches identical), so it unconditionally puts `vehicle.status` first in the subtitle — redundant with the marker-list UI's own `<span>{marker.layer.toUpperCase()} · {marker.status}</span>`, rendered immediately before it. Fixed by deleting the dead ternary and the redundant status entirely from the subtitle (`FLEET_STATUS_TONE` remains meaningfully used elsewhere, for marker-tone coloring, so nothing orphaned); since this is a JS/TS logic change (blocked JS rebuild path), verified via a standalone Node script reconstructing both the exact old and new logic and running all 4 real vehicle statuses from the actual screenshot through both — old always duplicated the status at the seam, new never does. Added 2 new tests to the existing `liveTrackingMapModel.test.ts`. `tsc --noEmit` and `eslint` both 0 errors, full frontend scope. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 208 — closed out the follow-up Cycle 207 flagged rather than opening a new route batch. Re-established design intent for `.profile-copilot-card__cta` (`src/components/profile/ProfileCopilotCard.tsx`, the "Open CareDroid Copilot" button on `/profile`): its own CSS (`profile-surface-normalization.css`) deliberately pairs a light muted surface background with link-colored text — a secondary/ghost button, not a solid-colored one. Live-measured it at the same broken 1.41:1 contrast Cycle 207 found on `fj-critical-banner__cta`, from the identical `color-normalization.css` `[class*='cta']` sweep. This time walked the full matching-rule cascade *before* fixing rather than assuming the same 3-rule shape: since this element is a real `<button>` (not an `<a>`), `text-normalization.css`'s plain-anchor rule doesn't match at all, and its `__cta`-specific rule *does* match but happens to already resolve to the exact same `--medical-text-link` token the component's own CSS wants — so only the background-painting sweep rule was actually wrong here, confirmed by patching only that one rule and re-measuring live (contrast rose to 6.60:1, confirmed both numerically and via a fresh screenshot showing a clean, readable secondary button) before adding the second, unnecessary exclusion. Added `:not(.profile-copilot-card__cta)` to the one rule that needed it, plus a 2-assertion regression test proven red-without/green-with the fix via a standalone script.

Also attempted to close out the 2 other `__cta` candidates Cycle 207 left "unverified" (`reception-job-profile-card__cta` on `/profile`, `reception-skill-strip__cta` on `/emergency/reception` — both gated behind a reception-facing demo role, and the default demo persona in this static-serve environment is a physician). Found and tried this repo's own existing QA tooling for exactly this (`scripts/capture-reception-screenshots.mjs`'s `localStorage`-seeded `caredroid_user_profile`/`caredroid_access_token` demo-auth pattern) — confirmed the key names are still correct (`AUTH_CONFIG.tokenStorageKey`/`userProfileStorageKey`) but the role switch still didn't take: `UserContext.tsx`'s `readStoredUser()` runs every parsed profile through `hydrateStoredDemoUser()`, which evidently needs a richer shape than that script's flat `{id, email, name, role, ...}` payload provides. Concluded the script itself is stale relative to the current user-hydration logic (the same class of drift this program has repeatedly found in its own QA infrastructure — orphan-detection's `.jsx`-only filter, `NotificationService.test.ts`'s legacy fixture) rather than pushing further to reverse-engineer the exact expected shape, which would be its own dedicated investigation. Left both candidates unfixed and honestly still-unverified rather than either speculatively patching source CSS with no live confirmation, or quietly dropping them. `tsc --noEmit` and `eslint` both 0 errors, full frontend scope. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 207 — continued roadmap item #22, but first corrected a real accounting error in the thread's own running total. Reconstructing exactly which real routes Cycles 196–206 covered (cross-referencing each cycle's named-page list against `qa/full-app-crawl/report.json`'s real 182-path inventory, and the exact per-cycle screenshot filenames still on disk for Cycles 203–206) found substantial unrecognized overlap: at least 12 route leaf-names — `triage`, `alerts`, `laboratory`, `simulation`, `billing`, `notifications`, `security`, `audit`, `dashboard`, `staff`, `reports`, `timeline` — were independently screenshotted in *both* an earlier cycle (198–200, a prior session) *and* a later one (203–204, this session) without either cycle recognizing the repeat, meaning the claimed "114 of 182" figure double-counted real routes rather than reflecting 114 *distinct* ones. Rebuilt the true covered set from scratch: 56 routes are exact-path-verified (Cycles 203–206, cross-checked against the on-disk `batch6`–`batch9` screenshot directories from this session), plus 43 more from best-effort fuzzy-matching Cycles 196–200's named pages against the real path list — **99 of 182 confirmed/likely covered, not 114** (also noticed the crawl's own 182-entry inventory has 3 literal path duplicates — `/emergency/whiteboard`, `/emergency/intake`, `/emergency/queues`, each listed twice under different route names — so the true distinct-path denominator is 179, not 182). Corrected the note here rather than continuing to compound the inflated figure, the same discipline as the Cycle 153 registry-count correction.

With an accurate baseline, screenshotted a fresh batch of 14 routes confirmed absent from every prior cycle's real coverage (`/emergency/intake`, `/emergency/patient-room`, `/emergency/copilot`, `/emergency/provincial-health`, `/emergency/federated-learning`, `/emergency/digital-twin`, `/emergency/ai-governance`, `/emergency/tools`, `/emergency/ed-readiness`, `/emergency/diagnostics`, `/emergency/integrations`, `/workspace`, `/operations-center`, `/documentation` — running total now **113 of 179** distinct real routes). `/workspace` and its redirect target `/start` both threw the app's ErrorBoundary ("Something went wrong") — investigated immediately, confirmed via source grep, `git log`, and the actual diff that this is `PlatformEntryHub`'s exact Cycle 154 crash (`useRouteChrome must be used within RouteChromeProvider`), already fixed in commit `a5bd6e2f` on 2026-07-22 with its own regression test, resurfacing only because the static-served `dist/` build (2026-07-19) predates that fix by 3 days. A confirmed dist-staleness false positive, not a new bug — no action taken, the program's 3rd such correctly-ruled-out finding (after Cycles 204 and 206).

Found and fixed a genuine WCAG contrast failure on a safety-critical control: the "Acknowledge" button for unacknowledged critical alerts (`.fj-critical-banner__cta` in `FullJourneyOperatingPage.tsx`, live on 4 real routes — `ed-readiness`, `diagnostics`, `handoffs`, `reports`) rendered at **~1.41:1 contrast** (dark teal text on dark navy background) instead of its own deliberately-designed white-on-red styling — measured precisely via `getComputedStyle` and the real WCAG relative-luminance formula, not eyeballed. Root-caused to *three* independent app-wide CSS sweep rules all matching this one element because its class contains the literal substring "cta": `color-normalization.css`'s `[class*='cta']` sweep (repaints background+color for anything "CTA-like"), plus *two* separate rules in `text-normalization.css` (a plain `a:not([class*='btn'])...` anchor-color rule, and a `[class*='__cta']:not(--solid):not(--primary))` rule assuming solid buttons are always BEM-flagged accordingly) — all three fired because this component doesn't follow the assumed `--solid`/`--primary` naming convention despite genuinely being a solid-filled button. This is the identical substring-match sweep-rule footgun class Cycle 199 first diagnosed (`[class*='--primary']` catching `.cdl-zone--primary-actions`), now confirmed recurring across a second, independent selector family in the same file. Fixed by adding `:not(.fj-critical-banner__cta)` to all three rules, restoring the component's own intended styling — verified live via the dist-patch-and-restore pipeline (contrast measured 1.41:1 before, **6.47:1 after**, comfortably clearing WCAG AA's 4.5:1 threshold), with a fresh zoomed screenshot confirming the button now reads clearly. Added a 4-assertion regression test (`fjCriticalBannerCta.contrast.test.ts`) covering all 3 exclusions plus the component's own unchanged styling, proven red-without/green-with the fix via a standalone script since Vitest remains blocked. While investigating, also found `.profile-copilot-card__cta` on `/profile` live-measured with the identical broken color pair — but tracing its root cause showed its background half is entangled with a different, deliberate light-muted-surface design intent (not a critical-red case like this fix), needing its own visual judgment call rather than a mechanical copy of this fix — flagged, not fixed, this cycle. `tsc --noEmit` and `eslint` both 0 errors, full frontend scope. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 206 — continued roadmap item #22, screenshotting a fresh batch of 14 previously-unaudited `/emergency/*` operational subpages (`patients`, `journey`, `reception`, `capacity`, `boarding`, `referrals`, `collaboration`, `dispatch`, `handoffs`, `documentation`, `pulse`, `shift`, `settings`, `help` — running total now **114 of 182** real routes). First chased what looked like the most severe bug found in this entire thread — `/emergency/patients`'s patient cards showed badly garbled, overlapping text ("Waiting foNext: cReassess" / "assessment") — through a deep investigation that ultimately confirmed it as a **dist-staleness false positive**, not a live bug: the compiled `dist/assets/emergency-route-*.js` bundle still has `PatientCard`'s badges (`PatientExperienceStatusBadge`, `WhatHappensNextBadge`) rendered directly inline inside an always-visible `.patient-card__priority-strip--graphic` div, but current source (`src/components/PatientCard.tsx`, last touched Cycle 153, well before this dist build) has already moved these exact badges into a collapsed-by-default `<details className="patient-card__more-signals">` disclosure with zero trace of the old `--graphic` structure remaining — confirmed by literal grep finding the old structure only in the compiled bundle, never in current `.tsx` source, and by a sibling page (`/emergency/boarding`'s row-layout view) rendering the identical status/next-action pair cleanly with no overlap. Correctly took no action on this finding — recorded it as a confirmed non-issue rather than either a silent skip or a redundant fix.

Two real, verified bugs found and fixed in the same batch. **First**: `/emergency/settings`'s "3-minute target" risk indicator rendered as a 711px-tall red box holding just 3 short lines of text, spread apart with huge gaps instead of grouped compactly — root-caused via live `getComputedStyle` measurement to a genuine CSS Grid default-value interaction: `.bottleneck-risk` (`src/components/bottlenecks/BottleneckPanels.tsx`'s `ThreeMinuteRiskIndicator`) sits in a 3-column grid (`.emergency-settings__bottleneck-admin`) alongside two content-heavy sibling columns; the grid's default `align-items: stretch` forces this narrow card to match its tallest sibling's height, and since `.bottleneck-risk` is itself `display: grid` with no `align-content` set, CSS Grid's *own* default value (`align-content: stretch`) then spread its 3 short `auto`-sized rows (label/status/summary) to fill that entire forced height — measured each row inflating to ~224px for text that needs ~25px. Fixed with one line, `align-content: start`, verified live via the dist-CSS-patch-and-restore pipeline: measured each row collapse from ~224px back to its natural 23-35px, then confirmed visually via a fresh screenshot that all 3 lines now sit compactly at the top of the card exactly like its sibling columns, with the remaining height simply unused background (a normal, common "some cards are taller than their content" pattern, not a broken one). **Second**: `/emergency/referrals` showed a visible double-period typo — "...authentication redirect.. Showing the last local referral queue..." — from `src/components/ReferralPanel.tsx` literally concatenating `{referralsModule.error}` (an `apiClient.ts`-sourced message that already ends in its own period) with a literal `. Showing the last local referral queue...` suffix. Fixed by stripping any existing trailing period(s) before appending (`.replace(/\.+\s*$/, '')`), verified via a standalone script proving correct single-period punctuation across 4 real message shapes this codebase actually produces (some ending in a period, some not) — a low-risk, purely additive fix; confirmed no test anywhere asserts the old text. `tsc --noEmit` and `eslint` both 0 errors, full frontend scope. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 205 — continued roadmap item #22, screenshotting a fresh batch of 14 previously-unaudited routes (`workflows`, `search`, `feature-flags`, `system-health`, `governance-registry`, `human-review`, `training`, `costs`, `brain`, `organization-intelligence`, `competencies`, `credentials`, `clinical-decision-support`, `predictive-analytics` — running total now **100 of 182** real routes). Found a real, reproducible chart-rendering bug affecting at least 2 pages: `/competencies` and `/credentials` both render a "status mix" bar chart whose entire body was invisible — the panel showed only its title/description then cut straight to the next section, with just 1-2 bare Y-axis gridline labels visible. Root-caused via direct DOM/computed-style inspection rather than guessing: the chart's real SVG (Recharts, `width=100% height={240}` explicitly set) genuinely renders in both cases, but its ancestor `.dashboard-visual-panel` — a CSS Grid item with `overflow: hidden` — was computing an actual height of just **42px** on `/competencies` (and 158px, still clipped, on `/credentials`), because per the CSS Grid/Flexbox spec, a grid item with `overflow: hidden` gets an *implied automatic minimum size of 0* rather than sizing to its content's natural height — confirmed by comparing against a working chart (`/costs`'s "Route mix", correctly 316px) whose panel happens to sit inside an intermediate chart-specific grid wrapper (`.cost-page__charts`) that the 2 broken pages don't have; both broken panels are instead *direct* items of their page's single top-level grid, where the collapse manifests. Fixed at the shared component level — `src/components/dashboard/DashboardVisualizations.css`'s `.dashboard-visual-panel` rule — by adding `.dashboard-visual-panel:has(.dashboard-chart) { min-height: 300px; }`, scoped specifically to chart-bearing panels (verified `:has()` is already an established pattern in this codebase, used in 5+ other files) so panels without a chart are completely unaffected; the 300px floor was derived from the chart wrapper's own fixed 210px height plus its header/description/padding, matching the real working panel's ~316px closely. This is a CSS-only fix, so — unlike the last 2 fixes in this thread — it could go through the full, established dist-CSS-patch-and-restore verification technique: patched the exact compiled selector into the built `dist/assets/DashboardVisualizations-*.css`, reloaded both pages, confirmed via `getComputedStyle` that the panel height jumped from 42px/158px to a full 300px and the parent grid row resized to match, then took fresh screenshots confirming both charts now render with real, visible, correctly-colored bars and x-axis category labels exactly like every other working chart on the platform — restored the build artifact byte-for-byte afterward (diffed to confirm). This fix applies at the shared component CSS level, so it protects all 36 real pages using `VisualizationPanel` in this codebase, not just the 2 confirmed-broken ones — the other ~34 were not individually re-audited this cycle to count how many were coincidentally already safe by virtue of their specific grid nesting. `tsc --noEmit` and `eslint` both 0 errors, full frontend scope (a CSS-only change, so this confirms nothing else broke rather than validating the fix itself — the live dist-patch screenshot is what validates the fix). See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 204 — continued roadmap item #22, screenshotting a fresh batch of 14 previously-unaudited routes (`dashboard`, `intake`, `queue`, `triage`, `alerts`, `staff`, `analytics`, `reports`, `protocols`, `research`, `laboratory`, `simulation`, `devices`, `timeline` — running total now **86 of 182** real routes). First confirmed a non-finding precisely rather than assuming: `/dashboard` showed the exact same dark-navy `.cdl-zone--primary-actions` rectangle Cycle 199 already fixed in source — traced via DOM inspection to the same class, confirmed the `:not(.cdl-zone--primary-actions)` exclusion is genuinely present in current `src/styles/color-normalization.css`, and concluded this is the *stale `dist/` artifact* still showing pre-fix CSS (Cycle 199's own verification technique explicitly restored `dist/` byte-for-byte after testing, since a real rebuild is blocked) — not a new bug, and useful confirmation the fix's real blast radius extends beyond the pages Cycle 199 originally checked. Then found a genuinely new, higher-severity bug via a different investigation thread: `/alerts` showed real situation-brief numbers ("13 active alerts," "13 need acknowledgement") but rendered *no alert list at all* below them — reading `ClinicalAlertsPage.tsx`'s source confirmed it builds a real, rich `activeWork` block (summary chips, search, controls, the actual alert cards) and passes it to the shared `EmergencyRoutePage` wrapper via an `activeWork={...}` prop. Confirmed live via DOM inspection that `ClinicalAlertsPage` genuinely *is* the mounted component (its own CSS class is present) but `.alerts-summary-strip`/`.alerts-controls` are completely absent — not empty, absent. Root-caused precisely: `EmergencyRoutePage` (`src/pages/emergency/emergencyRouteShared.tsx`) never declared an `activeWork` prop at all — only `children` — and since the whole props object is typed `any`, nothing caught the mismatch; the content was silently dropped every time. Grepped for every real usage of `<EmergencyRoutePage` and found a **second, independent instance of the identical mistake** in `HospitalCommandCenter.tsx` (the Command Center page's entire "Sentinel EMS command" section), confirming this isn't a one-off typo but a genuine footgun in the component's own API — the internal `zones` object it builds literally has a key named `activeWork`, which is exactly what a page author reading that code would reasonably (but incorrectly) assume is also a real prop name. Fixed at the root rather than patching either call site: added `activeWork` as a real, recognized prop on `EmergencyRoutePage` that falls back to `children` (`activeWork ?? children`) — confirmed via a direct check that neither broken call site also passes `children` (both are self-closing JSX tags), so this is a strictly additive fix with zero risk of duplicate/conflicting content. This one-line, two-file-blast-radius fix makes two previously-half-broken pages whole again with no changes needed to either page's own source. Added a new regression suite, `emergencyRouteShared.activeWork.test.tsx` (3 tests, `@testing-library/react`, mocking the component's real dependency hooks by their verified real export shapes — named vs. default — checked against source before writing the mocks, not assumed): confirms content passed via `activeWork` now renders (the fix), confirms content passed via `children` still renders identically (zero regression to the 4 other, already-correct call sites), and confirms neither-prop-given still correctly renders nothing (no false positive). Since frontend Vitest is still blocked by the same `esbuild` sandbox issue, supplemented with a standalone Node script proving the exact `activeWork ?? children` resolution across all 3 real-world call patterns byte-for-byte. `tsc --noEmit` and `eslint` both 0 errors, full frontend scope. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 203 — resumed roadmap item #22 (visual/UI polish) after 2 cycles on item #23, screenshotting a fresh batch of 14 previously-unaudited routes via the established static-`dist/`-serve + real-Edge pipeline (`billing`, `usage`, `notifications`, `security`, `audit`, `assets`, `ai-models`, `platform-analytics`, `department-intelligence`, `service-lines`, `product-intelligence`, `success-center`, `profile`, `help-center` — bringing the running total to **72 of the app's 182 real routes** (`qa/full-app-crawl/report.json`'s Cycle 154 crawl gives an exact figure now, tightening the prior "~190+" estimate). Found a real bug via a same-root-cause side-by-side comparison rather than a single page in isolation: `/notifications` rendered a raw, unstyled browser exception directly in the page — `Unexpected token '<', "<!DOCTYPE "... is not valid JSON` — while `/security` and `/audit`, hitting the exact identical underlying condition (this static-serve environment has no live backend, so every API call resolves to the SPA's own `index.html` fallback instead of JSON), showed a clean, designed message: "The API returned an HTML page instead of JSON. Check the API URL, proxy, backend availability, or authentication redirect." Root-caused, not just patched at the display layer: that friendly message comes from `src/services/apiClient.ts`'s shared `parseApiResponse` helper, which detects an HTML body/content-type *before* attempting `JSON.parse` and throws a designed `ApiResponseError` instead of raw `SyntaxError` — `/security`/`/audit`'s governance-dashboard component already uses it correctly, but `src/services/NotificationService.ts` was inconsistent: `getUnreadCount`, `markAllAsRead`, `fetchDevices`, and `toggleAllNotifications` already call `parseApiResponse` correctly, while 4 sibling methods on the exact same object — `fetchNotificationHistory`, `markAsRead`, `getPreferences`, `updatePreferences` — called raw `response.json()` directly, bypassing the HTML guard entirely and letting the browser's own `SyntaxError` propagate straight into `NotificationPreferences.tsx`'s `Promise.allSettled` error-surfacing path unguarded. Fixed all 4 by applying the file's own already-correct pattern (`const data = await parseApiResponse(response, {fallback:{}}); if (!response.ok) throw new Error(data?.message || getApiErrorMessage(null, response)); return data;`), zero invention — confirmed via a repo-wide grep that no test anywhere asserts the 4 hardcoded strings being replaced. While tracing test coverage for this fix, found the *same class of blind spot* Cycle 201 found in the AI-chat DTO: `src/test/NotificationService.test.ts` (the only file with a matching name) actually imports and tests an unrelated `./fixtures/legacyNotificationService` fixture, not this real service at all, and `NotificationPreferences.test.tsx` mocks `NotificationService` wholesale at the module boundary — meaning these 4 REST methods' real HTTP-parsing behavior had **zero real test coverage**, exactly why this bug was invisible. Wrote a new dedicated suite, `NotificationService.rest.test.ts` (6 tests), mocking only the actual network boundary (`apiFetch`) while keeping the real `parseApiResponse`/`getApiErrorMessage` logic live — the correct mock boundary for catching this exact bug class, unlike the existing component test's boundary. Since frontend Vitest is still blocked by the same `esbuild` sandbox issue (re-confirmed fresh) and even a plain `tsx` import of `apiClient.ts` hangs on its own dev-session/backend-reachability side effects outside a real browser, verified with a standalone Node script reconstructing `isProbablyHtml`/`parseApiResponse`/`ApiResponseError` verbatim from the real source: proved byte-for-byte that the OLD method logic reproduces the exact observed bug string, the NEW logic produces the exact friendly message already shown on `/security`/`/audit`, a genuine success response still parses and returns correctly (no regression), and a real backend-supplied error message on an actual 404 still surfaces correctly rather than being swallowed. `tsc --noEmit` and `eslint` both 0 errors, full frontend scope. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 202 — investigation-only follow-up to Cycle 201's finding, closing its natural next question: is the `stream`-field DTO-mismatch bug class isolated to one endpoint, or does it recur elsewhere in real AI traffic? Restarted the real compiled backend (same `NODE_ENV=production` + dev-auth-bypass configuration as Cycle 201) and checked the app's *other* "canonical" AI entry point, `POST /api/ai/node` (structured intent-based requests — `src/services/careDroidAiApi.ts`'s `transportCareDroidAINode`). Statically compared the frontend's `CareDroidAIRequest` TypeScript interface (`lib/ai/careDroidAITypes.ts`: exactly `intent`/`input`/`context`, nothing else) against the backend's `CareDroidAINodeDto` (`backend/src/modules/ai/dto/ai.dto.ts`: the identical 3 fields) — a clean 1:1 match, unlike the conversational endpoint's DTO, which was a hand-duplicated shape that had silently drifted from its own client's object-literal construction. Didn't stop at the static read: fired a real request through the real backend with a real `dev-session` JWT and the exact real payload shape — cleared validation with no 400, and reached the same real, legitimate `subscription-required (professional)` entitlement gate Cycle 201 already found and correctly left alone, confirming the request shape itself is genuinely fine. **Conclusion: the bug class doesn't recur here.** Root cause is now precise, not just "a DTO was wrong": it's specifically hand-duplicated request shapes with no compile-time link between a frontend object-literal builder and a backend DTO class — `/api/ai/node/conversational`'s DTO had that shape (independently hand-authored, drifted), `/api/ai/node`'s DTO doesn't (both sides reference/mirror the same 3-field shape). Also noticed, while transport-layer reading `careDroidAiApi.ts`, that `transportCareDroidAINode` has a try/catch that falls back to a local, frontend-only AI implementation (`runCareDroidAI`) on *any* backend failure — including a validation 400 like Cycle 201's — meaning a bug at this specific endpoint would have been additionally masked by looking like it "still works," just via a silent local fallback, rather than an obvious visible failure. Worth being aware of for any future investigation of this transport path, though not itself a bug (a deliberate, reasonable resilience pattern). Checking every one of the ~146 real POST/PATCH/PUT endpoints catalogued in `frontendApiCallsInventory.ts` for the same hand-duplication risk is out of scope for a single cycle — flagged as a pattern to stay alert to (added as a note on roadmap item #23) rather than claimed as exhaustively audited. No code changes this cycle; `git status` confirmed clean before and after besides the two pre-existing untracked concurrent-session directories. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 201 — per a direct request to "make sure the AI is interactive within the platform," did something no prior cycle in this 200-cycle program had ever done: drove a genuinely real, fully unmocked round trip — real compiled backend + real compiled frontend, real registration/dev-session bootstrap, real Playwright browser (system Edge) — through the Copilot chat, instead of relying on unit tests (which mock the HTTP layer), hermetic e2e specs (which stub the network at `page.route()` before any request reaches a real server), or static analysis. First confirmed something genuinely positive: the pre-existing `e2e/interactive-ai.spec.mjs` suite (IX12-14 — named streaming phases, keyboard-operable workflow-card Acknowledge, EMS inbox assign/comment collaboration, typed AI command-palette dispatch, offline/online resilience, 0 serious/critical a11y violations) had not been runnable since before Cycle 160's `esbuild` sandbox block appeared (last real run recorded 2026-07-18) — re-ran it fresh via the static-dist + real-Edge pipeline with `QA_REUSE_SERVER=true`, and it passed clean, 8/8, for the first time in ~10 days and dozens of cycles. Then went further: built and ran the actual backend (`nest build` + `node dist/backend/src/main.js`, unrelated to the blocked Vite/esbuild path — Nest's own build system was never affected), discovered it also serves the frontend's static `dist/` from the same origin when `NODE_ENV=production` (`registerProductionFrontendAssets` in `backend/src/main.ts`), and drove the two together for a real integration test never attempted before. Found and fixed a genuine, total, unconditional break in the core AI-chat feature: the browser client's real request body (`src/lib/ai/client.ts`'s `bodyForBackendRequest`) always includes a `stream` key — even `stream: false` — but the backend's `UnifiedNodeConversationalDto` (`backend/src/modules/chat/emergency-ai.controller.ts`) never declared it, and the app's global `ValidationPipe` (`whitelist:true, forbidNonWhitelisted:true`) rejects requests by property *presence*, not value — so **every single real call to `POST /api/ai/node/conversational` failed with 400 "property stream should not exist," unconditionally, streaming or not**, for every requestType this endpoint serves (Copilot chat, Smart Intake AI, referral summaries, analytics). Traced via `git log -S` to commit `9434922c` (2026-06-13) — this has been broken for 6+ weeks and ~120+ cycles, invisible to every prior test because the only existing spec touching this DTO (`emergency-ai.controller.spec.ts`) asserts against the *source text* (`readFileSync` + `toContain`), never against a real `class-validator` pass. Fixed by adding `stream?: boolean` to the DTO (`@IsOptional() @IsBoolean()`, matching the client's real intent — the field isn't wired to an actual SSE response at this endpoint yet, but declaring it unblocks real traffic without a larger streaming-implementation change). Wrote a genuine regression test, `emergency-ai-unified-node.dto.spec.ts`, that validates the *exact* real client payload shape through `class-validator` directly (not a source-text check) — confirmed it fails red without the fix (reverted it live, re-ran, watched it fail with the exact "stream should not exist" error, then restored) before trusting it. Continued past this fix to fully close the verification loop: with the DTO fixed, the same real request advanced from 400 → 403, surfacing a second, *legitimate, intentional* gate — `agent-clinical` requires `SubscriptionTier.PROFESSIONAL` per `backend/src/config/entitlements.config.ts`'s explicit, documented business-tier config (`featureFlagId: 'ai-clinical-copilot'`) — confirmed via source, not assumed, this is deliberate SaaS monetization, not a bug. Temporarily bumped one disposable local dev-DB test row to `professional` tier purely to prove the endpoint now resolves past validation and tier-gating into a third, also-legitimate `pack-required` entitlement check, then reverted the row byte-for-byte. Also separately confirmed a raw self-registration path (`/api/auth/register` + `/api/auth/login`) hits a *different*, expected dead end (`Organization context is required` — no org gets auto-provisioned for a bare self-registered account) — not a bug, since this app has no exposed login/registration UI at all (`deriveAuthMode`/`isRealAuthToken`/`isOpenAccessUser` in `src/auth/authSession.ts` are deliberately hardcoded post-"auth UI removal," per existing git history) — the real, intended local/demo entry point is `POST /api/auth/dev-session`, which *does* auto-provision a consistent real org+workspace+user and was what surfaced the actual, fixable bug. Verified: new DTO spec 1/1 (3/3, incl. the red/green proof), pre-existing `emergency-ai.controller.spec.ts` still 1/1, backend `tsc --noEmit` 0 errors, backend `eslint` 0 errors, full backend suite re-run clean (see below for count). Opened new roadmap item #23 documenting both the fix and the new full-stack-integration verification technique this cycle established. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 200 — continued roadmap item #22, screenshotted 14 more real pages (58 of ~190+ routes now looked at across 5 cycles), and found the first content/copy bug in this thread rather than a layout/CSS one: the Hospital Maturity Assessment page (`/maturity-assessment`) asks "How mature is your hospital's ai maturity?" and "...iot readiness?" — both visibly wrong-cased mid-sentence. Root-caused, not just re-cased: `src/data/hospitalReadinessAssessment.ts`'s question template blindly called `dimension.label.toLowerCase()` on the whole label string to make it read grammatically mid-sentence ("Digital maturity" → "digital maturity" — correct for 4 of 6 dimensions), but this indiscriminately also lowercased the 2 dimensions whose labels contain acronyms/mixed-case terms — "AI maturity" → "ai maturity" and "IoT readiness" → "iot readiness" — which should never be lowercased. Since this is a small, fixed, fully-enumerable set of 6 hardcoded dimensions (not dynamic/user-generated data), fixed by adding an explicit `midSentenceLabel` field to each dimension with the already-correct casing, rather than reaching for a smarter-but-still-fragile string-transform heuristic (e.g. "leave the first word alone if it's 2+ consecutive capitals") — data that's simply correct by construction beats detection logic for a fixed, small set. Left the existing `label` field (used elsewhere as a real heading/`aria-label`, confirmed via grep, e.g. `<h2>{dimension.label}</h2>` in `MaturityAssessment.tsx`) completely untouched. This fix is a JS/data change, not CSS, so it can't be verified via the dist-CSS-patch-and-screenshot technique Cycles 196-199 used (a JS bundle rebuild hits the identical blocked `esbuild`/Vite path) — instead verified mechanically, appropriate to a pure string-value bug: ran a standalone Node script computing all 6 question strings both before and after the fix, confirming byte-for-byte that only the 2 broken strings changed (now correctly "AI maturity"/"IoT readiness") and the other 4 are identical to before, a complete proof for a bug that's entirely about string content rather than layout/rendering geometry. `tsc --noEmit` and `eslint` both 0 errors on the changed file. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 199 — continued roadmap item #22, screenshotted 14 more real pages (44 of ~190+ routes now looked at across 4 cycles), and found the highest-blast-radius bug of the thread so far: a large, solid dark-navy rectangle with no content, consistently occupying the right portion of the "Critical actions" panel on every command-center-style page (Whiteboard, command-center, and the executive/admin fallback pages, which all share this panel). Diagnosed via DOM inspection, not assumed to be a missing-data artifact of the static-serve environment: the dark background belonged to `.cdl-zone--primary-actions` itself (`rgb(55,65,81)`/`#374151`), a layout-grid *section* wrapping the actual white "Critical actions" cards — meaning the section was painted dark everywhere the (few, demo-limited) cards didn't happen to cover it. Root-caused to a real, if not obviously visible, cross-cutting CSS bug rather than a per-page issue: `src/styles/color-normalization.css` has a deliberate, documented sweep rule (`role-accent-theme.css`'s own comment explains it) styling every BEM `--primary` button modifier dark navy via `[class*='--primary']` — a plain substring match with no word-boundary check — and `cdl-zone--primary-actions` (a layout *zone* name, not a button) happens to contain that exact substring, so the sweep silently painted the whole zone's background instead of just its intended button children. Confirmed via grep that this was the only accidental victim among 15 real classes containing `--primary` in the codebase (14 are genuine button/CTA modifiers the rule is correctly meant to catch) — and separately checked the rule's sibling `[class*='cta']` selector for the same collision risk, finding none (every `*cta*` class in the codebase is a genuine call-to-action, no accidental substring hits). Fixed with a targeted `:not(.cdl-zone--primary-actions)` exclusion, following the exact pattern the rule already uses for its one previously-known false positive (`:not(.reception-desk-toolbar__action)`) — a one-line, minimal, precisely-scoped fix that doesn't touch the deliberate invariant itself. Verified with the same dist-patch-and-screenshot technique as Cycles 196-198: patched the exact compiled selector in the built CSS, reloaded the Whiteboard page, and confirmed via both `getComputedStyle` (background now `rgba(0,0,0,0)`, fully transparent) and a live screenshot that the dark rectangle is completely gone, replaced by normal page background around the "Critical actions" card. Restored the build artifact byte-for-byte afterward. `tsc --noEmit` 0 errors. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 198 — continued roadmap item #22, screenshotted a further 12 real pages (30 of ~190+ routes now looked at across 3 cycles) and found a third real visual bug, this time a mid-word text-wrap break rather than a hard clip: on the Clinical Calculator Hub (`/tools/calculators`), category badges like "GENERAL" rendered as "GENERA" on one line and a lone "L" on the next. Root-caused via computed styles, not guessed: `.clinical-calculator-card__category` (`practitioner-compact.css`) is a sibling of the card's `<strong>` title inside a `display: flex` header row (`.clinical-calculator-card__header`), and — unlike the title, which has natural word-break points and already wraps gracefully — the badge had no `flex-shrink: 0`, so on cards with a long title (e.g. "ACS Workflow Assistant") it got squeezed by the flex algorithm below its own natural content width (measured: 44px, well under what "GENERAL" needs at 11px/700-weight/uppercase), and with `overflow-wrap: break-word` already active and no space to break at, it split mid-letter instead. Fixed with `flex-shrink: 0; white-space: nowrap` on the badge, so it always renders at its natural width and the already-flexible title absorbs any space pressure instead (the correct direction, since word-wrapping a multi-word title looks intentional while breaking a single-word badge mid-letter never does). Verified with the same dist-patch-and-screenshot technique as Cycles 196-197: confirmed all 4 visible category badges on the page — including "RESPIRATORY", 11 characters, longer than "GENERAL" — rendered correctly on one line after the patch, restored the build artifact byte-for-byte afterward, and confirmed via grep this is the class's only definition anywhere in the codebase (no competing rule to reconcile). `tsc --noEmit` 0 errors. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 197 — continued roadmap item #22's visual-polish thread, screenshotted 12 more real pages (18 of ~190+ routes now looked at), and found a significantly higher-impact bug than Cycle 196's: the Copilot launcher button (`.ed-copilot-launch`, rendered by `AppShell.tsx` on every page whenever the Copilot panel is closed) rendered as an unstyled, full-viewport-height, 131px-wide block along the entire right edge of the screen instead of the small rounded pill its own CSS clearly designs it as (`position: fixed; bottom: 16px; right: 16px; border-radius: 999px`). Root-caused rather than just patched around: the button's CSS lived in `CopilotPanel-part-02.css`, which is only ever loaded as a side effect of `AppShell.tsx`'s `lazy(() => import('./CopilotPanel'))` — but the launcher button specifically renders when the Copilot panel is *not* open, meaning the lazy import (and its CSS chunk) has never fired, meaning the button's real styling had never loaded. Confirmed by inspecting the live DOM (`getComputedStyle` showed `position: static`, not `fixed`, with the browser's default button box then stretched full-height by the app shell's flex layout) and by confirming in `dist/assets/` that `.ed-copilot-launch`'s base rule exists only in the separate `CopilotPanel-*.css` chunk, never in the main bundle — this is a real architecture bug that would reproduce for every real user, on every page, for their entire first session until they happened to open Copilot once, not an artifact of the static-serve verification method. Fixed by moving the rule (and its `:hover`/`:focus-visible` pair) out of the lazy-loaded stylesheet into `app-shell.css`, which `AppShell.tsx` already imports eagerly — the correct home, since that's the component that actually renders this button. Verified end-to-end with the same dist-patch technique as Cycle 196, this time moving the rule between the two real build chunks (not just editing one file) to mirror the source change exactly: reloaded the page fresh (no Copilot interaction, so the lazy chunk genuinely never loads) and confirmed via both a live screenshot and `getComputedStyle` (`position: fixed`, `border-radius: 999px`, `165×47px`, positioned bottom-right) that the button now renders exactly as designed — the entire right-edge grey bar is gone, replaced by normal page background with a small floating pill in the corner. Restored both build artifacts byte-for-byte afterward (diffed against pre-patch backups to confirm). Also re-ran `tsc --noEmit` (0 errors) and grepped to confirm the class's only other reference (`practitioner-compact.css`'s compact-mode override) is unaffected since it was already eagerly loaded. See Cycle log below).**

**Last updated: 2026-07-28 (Cycle 196 — first cycle explicitly aimed at visual/UI polish rather than backend correctness, per a user request to hold the app to a genuinely high, "top-notch" design bar and keep scanning for gaps against it. This requires actually looking at the app, not just static analysis — so first re-confirmed, fresh, that `npm run dev` still hits the exact same `esbuild`/Application Control `spawn UNKNOWN` documented since Cycle 160 (real error captured this cycle: `vite.config.ts` fails to load, `ensureServiceIsRunning` → `esbuild/lib/main.js` → `ChildProcess.spawn` → `Error: spawn UNKNOWN`) — still fully blocked, not stale information. Found a real workaround that doesn't need Vite at all: served the existing (8-day-stale but structurally current) production build in `dist/` with a plain static file server, then drove the real system Edge browser via Playwright (`playwright-core` + `channel: msedge`, the same technique `scripts/audit-full-app-crawl.mjs` established) to actually screenshot 6 real pages. Found a genuine visual defect on the Whiteboard: the header's operational status-chip rail (`ChromeStatusChips.tsx` → `.alarm-kpi-rail` in `alarm-system.css`) is `overflow-x: auto` with no visual cue that it scrolls, so when its chips don't all fit (measured: 346px of content in a 288px-wide rail), the last chip gets hard-clipped by the container edge mid-word — confirmed via DOM measurement, not guesswork, that the label `"Breached"` (the correct string, per `resolveCompactHeaderMetricLabel`'s own test) renders visually as `"Breache"` with no ellipsis, no fade, nothing — reads exactly like a rendering bug, not "scroll for more." Fixed with a `mask-image` edge fade (fades alpha, not a background color, so it's correct in both themes and behind any content) — purely additive CSS, touches nothing else, can't regress layout or behavior. Verified the fix actually renders correctly despite the Vite block still applying to any real rebuild: patched the already-built, gitignored `dist/assets/*.css` with the identical rule (restored byte-for-byte afterward), reloaded through the same static-serve+Edge pipeline, and screenshotted the exact pixels — the clipped text and chip border now visibly soften into the background instead of hard-cutting, confirmed by eye at 4x zoom on the literal edge in question, not inferred from a diff. Confirmed via grep this is the only real consumer of the bare `.alarm-kpi-rail` class (4 other rail usages in the codebase use a differently-named `.cdl-alarm-kpi-rail` class in a separate stylesheet, unaffected). Opened roadmap item #22 to track this as an ongoing, dedicated visual-polish thread going forward, separate from the backend-correctness cycles 190-195 ran. Verified: frontend `tsc --noEmit` 0 errors (CSS-only change, not type-checked, but confirms nothing else broke); no stylelint configured in this repo to run; full frontend Jest/Vitest suite still blocked by the same sandbox issue. See Cycle log below).**

**Last updated: 2026-07-27 (Cycle 195 — found and fixed a real, previously-unnoticed second choke point for the exact same class of bug Cycles 192-193 fixed. While re-reading `SmartIntakeService.createVerticalSlice` to double-check a Cycle 194 claim, noticed it unconditionally overrides `timeline` (not `flags`/`vitals`) before calling `createPatient` — a doubly-confirming detail for Cycle 194's "no fix needed" conclusion, since that endpoint's own malformed timeline never even reaches `createPatient` to begin with. Following that thread to `EmergencyPatientService.updatePatient` (`backend/src/modules/emergency-os/emergency-os.services.ts:1089`) found it does a completely raw `{...current, ...patch, id}` merge with zero normalization — none of Cycle 192/193's `normalizePatientFlags()`/`normalizePatientVitals()` guards apply here at all. Traced this to a real, live, currently-reachable production endpoint: `PATCH /api/patients/:patientId` (`PlatformSystemsController.updateEmergencyPatient`), authenticated (`AuthGuard('jwt')`) and permission-gated (`READ_PHI`/`WRITE_PHI`) but with a fully unvalidated `Record<string, any>` body — any caller with write access can PATCH `flags` as `{type,...}` objects or `vitals` as `bpSystolic`/`bpDiastolic`, silently reintroducing both bugs already fixed for the create path. Confirmed the frontend's own client wrapper for this endpoint (`updateEmergencyPatientRecord` in `patientManagementApi.ts`) is currently unused — every real frontend `updatePatient` call goes to the local Zustand store, not this HTTP endpoint — so no live frontend regression exists today, but the backend API surface itself was genuinely unguarded regardless of whether today's frontend happens to exercise it. Checked all 3 other real internal callers of `updatePatient` before fixing (a reception-escalation flags-append that already sends plain strings, an OCR-intake demographics-only patch, and one test patching only `roomId`) to confirm the fix couldn't regress any of them. Fixed by applying the same `normalizePatientFlags()`/`normalizePatientVitals()` normalization inside `updatePatient` itself, conditionally on `patch.flags`/`patch.vitals` being present so an omitted field is never coerced into an empty array. New dedicated spec, `emergency-patient-update-normalization.spec.ts` (6 tests: object-shaped flags patched in get normalized identically to creation-time; a plain-string flags patch — the real `ReceptionWorkspaceService.escalate` shape — passes through unchanged; `bpSystolic`/`bpDiastolic` in a patched vitals entry map to `sbp`/`dbp`; `recordedBy` defaults to the patient's own `assignedStaffId`; a patch that omits `flags`/`vitals` entirely leaves both completely untouched; an explicitly-empty `flags: []` patch genuinely clears flags rather than being treated as "no patch given"). Verified: new suite 1/1 (6/6) plus the pre-existing `updatePatient`-touching specs re-run clean; backend `tsc`/`eslint` both 0 errors (3 pure-formatting `prettier/prettier` findings in the new spec, fixed via `eslint --fix`, re-verified passing after); full backend suite **259/259 suites, 2183/2183 tests** (was 258/258, 2177/2177 — +1/+6, zero regressions; one run hit the same pre-existing CPU-contention-flaky `encryption.service.spec.ts` timing test documented since Cycle 181, re-confirmed via a clean isolated run and a second clean full-suite run, unrelated to this change). See Cycle log below).**

**Last updated: 2026-07-27 (Cycle 194 — closed out the last unexamined thread from Cycle 191's field-mapping evidence map: the `timeline`/`JourneyEvent` shape divergence, where `SmartIntake.tsx`'s vertical-slice flow sends timeline entries shaped `{id, patientId, type, timestamp, summary, metadata, from?, to?}` with no `staffId` at all, against the canonical `{id, from?, to, timestamp, staffId, note?}` — and `EmergencyPatientService.createPatient`'s `timeline: normalized.timeline || [...]` line stores whatever it's given completely verbatim, exactly the same unguarded pattern that caused the real `flags` (Cycle 192) and `vitals` (Cycle 193) bugs. Traced every real consumer before concluding anything, the same rigor those two fixes used: the one true UI-facing reader of backend-sourced journey events, `src/utils/patientTimeline.ts`'s `mapJourneyEvent`, already reads `eventRecord.staffId || eventRecord.actorStaffId || eventRecord.by || ''` (an optional field feeding an optional `actor?` display slot, confirmed via `staffLabel()` returning `undefined` cleanly on an empty id — no crash, no misleading value) and already categorizes by `eventRecord.type`/`from`/`to`, fields the vertical-slice's entries actually do send; backend's own `flow-snapshots.ts` only reads `event.type` (present) for a follow-up-scheduling check; `emergency-os.services.ts`'s summary-line fallback (`patient.timeline?.[...]?.note || patient.chiefComplaint`) already degrades gracefully when `note` is absent (the vertical-slice sends `summary`, not `note`); and the one place that reads `event.staffId` without a fallback, `emergencyStore.ts`'s `workflowLogFromJourneyEvent`, only ever operates on the frontend store's own natively-created `JourneyEvent`s (always populated with `staffId`) — never on the raw backend `getJourney()` payload, which flows through a completely separate, already-defensive code path. Unlike `flags`/`vitals`, this divergence was already fully absorbed by defensive code written for exactly this shape — confirmed with evidence, not assumed, closing the thread with a "no fix needed" finding rather than leaving it open or patching something that isn't broken. **This closes roadmap item #18's evidence-map triage completely** — all 4 field-shape questions Cycle 191 raised now have a resolved, evidence-backed answer (`notes`: no fix needed, Cycle 193; `flags`: fixed, Cycle 192; `vitals`: fixed, Cycle 193; `timeline`: no fix needed, this cycle). The remaining open scope on item #18 — the 7 extra fields with no canonical home, and the actual `whitelist:true`/`forbidNonWhitelisted:true` DTO-building work itself — remains exactly what Cycle 191 already scoped it as: a separate, dedicated future pass, not a drive-by. No code changes this cycle; `git status` confirmed clean before and after. See Cycle log below).**

**Last updated: 2026-07-25 (Cycle 193 — continued acting on Cycle 191's field-mapping evidence, fixing a second and even more severe real bug from the same source: `SmartIntake.tsx`'s vertical-slice flow sends blood pressure as `bpSystolic`/`bpDiastolic` rather than the canonical `EmergencyVitals.sbp`/`.dbp`, and omits the required `recordedBy` field entirely. Confirmed real impact before fixing, not assumed: grepped both trees for `.sbp`/`.dbp` consumers and found BP is one of the most widely-read vitals fields in the app — `qSOFA.tsx` (sepsis screening's hypotension criterion), `alertEngine.ts` (critical SBP<90/>180 alerts), `reassessmentEngine.ts` (SBP-triggered reassessment), `clinical-protocol.service.ts` and `deterioration-prediction-v3.service.ts` on the backend, plus the primary BP display in `PatientDetailPanel.tsx` and half a dozen other summary/handoff surfaces. For a patient created through the same Smart Intake vertical-slice path as Cycle 192's flags bug, blood pressure would silently read as missing everywhere — showing as `--` in the UI and never triggering hypotension-driven alerts, sepsis-screen criteria, or reassessment logic, regardless of how low the real value was. Fixed at the same canonical choke point, `EmergencyPatientService.createPatient`, with `normalizeVitalsEntry()`/`normalizePatientVitals()`: maps `bpSystolic`→`sbp`/`bpDiastolic`→`dbp` when the canonical field is absent (canonical wins if both are present), defaults `recordedBy` to the assigned staff id or `'intake'`, and correctly treats a `null` BP value (an empty form field, not a real reading) as genuinely absent rather than storing `null` or `0` — verified this exact nullish-coalescing edge case with a standalone Node check before writing the test, not after. Also separately checked the third divergence Cycle 191 found (`notes` sent as `{id,type,body,authorId,createdAt,metadata}` vs. canonical `{id,text,authorId,timestamp}`) and found it needs no fix at all — nearly every real frontend consumer already reads `note.text || note.body`, a defensive fallback pattern already in place across a dozen+ files, closing that thread with confidence rather than "fixing" something that isn't actually broken. Wrote a new dedicated spec, `emergency-patient-vitals-normalization.spec.ts` (8 tests: canonical fields pass through, the real `bpSystolic`/`bpDiastolic` shape gets mapped, `recordedBy` defaulting through 2 fallback levels, the null-vs-absent edge case, single-object vs. array input, multi-entry independence, and empty-array preservation). Verified: new suite 1/1 (8/8), backend `tsc`/`eslint` both 0 errors, full backend suite 258/258 (2177/2177, +1 suite +8 tests, zero regressions). Same scope discipline as Cycle 192 — backend-only, frontend side not touched since it remains unverifiable in this sandbox. See Cycle log below).**

**Last updated: 2026-07-25 (Cycle 192 — found and fixed a real bug that Cycle 191's field-mapping investigation surfaced but didn't act on. Tracing `SmartIntake.tsx`'s vertical-slice flow's `flags` field revealed it sends structured objects (`{type, reason, severity, detectedAt}`) rather than the canonical `EmergencyPatient.flags: string[]` — and `EmergencyPatientService.createPatient` stored `input.flags` completely as-given, with zero type coercion (`flags: normalized.flags || (priority-based default)`). Confirmed real, concrete impact rather than treating it as a type-level nitpick: grepped both trees for `.flags.includes(...)`/`.flags.some(...)` usage and found it's load-bearing in over a dozen places — 5+ backend call sites (reassessment-queue filtering, EMS-high-risk detection, admission-pending checks, escalation logic) and 6+ frontend call sites (`NEWS2.tsx`/`qSOFA.tsx`'s clinical-score reassessment triggers, `HandoffBriefGenerator.tsx`, `calculatorSave.ts`), every one written assuming string entries. For a patient created via Smart Intake's vertical-slice "Create-and-triage" action with P1/P2 priority or a reassessment-triggering vital sign — exactly the highest-acuity patients — `patient.flags` would silently contain unmatchable objects instead of `'HighRisk'`/`'ReassessmentDue'` strings, meaning every one of those `.includes()` checks would quietly return false. Fixed at the single canonical choke point, `EmergencyPatientService.createPatient`, with a new `normalizePatientFlags()` helper that accepts strings as-is, extracts `.type` from object-shaped entries (verified to exactly match `PatientFlag` enum values), drops unrecognizable entries instead of storing garbage, and dedupes — so every caller is protected regardless of what shape it sends, not just the one that triggered the discovery. Wrote a new dedicated spec, `emergency-patient-flags-normalization.spec.ts` (7 tests: plain strings pass through, the real object shape gets normalized, a string+object mix is handled, unrecognizable entries are dropped rather than stored, cross-shape deduplication, and 2 tests pinning the pre-existing P1/P2-default and empty-array-stays-empty edge cases so the fix provably doesn't change behavior it shouldn't). Verified: new suite 1/1 (7/7), backend `tsc`/`eslint` both 0 errors, full backend suite 257/257 (2169/2169, +1 suite +7 tests, zero regressions). Deliberately backend-only — the frontend side of this bug (its own local pre-persist state) wasn't touched, since verifying a frontend change is still blocked by the persistent `esbuild` sandbox issue; the backend fix closes the real data-integrity risk at its actual boundary regardless. See Cycle log below).**

**Last updated: 2026-07-25 (Cycle 191 — investigation-only cycle closing the prerequisite gap on roadmap item #18's validation-DTO sub-item, which had sat open since Cycle 180 specifically because it needed "enumerating every field real callers send today" before a safe DTO could be built. First ruled out further `ts-prune` progress: checked the 3 biggest shared `lib/` barrel candidates (`lib/cig`, `lib/native-ai`, `lib/patient-orchestration`, 177 combined findings) against real importers in both trees — all 3 confirmed genuinely, heavily live, closing out the barrel-hunting methodology as fully exhausted everywhere it could apply (backend module barrels: done Cy187-188; frontend module barrels: done Cy189; shared lib barrels: done this cycle, nothing left in this specific vein). Then traced all 5 real frontend callers of the 3 unvalidated intake endpoints (`QuickIntake.tsx`, `SmartIntake.tsx`'s vertical-slice, `receptionIntakeOrchestrator.ts`, `receptionQuickIntakeService.ts`, `emergencyStore.ts`) down to their actual constructed payload shapes, and compared each against the canonical `EmergencyPatient`/`EmergencyVitals`/`JourneyEvent` types a naive whitelist DTO would otherwise be built from. Found real, concrete divergence confirming the original caution was correct, not just defensive hedging: **notes** sent as `{id, type, body, authorId, createdAt, metadata}` by 2 of 5 callers vs. canonical `{id, text, authorId, timestamp}`; **flags** sent as a plain `string[]` by most callers (matches canonical) but as an object array `{type, reason, severity, detectedAt}` by `SmartIntake.tsx`'s vertical-slice path specifically — a real inconsistency *between* callers, not just against the canonical type; **vitals** sent with `bpSystolic`/`bpDiastolic` field names and no `recordedBy` vs. canonical `sbp`/`dbp`/`recordedBy` (required); **timeline/JourneyEvent** has 3 different real shapes in play — backend's canonical `{id, from?, to, timestamp, staffId, note?}`, the frontend's own richer `JourneyEvent` type used by `emergencyStore.ts`'s default fallback (a superset — safe), and `SmartIntake.tsx`'s vertical-slice timeline entries, which are missing the canonical-required `staffId` entirely and `to` on most entry types (would fail a strict required-field check as-is); plus several fields sent by real callers that don't exist in the canonical interface at all (`phone`, `healthCardNumber`, `healthCard`, `source`, `lastAssessedTime`, `quickSafetyFlags`, `waitingRoomStatus`). No code changes made this cycle — pure investigation, confirmed via a clean `git status` — but this closes the prerequisite gap explicitly named in the roadmap: any future DTO-building pass now has a real, evidence-backed map of what has to be accommodated instead of starting from the canonical type and discovering these breakages the hard way. See Cycle log below).**

**Last updated: 2026-07-25 (Cycle 190 — fresh dependency/security re-audit, first since Cycle 182 (8 cycles ago). Both trees now show a much larger "high" count than last measured — frontend 26 (was 0), backend 38 — but investigation traced almost all of it to one root cause, not a new class of real risk: an old `brace-expansion`/`minimatch` DoS advisory (GHSA-mh99-v99m-4gvg, `<=5.0.7` vulnerable) that several dev-only tooling packages pin internally and haven't released a fix for. Traced three separate un-fixable chains with hard evidence, not assumption: (1) `eslint-plugin-jsx-a11y@6.10.2` — the actual latest published version — still declares `minimatch: ^3.1.2`, whose own newest release (`3.1.5`) requires `brace-expansion: ^1.1.7`, whose newest 1.x release (`1.1.16`) is *still* inside the vulnerable range — there is no compatible patched release anywhere in that chain, confirmed via `npm view`, not guessed; (2) `jest`'s internal `@jest/reporters`/`babel-plugin-istanbul` chain pins an old `glob@7.2.3`, and `npm audit`'s own suggested "fix" (`jest@25.0.0`) is actually a downgrade from the installed `29.7.0`/`30.4.2` — verified by checking real installed versions, not trusted at face value (this program's Cycle 182 lesson applied again); (3) backend-only: `firebase-admin`'s `google-gax` and `mongoose`'s `gcp-metadata`→`gaxios` chains each pin their own internal `rimraf@5.0.10`→`glob@10.5.0`, invisible to and unfixable by any change to our own direct dependencies. One real, verified, if audit-count-invisible improvement made: bumped backend's own direct `rimraf` 5.0.5→6.1.3 (a genuine forward upgrade, not a downgrade — confirmed via `npm view` version history), cleaning our own `prebuild` script's dependency chain to a modern `glob@13.0.6`; **this does not reduce `npm audit`'s reported count**, since the identical advisory persists via the two third-party-pinned chains above that a change to our own `package.json` cannot reach — stated plainly rather than left to look like more progress than it is. Verified safe: backend `tsc`/`eslint` 0 errors, a working-CLI smoke test, and the full suite (2 of 3 runs 256/256 clean, 1 run's single failure isolated to the same pre-existing CPU-contention-sensitive timing test documented since Cycle 181 — re-confirmed via a 9ms isolated run, nowhere near its 100ms threshold, and fully unrelated to rimraf). Re-confirmed items #5 (`sharp`/`@xenova/transformers` — same conclusion, fresh 2026 CVE IDs) and #19 (`react-router` — unchanged, still needs the 7.x migration) with current evidence rather than carried-forward assumption. Opened new roadmap item #21 documenting the full un-fixable `brace-expansion` chain finding. See Cycle log below).**

**Last updated: 2026-07-25 (Cycle 189 — extended roadmap item #4's barrel-file methodology to the backend's remaining top clusters and to frontend for the first time, and turned up a real, unrelated finding along the way. Backend: checked `services/index.ts` (55 findings, the single largest remaining cluster) and confirmed it's genuinely live — imported by 10 legacy `api/*.routes.ts` files — so its findings are individual dead-exports-inside-a-live-file, a materially harder/lower-confidence category than whole-file deletion; the backend barrel-hunting vein from Cy187-188 is now exhausted. Frontend: grouped its 2075 strong-signal findings by file for the first time and checked the top barrel-shaped candidates against both relative AND `@/`-aliased import forms (this repo has real path aliases, `@/*` → `./src/*`) — `clinical-calculators`, `components/ai`, `alarm`, `components/primitives`, `components/layout`, and `services/sentinel` are all genuinely live; `contracts/index.ts` (72 findings) has zero real importers today but its own header comment declares it a deliberate forward-looking migration bridge ("New code should import from `@/contracts`... over time, migrate imports") — correctly left untouched, not dead code. `services/native/index.ts` (16 findings, 446 lines across 8 files) also has zero importers, but reading the code found a complete, real WebAuthn biometric helper plus voice/push/permissions/sync helpers — not dead code either. Traced it further: git history shows its sole consumer, `BiometricSetup.jsx`, was deliberately deleted in commit `ce80849c` ("open-access demo... auth UI removal"), leaving this entire client-side layer *and* the backend's fully-implemented, fully-tested `BiometricService`/`BiometricController` (Cycle 178, 25 tests) with **zero UI entry point** — a real product-facing gap, not a hygiene item, opened as new roadmap item #20 for a future dedicated decision (restore the UI vs. formally retire the feature) rather than unilaterally deleting 446 lines of real auth/crypto code or rebuilding a page without product sign-off. One confirmed-safe leftover from that same deletion commit — `src/pages/BiometricSetup.css`, zero references anywhere — was deleted this cycle. Verified: frontend `tsc --noEmit` and `eslint` both 0 errors (full Jest/Vitest run not possible in this sandbox — the same persistent `esbuild` block — explicitly disclosed; mitigated here since a CSS deletion has no import-graph risk beyond what `tsc`/repo-wide grep already confirmed). No backend files changed this cycle. See Cycle log below).**

**Last updated: 2026-07-25 (Cycle 188 — continued roadmap item #4 (`ts-prune` triage) by finishing the module-barrel verification Cycle 187 had left as "spot-checked, not individually verified": of the ~19 module-level `index.ts` barrels identified as the dominant false-positive shape, 7 (`tool-calling`, `ai/foundation`, `cost-optimizer`, `evaluation`, `training`, `memory`, `sentinel`) hadn't yet had their real-importer status confirmed. Checked each with a rigorous repo-wide grep for any import resolving to the bare module path (not the narrower pattern Cycle 187 used, which had a blind spot — it initially missed `ai/foundation`'s real importer, `ai.module.ts`, caught and corrected before concluding anything). Result: `ai/foundation` is genuinely used (kept); the other **6 are fully orphaned** — `app.module.ts` wires every one of them in by its specific `.module.ts` file (`./modules/sentinel/sentinel.module`, etc.), never through the bare barrel path, and nothing else in the repo (including the frontend tree, checked explicitly) imports the barrel either. Read all 6 files first to confirm they're pure `export *` re-export barrels with no side effects, matching Cycle 187's `src/index.ts` shape exactly, then deleted all 6. Verified: backend `tsc`/`eslint` both 0 errors, full backend suite unchanged at 256/256 suites (2162/2162 tests, zero regressions — none of the deleted files had test coverage of their own). Re-ran `ts-prune` and diffed the full before/after output (not just the raw count) to fully account for the change: **71 findings removed** (exactly matching the 6 files' combined finding count from Cycle 187's file-grouping), but **5 new weak-signal "(used in module)" findings surfaced** — type exports that were previously satisfied by the barrel's own `export *` re-export and are now correctly re-classified as used-only-locally now that the barrel is gone; still not real dead code, fully explained rather than left as an unexplained discrepancy. Net: 1314→1248 backend findings (-66). Item #4 remains open — this closes out the barrel-file sub-investigation specifically; ~1,212 backend findings outside that pattern and all 4356 frontend findings are still untriaged. See Cycle log below).**

**Last updated: 2026-07-25 (Cycle 187 — real evidence-backed progress on roadmap item #4 (`ts-prune` unused-export triage), which had sat as "investigation-only" since it was opened. Ran fresh `ts-prune` against both trees rather than trusting the stale estimate: backend is **728 backend-owned findings** (not the stale "~702" — split cleanly into 622 more findings that are actually in shared repo-root files (`types/`, `engine/`, `ml-services/shared/`) outside backend's own ownership, wrongly folded into the original backend count) further split into **328 "used in module" (weak signal — the export is genuinely referenced, just not importable through the flagged binding; not real dead code) vs. 400 true zero-reference (strong signal)**; frontend is **4356 findings**, a real ~2.2x growth from the stale "~1,953" estimate, reflecting genuine codebase growth over the many cycles since that number was last measured (not triaged this cycle — see below). Grouped the 400 backend strong-signal findings by file and found the dominant pattern: **274 of 400 (68%) trace back to just ~19 module-level `index.ts` barrel re-export files** (`modules/moe-router`, `modules/tool-calling`, `modules/sentinel`, etc.) whose contents are genuinely used elsewhere via direct imports that bypass the barrel — a systemic false-positive shape for this codebase's barrel-export convention, not real dead code, and confirmed (not assumed) by checking real importers module-by-module. One of these barrels, `backend/src/index.ts` (a 1-line `export * from './services/service-registry'`), was confirmed to have **zero real importers anywhere** — its own re-exported service-registry contents are all reached directly by `main.ts`/`health.routes.ts`/`services/index.ts` instead — so it was deleted outright, cleanly resolving 36 of the 400 strong-signal findings (1350→1314 total backend findings) with zero behavior change. Verified: backend `tsc`/`eslint` both 0 errors, full backend suite 256/256 (2162/2162, unchanged — the deleted file had no test coverage of its own since nothing used it), fresh `ts-prune` re-run confirms the exact expected 36-finding drop. **This is real progress, not full closure**: the other ~18 module barrels showing the same shape were spot-checked at the "does anything import the bare module path" level, not individually verified end-to-end, and the remaining ~1,278 backend findings plus all 4356 frontend findings are still open — explicitly scoped as future work, not claimed done. See Cycle log below).**

**Last updated: 2026-07-25 (Cycle 186 — closed `SentinelController` on roadmap item #17, the final remaining candidate after a 9-cycle sweep (Cy167–185). This controller (424 lines, 17 endpoints, 4 injected services) had been repeatedly deferred for size across every prior pass at this roadmap item — finally tackled once it stopped competing with a smaller, better-scoped alternative. 53 new tests covering all 17 endpoints: the 2 real aggregation endpoints (`health` — message branches on tracking-enabled state; `commandSnapshot` — per-unit ETA attachment, null-safe coordinate formatting, geofence field-reduction, and pending-only AI-recommendation filtering), 6 one-line delegation endpoints each confirmed to call the right service method and envelope the result, `unitPositions`'s limit-clamping arithmetic across 7 boundary cases (including the non-obvious `limit='0'` → 50 rather than 1, since `Number('0')` is falsy and falls through the `||` to the default), `ingestCad`'s clinical-field/patient-block detection and payload-merge branching, `upsertInbound`'s numeric/string field coercion and null-vs-undefined defaulting, `prepRecommendation`'s `!== false` default-true semantics, `alarmAction`'s full 13-entry short-verb-to-canonical-status mapping table plus its unknown-action rejection path and actor-id fallback chain, `reviewAi`'s reviewer-id fallback chain, and `analytics`'s real dispatch-to-arrival/ETA-error averaging math (both independently verified against a standalone Node computation before the assertions were written). Verified: new suite 1/1 (53/53), backend `tsc` and `eslint` both 0 errors, full backend suite 256/256 (2162/2162, +1 suite +53 tests, zero regressions). **Roadmap item #17 is now fully closed — all 9 originally zero-coverage services/controllers have dedicated unit test suites, 0 remain.** See Cycle log below).**

**Rebuilt: 2026-07-23 (Cycle 158).** The previous version of this file had become structurally broken: its "Last updated" line had grown, by prepending each cycle's narrative for 157 cycles, to **162,783 characters on a single line** — unreadable by normal editors and tooling. Several other sections had the same runaway-accumulation shape. Nothing was deleted: the full prior file is preserved verbatim at [docs/archive/SCORECARD-archive-2026-07-23-cycles-1-157.md](docs/archive/SCORECARD-archive-2026-07-23-cycles-1-157.md), and the complete cycle-by-cycle narrative (157+ cycles) remains in project memory (`project-quality-baseline-cycle0.md`). This rebuild also found that parts of the old file were internally stale before it was even retired, and that the frontend test suite's last-recorded pass count was stale too — see the Cycle 158 entries in "Testing, accessibility & delivery" and "Cycle log" below.

**Architect Mode (2026-07-15) — structural coherence rescore:** **97/100** measured. Unified proof pack: [docs/architecture/architect-mode/PROOF-PACK.md](docs/architecture/architect-mode/PROOF-PACK.md). Gates: Architect FE **100**, BE **51**, contract-matrix **19**, cohesion-security **30**, redirect-parity **58**, dual builds **PASS**. Residual: dual Nest/Express, Postgres tenant e2e, full Playwright. *(Carried forward, not re-verified this cycle.)*

**Companion artifact (interactive):** <https://claude.ai/code/artifact/01325587-a9ae-4c6a-920a-c48378ba3352> (kept current per cycle, now reflecting Cycle 173 as of its last publish; this line had drifted to point at a stale Cycle 111 snapshot back at Cycle 165 — corrected then, and periodically re-checked since)

> A self-reported internal "maturity model" script (`scripts/platform-scorecard.mjs`) also exists in this repo and outputs ~86/100 — that reflects the app's own sample/demo self-assessment data, not measured reality, and is **not** used here. Every score below is tied to a specific command, file, or test run.

---

## Cycle 158 — fresh, re-run-today evidence

Everything in this section was actually executed on 2026-07-23 against the current tree (`HEAD` `70cef8c5`), not carried forward.

| Check | Command | Result |
|---|---|---|
| Frontend type safety | `npx tsc --noEmit -p tsconfig.json` | **0 errors** |
| Backend type safety | `npx tsc --noEmit -p tsconfig.json` (in `backend/`) | **0 errors** |
| Frontend lint | `npm run lint` (`eslint src`) | **0 errors, exit 0** |
| Inline-style gate | `npm run check:inline-styles` | **110/690** (7 file ceilings enforced) — improved from the last recorded 137/690; not independently investigated this cycle, likely partly attributable to concurrent unrelated work in this repo |
| Frontend dependency audit | `npm audit --omit=dev` | **2 vulnerabilities** (1 high: `axios` 1.0.0–1.17.0, prototype-pollution/DoS family; 1 moderate: `protobufjs` 7.5.0–7.6.4, DoS via infinite loop) — regressed from the last-recorded "0 vulnerabilities"; fixes available via `npm audit fix` |
| Backend dependency audit | `npm audit --omit=dev` (in `backend/`) | **8 vulnerabilities** (1 moderate, 6 high, **1 critical**) — regressed from the last-recorded "4 known findings, unchanged." Critical + most highs trace to `protobufjs` (arbitrary code execution, multiple CVEs) pulled in transitively via `@xenova/transformers` → `onnxruntime-web` → `onnx-proto`; also `sharp` (high, libvips CVEs), `dd-trace` (high, W3C baggage header DoS), `brace-expansion` (high, DoS). `npm audit fix` clears the non-breaking ones; the `@xenova/transformers` chain needs `--force` (breaking downgrade) — this is the same abandoned-package migration already tracked as roadmap item #12 below, now materially more urgent given the critical severity |
| Frontend test suite | `npm run test:run` (vitest) | see below |
| Backend test suite | `npm test` (jest, in `backend/`) | see below |

**Not fixed this cycle:** the two dependency-audit regressions above are reported, not remediated — `npm audit fix --force` on the backend is a breaking dependency change (downgrades `@xenova/transformers`) and deserves its own verified pass, not a drive-by inside a file-restructuring cycle. Flagged as the new top of the roadmap below.

*(Test suite result rows below were filled in after both suites finished — see "Testing, accessibility & delivery" gauge.)*

## Cycle 160 — dependency-audit regression, closed (mostly)

| Check | Result |
|---|---|
| Frontend `npm audit --omit=dev` | **0 vulnerabilities** (was 2) — `npm audit fix` cleanly bumped `axios`, `protobufjs`, `js-yaml`, `shell-quote`, `brace-expansion` within their existing `package.json` semver ranges |
| Backend `npm audit --omit=dev` | **2 high** (was 8: 1 moderate, 6 high, 1 critical) — `npm audit fix` (non-force) cleared `typeorm`, `brace-expansion`, `dd-trace` and dependents; a targeted `overrides` entry (`protobufjs: ^7.6.5`, package.json) deduped the last critical finding (arbitrary code execution via `onnx-proto`'s vendored `protobufjs@6.11.6`) onto the same 7.6.5 already used elsewhere in this tree via `firebase-admin`/`google-gax` — zero new packages introduced |
| Real embedding smoke test | **Verified** — `pipeline('feature-extraction', 'Xenova/all-mpnet-base-v2')` against real cached model weights, real text in, real 768-dim float vector out, after the `protobufjs` override. This is the actual runtime path the critical finding sat on (`@xenova/transformers` → `onnxruntime-web` → `onnx-proto` → `protobufjs`), not just a clean `npm ls` |
| Backend test suite | 245/245 suites, 1922/1922 tests — unchanged, re-run twice across the dependency changes |
| Frontend test suite | **Not re-verified this cycle** — see below |

**Still open: `sharp <0.35.0` (high, libvips CVEs) via `@xenova/transformers`'s vendored copy.** Attempted the same override approach; the fix requires npm to write a fresh native `.node` binary, which a local Windows Application Control policy in this dev sandbox blocked outright (`ERR_DLOPEN_FAILED`) — confirmed via direct `dlopen()` test, not just the wrapped error. Reverted to the original, already-trusted `sharp@0.32.6` rather than ship an unverified native-dependency bump; `sharp` is 0.x (pre-1.0 semver, minor bumps can be breaking) and is eagerly imported by `@xenova/transformers`'s pipeline machinery even for text-only feature-extraction — a broken sharp would take the whole embedding path down, so this isn't a low-stakes swap to make blind. Left open for a cycle with CI/Linux verification access (production runs `FROM node:20-alpine`, a different binary entirely, so this sandbox's block has no bearing on production safety — it's a *local verification* blocker, not a production risk indicator either way).

**Frontend runtime verification also blocked this cycle, unrelated to the dependency changes' correctness:** after `npm audit fix` touched `node_modules`, the same Application Control policy re-flagged the `esbuild` native binary Vite/Vitest depend on to even load their config file — `tsc` and `eslint` don't spawn it and both stayed clean, but `npm run test:run` cannot run in this sandbox for the remainder of this session. Reported honestly rather than assumed passing.

## Cycle 161 — real `strictNullChecks` blast-radius baseline established (166 errors), 44 closed

The `esbuild` Application Control block from Cycle 160 is still in effect in this sandbox (confirmed by re-testing `npm run test:run` before starting — same `spawn UNKNOWN`), so this cycle was scoped to backend-only work fully verifiable via `tsc`/`jest` (frontend `tsc --noEmit`/`eslint` re-confirmed clean, 0 errors, as a sanity check that they're unaffected).

| Check | Result |
|---|---|
| `strictNullChecks` blast radius (`tsc --noEmit -p tsconfig.eslint.json --strictNullChecks true`) | **166 errors → 122 errors** (44 closed) across the ~800-file backend tree — the roadmap's "blast radius unknown" is now a real, categorized number |
| Backend `tsc --noEmit -p tsconfig.eslint.json` (real project config, full scope incl. tests) | **0 errors**, unchanged |
| Frontend `tsc --noEmit` | **0 errors**, unchanged |
| Backend full jest suite | **245/245 suites, 1922/1922 tests** — including `test/operational-intelligence-build-snapshot.spec.ts`, which directly exercises the most heavily-touched file this cycle |
| Backend `npm run lint` | **21 pre-existing errors** (all `prettier/prettier` formatting, in 7 files this cycle didn't touch: CIG projection/entity mapper files, `intent-classifier.service.ts`, `document-chunker.spec.ts`) — see callout below |

**What the 44 closed errors actually were:** re-ran `tsc` with `--strictNullChecks true` against the full-scope config to get a real count instead of the roadmap's "unknown," then categorized all 166 by TS error code: 41 were `TS2345`/`TS2339` errors bearing the exact text `type 'never'`, all traced to the same single root cause — `const arr = [];` accumulator arrays with no type annotation, which TypeScript infers as `any[]` when `strictNullChecks` is off (today's setting) but as `never[]` when it's on, breaking every later `.push()`/property-access. This pattern repeated across 10 files (`lib/operational-intelligence/buildSnapshot.ts` alone had 15 of the 41, all from two untyped arrays feeding the exported `anomalies`/`recommendations` fields — fixed by annotating them against the already-existing `OperationalIntelligenceSnapshotOutput` contract type instead of duplicating it). Fixed all 10 files with explicit array element types, mostly reusing an existing declared return type, entity type, or sibling `Awaited<ReturnType<...>>` idiom already present in the same file rather than inventing new shapes.

**Bonus find while fixing `tenant-provisioning.service.ts`:** fixing its one `never[]` error surfaced 3 more, different errors nearby — `resolveWorkspaceDefaults()`'s `.filter()` used a type predicate reading `workspace is ReturnType<...normalizeWorkspaceDefault>`, but `normalizeWorkspaceDefault` can return `null`, so the predicate never actually narrowed `null` out of the *type* (even though the runtime `.filter(Boolean)` genuinely does drop falsy values — this was a type-annotation-only bug, not a runtime one). The correct idiom, `NonNullable<ReturnType<...>>`, was already used correctly two methods away in the same file (line 259) — this one call site just didn't match it. Fixed to match.

**Real, verified regression check:** frontend and backend `tsc` both re-confirmed 0 errors under their actual project configs (not just the experimental `--strictNullChecks true` flag), and the full backend jest suite re-ran clean at 245/245 (1922/1922) after all 10 files changed — including the one spec file that directly exercises the most-touched file (`buildSnapshot.ts`).

**What this cycle deliberately did NOT do:** flip `strictNullChecks: true` in `backend/tsconfig.json`. 122 errors remain, and unlike the 44 closed here, most of the rest are genuine "possibly null/undefined" findings (`TS18048`/`TS18047`/`TS2531`/`TS2532`, ~40 of the 122) inside sensitive, high-traffic services — `auth.service.ts` (11), `subscriptions.service.ts` (10), `ai.service.ts` (9), `chat.service.ts` (6 more, a different cluster than the ones fixed here) — where the correct fix requires understanding each call site's real nullability invariant, not a mechanical annotation. Rushing that without the ability to cross-check against a live frontend integration run (blocked this session) would risk exactly the kind of drive-by mistake this project's cycle discipline exists to avoid. Left as a well-scoped, now-quantified P2 roadmap item instead of a global flag flip.

**Also found, not fixed:** backend `npm run lint` currently has 21 real, pre-existing `prettier/prettier` errors in 7 files, none touched by this cycle and none touched recently by this session's own history (last commit on any of them: `14233485`, 2026-07-21, CIG/AI work). This directly contradicts this file's own "What's genuinely solid" claim that backend `eslint` was clean — that claim's "Cycle 158" citation, on closer reading, only ever re-confirmed *frontend* lint fresh; backend lint had not actually been re-run this session until now. Not fixed here (out of this cycle's chosen scope, and touching files with recent unrelated activity deserves its own pass rather than a drive-by inside a type-safety cycle) — corrected in "What's genuinely solid" below and added as a fresh roadmap item.

---

## Exit-criteria gauges

*Carried forward from the pre-rebuild archive (last touched by Cycle 157 or earlier); not independently re-verified this cycle except where marked "Cycle 158."*

| Category | Score | Target | Note |
|---|---|---|---|
| Architecture | 86 / 95 | 95 | carried forward |
| Frontend UX/UI | 95 / 95 | 95 | carried forward |
| Backend reliability | 88 / 95 | 95 | carried forward |
| Performance | 81 / 90 | 90 | carried forward |
| Accessibility | 86 / 90 | 90 | carried forward |
| Testing & quality | 90 / 90 | 90 | **Cycle 159: both suites now clean — 927/927 frontend files (12045/12045 tests), 245/245 backend suites (1922/1922 tests). All 18 failures found in Cycle 158 root-caused and fixed. Number carried forward but no longer contradicted by known failures.** |
| Security & privacy | 94 / 95 | 95 | **Cycle 160: frontend audit fully clean, backend down to 2 high (from 8 incl. 1 critical) — the critical finding that prompted this number's flag in Cycle 158 is resolved and verified** |
| Clinical workflow | 85 / 95 | 95 | carried forward |
| **Overall readiness** | **95 / 95** | **95** | carried forward |

Every category but Frontend UX/UI still sits below target — an honest reflection of a platform mid-build, not a grading error.

---

## Category detail

*All tables below are carried forward verbatim from the pre-rebuild archive. Per-row deltas ("was N") reflect the cycle that last touched that row, not necessarily Cycle 157 — several rows (flagged) were found stale relative to the rest of the document during this rebuild's extraction pass. None of these were independently re-verified during Cycle 158 except Testing (above).*

### Architecture & backend engineering
*Principal Software Architect · Principal Backend Engineer*

| Metric | Score | Note |
|---|---|---|
| Module boundaries | WARN 56 (was 53) | God-controller breakup round 2: `platform-systems.controller.ts`'s remaining ~33 routes split; 23 moved into 2 new controllers with 25 new tests. |
| API / service consistency | WARN 60 (was 58) | Canonical `/api/ai/unified` envelope endpoint + reception task→intent routing shipped; still WARN on stream/retry/tool-execute and ~180 chat-passthrough tools. |
| Backend reliability & ops | GOOD 85 (was 83) | Found+fixed a real live defect: fresh SQLite could not cold-boot at all (`synchronize: true` hardcoded for SQLite, unconditional, causing an AUTOINCREMENT schema error). |
| Type safety / maintainability | GOOD 86 (was 84) | "0 tsc errors" claims for Cycles 12–39 had actually excluded test files from scope; widened scope, fixed the 1,201 frontend + 140 backend errors that exclusion had hidden. **Cycle 158 re-confirms 0 errors at full scope, both trees. Cycle 161: established a real `strictNullChecks` blast-radius baseline (166 errors, was "unknown") and closed 44 of them (10 files), including one genuine type-predicate null-narrowing bug — 122 remain, mostly requiring per-site null-safety judgment in sensitive services, correctly left unrushed.** |

### AI & clinical depth
*AI Systems Architect · Emergency Department Director · CMIO*

| Metric | Score | Note |
|---|---|---|
| AI Chief orchestration depth | WARN 72 (was 65) | Row had gone stale since Cycle 74; re-verified fresh — all 3 previously-cited blockers confirmed actually closed. |
| OCR & smart intake | WARN 58 (was 30) | Real self-hosted `tesseract.js` OCR integrated as the default provider (reversed an earlier "no real OCR" finding). |
| EMS & triage workflows | WARN 63 (was 50) | Row unverified for ~40 cycles at one point (longest-frozen row found); real EMS workspace sub-components and a live `postEmsHandoff` backend route confirmed shipped since. |
| Clinical tool breadth vs. depth | WARN 58 (was 52) | Wells DVT + ABG interpreter closed the entire originally-scoped executor roadmap item; hand-derived against clinical references, 34 new tests all passed first run. |

### Frontend & design system
*Principal Frontend Engineer · Design System Lead · UX Architect*

| Metric | Score | Note |
|---|---|---|
| Design language consistency | WARN 117 (was 116) | Inline-style gate at 137/690 as of last recording — **Cycle 158 re-check: now 110/690**, see fresh evidence above. |
| Component reuse | WARN 75 (was 73) | `ts-prune` unused-export lever opened; 5 confirmed zero-importer components deleted (net 11 real deletions after a near-miss where a live CSS `@import` aggregator was almost wrongly deleted). |
| Navigation & routing | GOOD 66 (was 65) | Fixed a real gap: `/dashboard` had no explicit redirect in `router.tsx` unlike its siblings. Since superseded by Cycles 153–154's routes/nav-wiring and full-app crawl work (see project memory). |
| State management | WARN 54 (was 51) | Found+fixed a live full-page crash on `/emergency/command-center`, root-caused to a dev-only offline shim's placeholder data shape reaching a real consumer expecting Sentinel-specific fields. |
| Bundle size & code-splitting | WARN 75 (was 73) | Whole-app JS payload cut 27.5% (8.58MB→6.21MB) by fixing a repo-wide production-build defect (`.env`'s `NODE_ENV=development` was shipping React's dev JSX runtime app-wide). |
| Runtime performance (live-browser) | WARN 77 (was 72) | Same root cause as above — discovered while regression-checking CLS. |
| Accessibility automation | WARN 84 (was 78) | Repo-wide ARIA bare-boolean/template-string sweep across ~110 files, confirmed 0 remaining via full scan. |

### Security, privacy & compliance
*Security Engineer · Data Architect · Healthcare Operations Consultant*

| Metric | Score | Note |
|---|---|---|
| Perimeter security | GOOD 60 | Unchanged. |
| Authorization depth (RBAC) | WARN 73 | Entire remaining RBAC controller triage list closed — all ~17 remaining controllers read in full and found clean under 4 legitimate authorization patterns. |
| PHI encryption reach | WARN 57 (was 56) | Verified `User.emailEncrypted` genuinely encrypted (AES-256-GCM); `UserProfile`'s DOB/history/allergy/medication encrypted columns were found never actually written to (non-functional no-op lifecycle hooks) — removed/documented. |
| Dependency security | GOOD 75 (was 74) | Regressed Cycle 158 (frontend 2, backend 8 incl. 1 critical). **Cycle 160: frontend back to 0, backend down to 2 high (critical cleared, verified via a real embedding smoke test) — remaining `sharp` finding is a native-binary version bump this sandbox can't safely verify locally, left open rather than shipped blind.** |
| Multi-tenant data isolation | WARN 62 | Closed a RAG tenant-scoping gap (vector DB had no `organizationId` concept) — not yet exploitable in practice, but was a real cross-tenant PHI-leak risk once org-specific ingestion ships. |

### Testing, accessibility & delivery
*QA Lead · Accessibility Specialist · DevOps Engineer*

| Metric | Score | Note |
|---|---|---|
| Test suite: behavior vs. wiring | GOOD 91 (was 80) | Essentially the entire prior test backlog closed: 37 files/165 tests reconciled to real tool-contract behavior. **Cycle 158 re-ran both full suites fresh — see below.** |
| CI/CD coherence | WARN 66 (was 54) | Fixed the `index.html`→nonexistent `main.jsx` entry bug; `validate.yml` confirmed a real, comprehensive CI gate. |
| Deployment & observability | WARN 64 (was 48) | First real audit of this row found substantial pre-existing, uncredited observability infra (multi-component health checks, 27 services, WebSocket/MQTT, 2 external APIs). |
| Accessibility (live-browser WCAG) | WARN 68 (was 66) | First-ever successful axe scan of `/emergency/command-center` (previously always crashed) found one real serious violation: a horizontally-scrolling status-chip rail with no keyboard access. |

**Cycle 158 fresh test-suite run:**

| Suite | Result | Notes |
|---|---|---|
| Backend (`npm test`, jest, in `backend/`) | **244/245 suites, 1917/1922 tests passing** | 1 file failing: `test/tool-calling.spec.ts` — `this.aiGateway.attachUnifiedNode is not a function`. `attachUnifiedNode` genuinely exists on the real `AiGatewayService` class, so this reads as a stale test double/DI wiring in that spec file lagging behind a real method addition — not investigated further this cycle (same "test wiring lags implementation" pattern as the "Test suite: behavior vs. wiring" row above). Test count grew from a last-recorded 1688 to 1922, consistent with ongoing work elsewhere in the repo. |
| Frontend (`npm run test:run`, vitest) | **910/927 files, 12020/12045 tests passing** | 25 real named-assertion failures across 17 files, plus 43 separate `EnvironmentTeardownError` occurrences (a previously-documented Vitest teardown-flakiness class, not new — see "What's genuinely solid"). The 25 failures are **not** teardown noise: named assertions in files like `backendOrphanAudit.test.ts`, `wiringAuditConsistency.test.ts`, `platformCapabilityMatrix.test.ts`, `segmentInventory.test.tsx`, `pr4aComprehensive.test.ts`, `pr5Consistency.test.ts` — self-referential contract/consistency checks over the calculator registry, route inventory, and orphan-detection wiring. None of the touched files trace to the most recent commit (`70cef8c5`, checked via `git show --stat` — it only touches Collaboration Hub/smart-intake/reception-theme files, none of which overlap this failure list). Root cause not investigated this cycle — flagged as the top open item below. |

**This is this cycle's most consequential finding.** The pre-rebuild archive's last recorded frontend number was "832/836 files, 11547/11556 tests passing, remaining 4 confirmed pure teardown noise" — a number that was carried forward, unverified, for an unknown number of cycles. The real, current state is 17 failing files with genuine named-assertion failures, not 4 teardown artifacts. Not fixed here (17 files of contract-test drift is a dedicated cycle's worth of root-causing, not a drive-by inside a file-restructuring pass) — logged as the new top roadmap item.

---

## What's genuinely solid (cumulative)

*Carried forward from the pre-rebuild archive — a running record of verified wins across 157 cycles, condensed to one line each. Full detail for any item lives in project memory.*

- Frontend + backend `tsc --noEmit`: 0 errors at full scope including test files (was hiding 1,341 errors before this was caught) — **re-confirmed fresh, Cycle 158.**
- Frontend + backend `eslint`: 0 errors across both trees — frontend **re-confirmed fresh, Cycle 158**; backend was found to actually have 21 pre-existing errors when finally re-checked in **Cycle 161** (the prior "both trees" wording had only ever verified frontend), then genuinely closed in **Cycle 162**.
- Repo's own inline-style CI gate passing — **now 110/690, Cycle 158.**
- Zero remaining bare-boolean ARIA attribute violations frontend-wide.
- 208/208 backend test suites, 1688/1688 tests passing at last full count (up from 192/1487) — **re-run, Cycle 158, see fresh results above.**
- Frontend/backend clinical-tool contract fully reconciled — all 37 backend executors correctly mirrored on frontend.
- 37 clinical tools have real, verified backend execution logic (up from 3 at Cycle 0), spot-checked against 17+ scenarios.
- Removed a real duplicate (`corrected-sodium`/`osmolal-gap` dead code).
- Real AES-256-GCM PHI/PII encryption at rest for both entities that collect it.
- Completed RBAC audit with zero open authorization questions.
- Main entry bundle chunk cut 44% (1,306.56 KB → 727.81 KB) across two passes.
- Fixed a real previously-invisible entry-point bug (`main.jsx` → `main.tsx`).
- Real cross-role accent-color token system shipped and correctly scoped (0 raw hardcode bypasses across 348 refs/114 files).
- 2 real pre-existing architecture bugs found by actually rendering the app in a browser (not static analysis alone).
- Sentinel module (7 permissions, 17 routes) now fully mirrored across all 3 frontend contract catalogs.
- 2 reception-facing CTA buttons fixed from ~1.02:1 to 7.56:1 WCAG contrast (a 3rd real instance of the `[class*='cta']` sweep-rule footgun), verified via a new raw-HTML-plus-real-CSS harness technique for when source has outpaced the only available compiled build artifact — Cycle 213.
- The app's entire sky-blue accent-color identity restored after confirming a real regression (not a redesign) had flattened it to monochrome.
- All 39 catalogued "portable/derivable" clinical tools now have real backend executors.
- A real live dark-mode feature shipped and reconciled against stale tests.
- Entire missing-schema P0 closed: all 65 entity-declared tables now have real migrations (was 19/65), verified via full forward→revert→forward round trip.
- Real static a11y linting added (`eslint-plugin-jsx-a11y`); all 87 violations found were fixed with targeted repairs.
- **(Cycles 153–157, post-archive)** Fixed a genuinely orphaned page (`PatientRoomDisplay`), its wrong Help Center link, and the broken orphan-detection tool (`.jsx`-only extension filter) that should have caught it.
- **(Cycle 154)** Shipped a real full-app crash/layout crawl tool (182 routes, 0 crashed/blank/overflow) after diagnosing and eliminating a false-positive-generating browser-resource-exhaustion bug in the crawler itself.
- **(Cycle 155)** Fixed a real, user-reported visual layout bug in Reception's Waiting list via root-cause CSS grid analysis.
- **(Cycle 156)** Confirmed backend/frontend wiring is sound (0 unguarded API calls, 0 contract gaps) via the app's own audit infrastructure; fixed a report-test timeout bug that had silently prevented that confirmation from ever being regenerated into docs.
- **(Cycle 157)** Found and fixed a tool-catalog status-labeling bug that mislabeled 7 deliberately chat-only clinical tools as broken ("component gap") after they gained real backend executors.
- **(Cycle 159)** First fully clean full-suite run recorded in this file's history: frontend 927/927 files (12045/12045 tests), backend 245/245 suites (1922/1922 tests), both `tsc` trees and `eslint` at 0 errors — closed an 18-test regression across 2 suites plus 6 previously-uncaught `tsc` errors, all individually root-caused rather than mass-suppressed.
- **(Cycles 177–186)** Roadmap item #17's 9-cycle zero-test-coverage sweep is fully closed: `PineconeService`, `BiometricService`/`BiometricController`, `CacheService`, `SentinelController`, `DeviceTokenService`, `AssetAccessService`, `PersonalizationService`, and `ERPulseHandoverService` all now have dedicated, substantive unit test suites (well over 200 new tests combined) — including a real property-name bug (`AssetAccessService`) that had been hiding in production code purely for lack of coverage until Cycle 183 found it.
- **(Cycle 192)** Found and fixed a real, silent data-corruption bug reachable through Smart Intake's vertical-slice "Create-and-triage" flow: `patient.flags` could end up holding objects instead of the canonical `string[]`, quietly breaking `.includes(PatientFlag.X)` checks load-bearing in over a dozen places across both trees — reassessment-queue filtering, EMS-high-risk detection, sepsis/NEWS2/qSOFA reassessment triggers — for exactly the highest-acuity patients (P1/P2 priority, reassessment-triggering vitals). Fixed at the single canonical patient-creation choke point rather than patching every consumer.
- **(Cycle 193)** Found and fixed a second, even more widely-reaching bug from the same source: the same Smart Intake vertical-slice flow sends blood pressure as `bpSystolic`/`bpDiastolic` instead of canonical `sbp`/`dbp`, silently blanking BP everywhere it's read — sepsis screening (`qSOFA.tsx`), critical hypotension alerts (`alertEngine.ts`), SBP-triggered reassessment, and the primary patient BP display. Fixed at the same canonical choke point. Also confirmed a third divergence from the same investigation (`notes` field-name mismatch) needs no fix — the app already defensively handles both shapes everywhere it matters.
- **(Cycle 160)** Frontend `npm audit` back to 0 vulnerabilities; backend's critical RCE finding (`protobufjs` via `onnx-proto`) fixed via a targeted `overrides` entry and verified with a real embedding smoke test against the actual cached ONNX model, not just a clean dependency tree.
- **(Cycle 161)** Replaced the roadmap's "`strictNullChecks` blast radius unknown" with a real, categorized number (166 errors) and closed 44 of them across 10 files — including one genuine type-predicate bug (a `.filter()` null-narrowing check that looked correct but wasn't) found and fixed along the way. All 245/245 backend tests still pass.
- **(Cycle 162)** Backend `eslint` genuinely clean now (0 errors, both trees) — closed the 21-error finding Cycle 161 caught, verified formatting-only via a whitespace-stripped diff per file, 245/245 backend tests unchanged.
- **(Cycle 163)** `strictNullChecks` blast radius down to 109 (was 166 at Cycle 161's baseline) — closed the single largest remaining cluster (13 errors, one test file, one root cause) with a safe, idiomatic fix; 245/245 backend tests unchanged.
- **(Cycle 164)** `strictNullChecks` blast radius down to 102. Bigger find: discovered and root-caused a real bug in `encryption.e2e-spec.ts` (silently testing against the wrong encryption key due to a missing `ConfigModule.forRoot()` in the test's own module setup) — confirmed production is unaffected, fixed the test, 21/23→23/23. Also discovered the entire 10-file backend e2e test tier had never been run by this program before (separate Jest config, doesn't match the main suite's `testRegex`) — real baseline now on record instead of silently absent.
- **(Cycle 165)** Closed both e2e findings Cycle 164 left open: fixed a real missing-provider bug in `tool-orchestrator-api.e2e-spec.ts` (2 genuine clinical-tool services — `WellsDvtService`, `AbgInterpreterService` — invisible to its own test, plus 2 stale hardcoded tool-count assertions once they were added back), and confirmed `rag.e2e-spec.ts`'s skip is an intentional Pinecone-credential gate, not a bug. Backend e2e tier now **9/9 real suites, 202/202 real tests** — a from-scratch tier went from silently-absent to fully green in 2 cycles.
- **(Cycle 166)** `strictNullChecks` blast radius down to 97. Found a genuine TypeORM `create()` overload-resolution bug in `workspaces.service.ts` (3 call sites passing explicit `null` to entity fields typed as plain, non-nullable `string` despite being DB-nullable columns) — fixed at the call sites, verified DB-equivalent (both map to SQL NULL for a nullable column with no default), one test assertion updated to match. Also re-verified and corrected a stale roadmap claim: the ported Mongo integration suite's real blocker is the same `esbuild` Application Control issue affecting all Vitest work, not a Mongo-specific one.
- **(Cycle 167)** `strictNullChecks` blast radius down to 89. Closed `intent-classifier.service.ts` (one utility function's `T | null` return meeting an optional-property `T | undefined` field, fixed once at the single call site) and `device-token.service.ts` (entity fields typed non-optional despite being DB-nullable — widened to match, the idiomatic TypeORM `?` convention, zero behavior change). Found a real, previously-unflagged gap along the way: `DeviceTokenService` has no dedicated test spec at all.
- **(Cycle 168)** `strictNullChecks` blast radius down to 82. Cycle 166's `workspaces.service.ts` TypeORM `create()`-overload bug turned out to be a recurring pattern, not a one-off — found and fixed the identical shape in 3 more files (`notification.service.ts`, `personalization.service.ts`, `user-activity.service.ts`), applying the correct fix per context (call-site `|| undefined` for `create()` calls, entity-field widening for direct assignments — same distinction Cycle 167 established). Found a second, previously-unflagged test-coverage gap: `PersonalizationService` also has zero dedicated test coverage.
- **(Cycle 169)** `strictNullChecks` blast radius down to 71. The Cycle 166 pattern recurred a 3rd time (3 memory services), plus a new variant in `memory-fabric.service.ts` (`?? undefined` boundary conversion). Found a genuinely different bug shape in `integration-hub.service.ts`: TypeORM's `FindOptionsWhere<T>` rejects bare `null` for nullable columns even when the entity itself is correctly typed — the fix (TypeORM's `IsNull()` operator) has real behavioral stakes in a WHERE clause (unlike `create()`, `null` and `undefined` mean different things there), so verified it against a real SQLite database rather than just reasoning about equivalence.
- **(Cycle 170)** `strictNullChecks` blast radius down to 61. Fixed a real nullability gap in `EmergencyPatientService.persistPatientToDatabase` (the live, real-DB write-through path for actual patient records) where 2 fields could carry an explicit `null` the entity didn't expect. Fixed a genuine clinical-scoring type-inference quirk in `sofa-calculator.service.ts`'s SOFA total-score `.reduce()` — verified zero arithmetic change via its existing 54-test dedicated suite. Also fixed 2 deliberately-minimal pre-existing test fixtures (`administrative-automation-orchestration.lib.spec.ts`) that didn't structurally overlap enough with the real `Patient` type for a direct cast under strict checks.
- **(Cycle 171)** `strictNullChecks` blast radius down to 49. Found and fixed a genuine functional bug in `asset-access.service.ts`: role-recommended assets were computed from a property name (`recommendedAssetIds`) that doesn't exist on the `RoleProfile` entity (the real field is `preferredAssetIds`), so every user's role-based asset recommendations were silently empty. Widened 2 security-critical entities (`BiometricConfig.lockedUntil`, `TwoFactor.secret`/`.backupCodes`) to their real DB-nullable types, and narrowed `BiometricService.verifyBiometric`'s return type to match its actual all-succeed-or-throw control flow. Found 3 more zero-test-coverage services along the way (`BiometricService`, `BiometricController`, `AssetAccessService`), including the exact file the property-name bug was hiding in.
- **(Cycle 172)** `strictNullChecks` blast radius down to 37 — the entire remaining tail of scattered small clusters closed in one pass across 10 files. Found a real webhook-security gap in `subscriptions.controller.ts`: Stripe webhook handling had no guard against a missing raw request body or a missing webhook secret, both required for HMAC signature verification, and would have thrown an uncontrolled runtime error instead of failing closed with a clear rejection. Widened `AuditLog.userId` for its deliberate HIPAA-anonymization write path (verified the tamper-detection hash function already normalized the value, so the fix is provably behavior-neutral). Everything left in the roadmap item is now exactly the 4 large, previously-deferred sensitive clusters.
- **(Cycle 173)** `strictNullChecks` blast radius down to 25 — closed the first of the 4 large deferred clusters, `subscriptions.service.ts`. Found the sibling gap to Cycle 172's webhook fix: `createCheckoutSession` and `createCustomerPortalSession` called Stripe's API unconditionally, with no guard against the client being unconfigured, meaning any environment without Stripe set up would throw an uncontrolled runtime error on the very first checkout attempt instead of a clear, typed rejection. Fixed with a `ServiceUnavailableException` guard — and added 2 new tests specifically to exercise it, since the existing spec force-injects a mock Stripe client after construction, meaning this failure path had never actually been run by any test before.
- **(Cycle 174)** `strictNullChecks` blast radius down to 20 — closed `smart-handover-v2.service.ts` (real SBAR clinical handover summary generation). All 5 errors traced to one root cause: `buildPatientContext()` always hydrates `vitals`/`imaging`/`consults`/etc. with real defaults, but its declared return type used the loose, all-optional base interface, hiding that guarantee from every downstream consumer. Fixed with one new type (`HydratedPatientContext`) capturing what the function actually promises, rather than 5 separate defensive coalesces — verified against the one real, unmocked test exercising this exact code path. A concurrent parallel session had substantial in-progress work on `emergency-os`/`ocr-intake` files this cycle; confirmed zero file overlap before starting and left it entirely untouched.
- **(Cycle 175)** `strictNullChecks` blast radius down to 9 — closed `auth.service.ts`, the larger of the 2 remaining sensitive-service clusters. Added a fail-closed guard in `createDevSession` against its post-create re-fetch unexpectedly returning null, and widened 4 real DB-nullable `User` columns (verification/reset token+expiry pairs) that had been typed non-nullable. Widening surfaced a genuine parity gap: `verifyEmail`'s expiry check lacked the null guard its sibling `resetPassword` already had — added it, confirmed behavior-neutral via JS's null→0 relational-comparison coercion. Only `ai.service.ts` (9) remains. Full backend suite, full e2e tier, and the dedicated auth e2e spec all re-verified unchanged.
- **(Cycle 176 — MILESTONE)** `strictNullChecks` blast radius reached **0**. Closed the last cluster, `ai.service.ts` (9), with one field widening (`AIQuery.response`, the same TypeORM `save()`-overload failure shape as Cycles 166-172's `create()` bugs). With the count genuinely at zero rather than just small, flipped `backend/tsconfig.json`'s `strictNullChecks` from `false` to `true` for real — 16 cycles and 166 closed errors across 51 files are now permanently CI-enforced, not a re-measured snapshot that could silently regress. Verified both `tsc` configs and `ts-jest`'s inline override all respect the flag cleanly before committing. A full (not per-file) `eslint` sweep — the first in several cycles — also caught 10 pre-existing formatting errors this program's own Cycles 172-173 had left behind, undetected because later cycles only lint-checked their own changed files; fixed and manually verified token-preserving.
- **(Cycle 177)** Closed the most concerning entry on roadmap item #17 (zero-test-coverage services): `PineconeService`, the real RAG vector-similarity retrieval path. 20 new tests, including a full real round-trip against the in-memory store's actual cosine-similarity math and disk persistence, plus — via a lightweight fake of the Pinecone SDK — the entire previously-untested "real Pinecone" branch (the Cycle 172 minScore fix, filter-syntax construction, batching, metadata round-tripping, namespace routing, error handling). The first real test run caught a genuine wrong assumption in the test itself, confirming it exercises real logic rather than tautologies. 8 services remain on the list.
- **(Cycle 178)** Closed the `BiometricService`/`BiometricController` pair on roadmap item #17: real enrollment, verification, and account-lockout logic, tested with real (not mocked) SHA-256 hashing and `crypto.timingSafeEqual` comparison, the full 5-attempt lockout state machine, a defensive test for a corrupted stored-hash length failing closed instead of leaking a raw crash, and controller-level leak checks confirming shaped responses never expose the hashed challenge token or `User.passwordHash`. `AssetAccessService`, the list's other candidate, was deliberately deferred — a concurrent session was actively editing it this cycle. 6 services remain.
- **(Cycle 179)** Closed `CacheService` on roadmap item #17: 29 tests covering Redis connection auto-disable logic, graceful fallback on connect failure, and every cache operation both connected and disabled — including a correctness check for `getOrSet`'s `!== null` cache-hit test (would silently break for falsy cached values like `0` if ever loosened to a truthiness check). `SentinelController` was assessed and deferred to its own cycle given its size; `AssetAccessService` remains blocked by ongoing concurrent-session edits. 5 services remain. Cycle interrupted mid-flight by a large Reception Desk + platform-wide audit request — see separate Reception audit thread.
- **(Cycle 180)** Closed the top finding from the Reception Desk backend audit: the primary patient-creation endpoints had no duplicate-patient check of their own — only a Mongoose intake path that's off by default did. Ported the existing weighted-scoring duplicate algorithm (already shipped twice — frontend `patientDuplicateDetection.ts`, backend `mpi.service.ts`) into a pure, dependency-free module and wired it into `SmartIntakeService`, blocking a high-confidence match with a 409 unless the caller passes `confirmDuplicateOverride`. Found and corrected a real overstatement in the initial audit along the way: the reception UI already had a genuine, working client-side duplicate gate (3 separate places — `ReceptionWorkspace`'s modal, `SmartIntake`'s link-vs-create step, `QuickIntake`'s acknowledgment checkbox) — the actual gap was narrower (no *server-side* enforcement, so only an unattested caller bypassing the UI was ever at risk), and every trusted internal caller now passes the override flag so none of those working flows changed behavior. 18 new tests, zero regressions (251/251 suites, 2018/2018 tests).
- **(Cycle 181)** Closed `DeviceTokenService` on roadmap item #17: 16 tests covering push-notification device-token registration (create-vs-update-in-place branching), the soft-delete (`deactivateToken`) vs. hard-delete (`removeDeviceToken`, OR-matching by token or id) distinction, multi-user token grouping in `getTokensForUsers` (verified accumulation, not overwrite, across users with multiple devices), and `cleanupOldTokens`'s date-cutoff arithmetic verified to the exact ISO timestamp under fake timers, both at the 90-day default and a custom window. Also corrected an overstatement in last cycle's own roadmap entry: the "fire-and-forget DB write" item opened in Cycle 180 turned out to be a deliberate, already-documented design choice tied to the pre-existing item #2, not a standalone bug — removed from item #18's scope rather than left to stand uncorrected. One flaky failure surfaced under load in a full-suite run (`encryption.service.spec.ts`'s hash-throughput timing assertion) — confirmed unrelated via an isolated re-run and a second clean full run (252/252, 2034/2034).
- **(Cycle 182)** First full dependency/security re-audit in 22 cycles: closed a high-severity `postcss` CVE and a moderate `mongoose` prototype-pollution CVE plus a moderate `tar`/`sqlite3` DoS CVE, all via safe non-major patch bumps; also pruned a stray untracked `esbuild-wasm` package sitting in `node_modules` with zero references in `package-lock.json`. Found 2 new moderate `react-router`/`react-router-dom` CVEs with no fix available inside the installed 6.x line — correctly scoped as a real future migration (7.x) rather than attempted as a drive-by fix, and opened as a new roadmap item rather than silently skipped. Re-confirmed the pre-existing `@xenova/transformers`/`sharp` finding is unchanged, with fresh evidence rather than an assumed-still-true carry-forward. Full backend suite 252/252 (2034/2034) and e2e tier 9/9 (202/202) both unchanged after the mongoose/mongodb driver bump.
- **(Cycle 183)** Closed `AssetAccessService` on roadmap item #17 — the single strongest evidence-backed candidate on the list (a real bug, the Cycle 171 role-recommendation property-name mismatch, was already found hiding in this file purely for lack of coverage), unblocked once its long-pending concurrent-session diff was finally committed. 39 tests covering the full `resolveAccess` priority-ordered access-state cascade end to end, all 4 admin-only governance signal sources independently, the pinned>workspace-recommended>role-recommended>recent priority ordering (including confirming a locked asset can't be pinned past its lock), the complete `effectiveAccessState` mapping table, and orchestration-level coverage including legacy tool-id alias resolution that expands to a genuinely different real asset id. Zero regressions (253/253 suites, 2073/2073 tests).
- **(Cycle 184)** Closed `PersonalizationService` on roadmap item #17: 16 tests covering get-or-create preference defaulting (including the less-obvious case of an existing row with null fields, not just first-create), `Set`-based deduplication on partial updates, `recommendationsForUser`'s allow-list filtering semantics (empty = unrestricted, toolId-less workflows always pass), user-scoped saved-prompt CRUD with a confirmed no-leaked-`userId` serialization boundary, and `recordRecentPrompt`'s whitespace normalization/truncation/10-entry cap. Caught and fixed a wrong assumption in one of the tests themselves before ever running it — the whitespace-collapsing regex leaves one leading space rather than stripping it, verified with a standalone Node check before finalizing the assertion. Zero regressions (254/254 suites, 2089/2089 tests).
- **(Cycle 185)** Closed `ERPulseHandoverService` on roadmap item #17 — the last true zero-coverage service, upgrading a previously-thin, incidental coverage path (one unmocked call from a controller spec) to 20 dedicated tests: the full context-defaulting cascade (empty-context and fully-custom-context cases, confirming zero default leakage), all 5 independent risk-alert branches (hypoxia, tachycardia, ACS, sepsis/shock, critical imaging) plus a negative case, pending-action composition rules, and `fineTunePrompt`/`getPromptFeedback`'s ordering and defensive-copy semantics. Caught and fixed two of my own wrong test assumptions before ever running them — passing `labs: []` doesn't clear the service's default lab set, it silently falls through to it (`context.labs?.length ? ... : [...]`), which had smuggled extra risk alerts into two tests' expected results — verified with a standalone Node check and fixed before the first test run. Roadmap item #17 is now 8 of 9 closed; only `SentinelController` remains, still deferred for size. Zero regressions (255/255 suites, 2109/2109 tests).
- **(Cycle 186)** Closed `SentinelController` on roadmap item #17 — the last remaining candidate, finally tackled now that it no longer had to compete with a smaller, better-scoped target. Read all 4 injected services' real method signatures before writing anything, rather than guessing at the mock shape. 53 tests covering all 17 endpoints, weighted toward the ones with real logic: `unitPositions`'s limit-clamping across 7 boundary values (verified the surprising `'0'` → 50 case against a standalone Node snippet before trusting it — an empty-string-adjacent falsy trap, not the `[1, 200]` clamp one might assume from a glance), `ingestCad`'s clinical-field-detection/patient-block-merge branching, `alarmAction`'s full 13-entry verb-mapping table plus its reject-unknown-action and actor-id-fallback paths, and `analytics`'s real averaging math (independently computed by hand first). **All 9 candidates originally found on this item are now closed — roadmap item #17 fully closed after a 9-cycle sweep (Cy167 discovery, Cy177–186 closure).** Zero regressions (256/256 suites, 2162/2162 tests).
- **(Cycle 187)** With item #17 fully closed, picked up roadmap item #4 (`ts-prune` unused-export triage), which had sat untouched at "investigation-only" for many cycles — likely because a flat "~2,655 combined findings" reads as too unbounded for a single well-scoped pass. Tried the two blocked candidates first and ruled them out with real evidence rather than skipping silently: item #8 needs a fresh production build to re-verify, but `vite build` still fails on the same `esbuild`/Application Control `spawn UNKNOWN` this program has documented as blocking all Vite config-loading since Cycle 160; item #6 needs Docker, and `docker`/`docker ps` both report command-not-found in this sandbox. `ts-prune` itself needs neither — pure static analysis via the TS compiler API — so it stayed viable.

  Ran fresh `ts-prune` against both tsconfigs rather than trusting the stale roadmap estimate, then built real methodology instead of eyeballing the list: split the backend's 1350 raw findings into 728 actually inside `backend/` vs. 622 in shared repo-root files (`types/`, `engine/`, `ml-services/shared/`) that had been miscounted into the original "~702 backend" figure; split those 728 into 328 "used in module" (ts-prune's weak-signal tag — the export IS referenced, just not through the exact binding it's checking, not real dead code) vs. 400 true zero-reference. Grouped the 400 by file rather than reading them one at a time, which surfaced the dominant shape immediately: 19 files account for 274 of the 400 (68%), and every one of those 19 is a module-level `index.ts` barrel (`modules/moe-router`, `modules/tool-calling`, `modules/sentinel`, `modules/ai/foundation`, etc.). Checked real importers for each — `grep` for anything importing the bare module path, not assumption — and found most of these barrels genuinely are imported by other modules (`moe-router`, `surveillance`, `platform-governance`, `simulation`, `hospital-map`, `telemetry` all have real consumers reaching them through the bare path), so ts-prune is just flagging individual named exports within an otherwise-live barrel as a false positive. One barrel, `backend/src/index.ts` (a single line, `export * from './services/service-registry'`), had **zero importers anywhere** in the tree — confirmed via repo-wide grep, not inferred from the pattern — while its underlying `service-registry.ts` exports remain genuinely reachable through `main.ts`, `health.routes.ts`, and `services/index.ts`, none of which go through this particular barrel. Deleted it outright.

  Frontend `ts-prune` was also run fresh (4356 findings, vs. the stale "~1,953" estimate — real ~2.2x growth over the cycles since that number was last measured) but deliberately not triaged this cycle — same "don't overreach" discipline as every prior cycle that found a bigger-than-one-cycle scope and said so honestly rather than rushing a shaky pass.

  **Verification:** backend `tsc`/`eslint` both 0 errors; full backend suite unchanged at 256/256 suites, 2162/2162 tests (the deleted file had no test of its own, since nothing imported it to test); re-ran `ts-prune` after the deletion and confirmed the count dropped by exactly the expected 36 (1350→1314), isolating the fix with no side effects. Committed and pushed.

  **Lesson: when a roadmap item's number looks too large to triage in one pass, the fix usually isn't to attempt a fraction of it at random — it's to build real categorization first (location, signal strength, file-level clustering) and let the data show where the leverage actually is. Here, grouping by file turned "400 scattered findings" into "19 files explain 68% of them," which is a fundamentally different, much more tractable problem than the flat list ever looked like.**
- **(Cycle 188)** Finished the module-barrel verification Cycle 187 had explicitly left open ("spot-checked, not individually verified") for 7 of the ~19 barrel files identified as the dominant `ts-prune` false-positive shape. Used a more rigorous repo-wide import search this time — Cycle 187's narrower pattern had a real blind spot, missing `ai/foundation`'s genuine importer (`ai.module.ts`) — caught and corrected before concluding anything, rather than trusting the earlier quick check. The other 6 (`tool-calling`, `cost-optimizer`, `evaluation`, `training`, `memory`, `sentinel`) confirmed fully orphaned: `app.module.ts` wires each in by its specific `.module.ts` file, never the bare barrel path, and nothing else in the repo — checked frontend too — imports the barrel either. Read all 6 files first to confirm they're pure `export *` re-export barrels with no side effects before deleting any of them. Diffed the full `ts-prune` output before/after rather than trusting the raw count alone: 71 findings removed as expected, but 5 new weak-signal findings appeared — investigated rather than dismissed, and fully explained (types previously satisfied by the barrel's own re-export, now correctly re-classified as used-only-locally now that the barrel's gone; still not real dead code). Net -66 (1314→1248). Zero regressions (256/256 suites, 2162/2162 tests, unchanged — none of the 6 had test coverage of their own). **7 barrel files removed across Cy187-188, 107 findings resolved total; the barrel-file sub-investigation is now closed, though item #4 as a whole remains open.**
- **(Cycle 189)** Checked backend's last big remaining `ts-prune` cluster, `services/index.ts` (55 findings) — confirmed genuinely live (10 real importers among the legacy `api/*.routes.ts` files) — closing out the backend barrel-hunting vein Cy187-188 opened: everything left there now needs individual per-export judgment, not whole-file deletion. Extended the same barrel methodology to frontend for the first time, this time explicitly accounting for this repo's real path aliases (`@/*` → `./src/*`) so the importer search wouldn't have a blind spot the way Cycle 187's first pass briefly did. Result was a genuinely different, more interesting shape than backend's: most barrel candidates were live, one (`contracts/index.ts`, 72 findings) turned out to be a deliberately-unadopted-yet migration bridge (its own header comment says so — "New code should import from `@/contracts`... over time, migrate imports"), correctly left alone rather than deleted just because it matched the zero-importer pattern. One more (`services/native/index.ts`, 16 findings) led somewhere real: reading its content instead of just trusting the "zero importers" signal revealed a complete, working WebAuthn biometric helper — not dead code by any reasonable definition. Pulled the thread with `git log --diff-filter=D` rather than guessing, and found the actual story: commit `ce80849c` ("open-access demo... auth UI removal") deliberately deleted the one page that used to call this whole `native/` layer, as part of a real product pivot — not a bug, but it left the entire client-side biometric integration, and the backend's fully-built, fully-tested `BiometricService`/`BiometricController` (25 tests from Cycle 178) alongside it, with no way for a user to ever reach them. Opened as roadmap item #20 rather than either deleting 446 lines of real auth/crypto code unilaterally or trying to rebuild a login page without any product sign-off on which direction is even wanted. Deleted the one unambiguous leftover from that same commit — `src/pages/BiometricSetup.css`, a stylesheet with zero references anywhere. Verified: frontend `tsc --noEmit` and `eslint` both 0 errors; full Jest/Vitest run still blocked by the same persistent `esbuild` sandbox issue as every prior cycle, disclosed explicitly rather than glossed over — mitigated for this specific change since a CSS file deletion has no import-graph risk beyond what `tsc` and a repo-wide grep already confirmed. No backend files touched this cycle.
- **(Cycle 190)** First dependency/security re-audit since Cycle 182 (8 cycles ago) — the headline numbers looked alarming at first (frontend 0→26, backend recount to 38 high) until traced to source rather than reacted to. Grouped every finding by its `via` chain and found nearly all of it converges on one shared advisory, `brace-expansion`'s DoS vulnerability (GHSA-mh99-v99m-4gvg), reached through 3 separate, independently-confirmed-unfixable paths. Applied Cycle 182's "check real installed versions before trusting `fixAvailable`" lesson rigorously: `npm audit` suggested `jest@25.0.0` as the fix — checked the real installed version first (`29.7.0`/`30.4.2`) and confirmed that's a downgrade, not a fix. Same discipline for `eslint-plugin-jsx-a11y`: used `npm view` to confirm its actual latest release (6.10.2, already installed) still declares `minimatch: ^3.1.2`, and that minimatch line's own newest release still requires a `brace-expansion` version inside the vulnerable range — a definitive "no fix exists," not a guess.

  Found one of the three suggested fixes was, unlike the others, a genuine forward version: `rimraf` 5.0.5→6.1.3. Checked its real usage first (`prebuild: rimraf dist`, about as low-risk as a CLI dependency gets) and applied it. `npm install` showed real package churn but a full before/after audit diff showed **zero change** in the vulnerability count — traced why rather than left confused: `firebase-admin`'s `google-gax` and `mongoose`'s `gcp-metadata`→`gaxios` both pin their own internal `rimraf@5.0.10`→`glob@10.5.0`, completely independent of this repo's own declaration. Kept the bump as a real, verified improvement, but wrote it up explicitly as not reducing the count rather than letting the diff imply more than it delivered.

  One test failure surfaced across 3 full-suite verification runs: `encryption.service.spec.ts`'s hash-throughput timing assertion, the same test flagged as CPU-contention-flaky in Cycle 181. Re-applied that cycle's exact recipe: isolated run measured 9ms (nowhere near the 100ms threshold, unrelated to `rimraf`), and a follow-up full run came back clean (256/256, 2162/2162) — 2 of 3 runs clean, the one failure fully explained as pre-existing noise, not a regression.

  Refreshed items #5 and #19 with today's evidence instead of letting old citations go stale — #5 now cites the actual current CVE IDs behind the same unchanged conclusion; #19 reconfirmed the fix boundary is still `>=7.18.0`. Opened item #21 for the `brace-expansion` chain with the full 3-path evidence trail, explicitly noting low real-world exploitability (every affected package is dev/build tooling or an SDK's internal file-cleanup utility, never reached by untrusted input) rather than either alarmist framing or silent dismissal.
- **(Cycle 191)** Investigation-only cycle, no code changes. First closed out `ts-prune` barrel-hunting for good: checked the last 3 unverified candidates, the shared `lib/` barrels (`lib/cig`, `lib/native-ai`, `lib/patient-orchestration`, 177 combined findings) — all 3 confirmed heavily live across both trees via real importer checks, same rigor as Cycles 187-189. This closes the methodology out completely: every barrel-shaped candidate this program's `ts-prune` investigation ever surfaced has now been individually verified, not just pattern-matched, and only 7 files total across 4 cycles turned out to actually be dead.

  Then picked up roadmap item #18's validation-DTO sub-item, open since Cycle 180 specifically because building a safe whitelist DTO needed "enumerating every field real callers send today" first — a real prerequisite, not a stalling excuse. Traced all 5 real frontend callers of the 3 unvalidated intake endpoints down to their actual constructed payloads (not their TypeScript types, which are loosely-typed `any`/`Partial<Patient>` at the call-site level and wouldn't reveal what's really on the wire) and compared each against the canonical `EmergencyPatient`/`EmergencyVitals`/`JourneyEvent` types a naive DTO would otherwise be modeled on. The divergence was real and specific, not vague: `notes` has a consistent real shape (`{id,type,body,authorId,createdAt,metadata}`, used by 2 of 5 callers) that doesn't match the canonical interface's `{id,text,authorId,timestamp}` at all; `flags` is even more interesting — most callers send a plain `string[]` (matching canonical), but `SmartIntake.tsx`'s vertical-slice path sends structured objects (`{type,reason,severity,detectedAt}`) instead, a genuine inconsistency *between real callers*, not just against the idealized type; `vitals` uses `bpSystolic`/`bpDiastolic` field names where canonical declares `sbp`/`dbp` and requires a `recordedBy` field that no traced caller ever sends; and `timeline`/`JourneyEvent` turned out to have 3 distinct real shapes in simultaneous use, one of which (`SmartIntake.tsx`'s vertical-slice timeline entries) is missing the canonical-required `staffId` field entirely — exactly the kind of gap that would make a strict `required()` validator reject a real, working, currently-succeeding request. Also catalogued 7 fields real callers send that don't exist in the canonical interface at all (`phone`, `healthCardNumber`, `healthCard`, `source`, `lastAssessedTime`, `quickSafetyFlags`, `waitingRoomStatus`) — every one of these would be silently stripped or rejected under `forbidNonWhitelisted:true` today if a DTO were built straight from the canonical type without this evidence.
- **(Cycle 192)** Directly acted on a real finding Cycle 191 surfaced but hadn't fixed: `SmartIntake.tsx`'s vertical-slice flow builds `flags` as `{type, reason, severity, detectedAt}` objects, but `EmergencyPatientService.createPatient` stored `input.flags` verbatim with zero type coercion, and the canonical `EmergencyPatient.flags` type is `string[]`. Didn't stop at "this is a type mismatch" — grepped both trees for every real consumer of `.flags` and found it's load-bearing well beyond display code: 5+ backend call sites (reassessment-queue filtering, EMS-high-risk detection, admission-pending checks, escalation handling) and 6+ frontend call sites (`NEWS2.tsx`/`qSOFA.tsx`'s clinical-score reassessment triggers, `calculatorSave.ts`, `HandoffBriefGenerator.tsx`) all do `.flags.includes(PatientFlag.X)`, which silently returns `false` forever against an array of objects — no crash, no error, just a quietly-broken safety signal for exactly the highest-acuity patients (P1/P2 priority, or vitals that trigger reassessment) created through this one specific intake path.

  Fixed at the single canonical choke point (`EmergencyPatientService.createPatient`) rather than patching each consumer: a new `normalizePatientFlags()` helper passes strings through unchanged, extracts `.type` from object-shaped entries (verified the exact real shape against `SmartIntake.tsx`'s own `createFlag()` helper and confirmed `'HighRisk'`/`'ReassessmentDue'` match the `PatientFlag` enum's real values before writing the extraction logic), drops anything unrecognizable rather than storing `"[object Object]"` garbage, and dedupes. Every caller is protected now, not just the one that surfaced the bug. Wrote a dedicated new spec (`emergency-patient-flags-normalization.spec.ts`, 7 tests) covering the real object shape, a mixed string/object array, unrecognizable-entry dropping, cross-shape deduplication, and — importantly — 2 tests that pin the pre-existing P1/P2-default and empty-array-stays-empty behaviors unchanged, proving the fix is additive rather than a behavior change in disguise. Deliberately backend-only: the frontend's own local pre-persist state wasn't touched, since a frontend change still can't be verified in this sandbox (the persistent `esbuild` block) — the backend fix closes the actual data-integrity boundary regardless of what the frontend holds transiently before the API round-trip completes. Zero regressions (257/257 suites, 2169/2169 tests).
- **(Cycle 193)** Continued straight down Cycle 191's evidence trail: the `flags` bug fixed last cycle and the `vitals` bug fixed this cycle share the exact same source (`SmartIntake.tsx`'s vertical-slice flow) and the exact same fix location (`EmergencyPatientService.createPatient`), so checked the vitals divergence with the same rigor before assuming it mattered as much. It mattered more: grepped both trees for `.sbp`/`.dbp` consumers and found blood pressure is read far more widely than flags ever were — `qSOFA.tsx`'s sepsis-screening hypotension criterion, `alertEngine.ts`'s critical SBP<90/>180 alert, `reassessmentEngine.ts`'s SBP-triggered reassessment, 2 backend services (`clinical-protocol.service.ts`, `deterioration-prediction-v3.service.ts`), and the primary BP display across half a dozen patient-summary/handoff surfaces. `SmartIntake.tsx`'s `normalizeSmartIntakeVitals()` sends `bpSystolic`/`bpDiastolic`, never `sbp`/`dbp`, and never sends `recordedBy` at all (canonical type requires both `sbp`/`dbp` optionally and `recordedBy` as required) — meaning a real, currently-live pathway sends vitals that silently show as missing BP everywhere, for exactly the same class of high-acuity patients as Cycle 192's bug.
- **(Cycle 194)** Closed out item #18's evidence-map triage completely by resolving the 4th and last divergence Cycle 191 found: `SmartIntake.tsx`'s vertical-slice `timeline` entries are missing the canonical-required `staffId` field. Unlike the `flags`/`vitals` bugs, traced every real consumer and found this one causes no actual failure — the one UI-facing reader of backend journey events (`patientTimeline.ts`'s `mapJourneyEvent`) already falls back through `staffId`/`actorStaffId`/`by` into an optional display field, and the one non-defensive reader (`emergencyStore.ts`'s `workflowLogFromJourneyEvent`) only ever operates on the frontend store's own natively-created events, never the raw backend payload. Confirmed "no fix needed" with the same consumer-tracing rigor as a "yes, fix it" finding — investigation-only, zero code changes.
- **(Cycle 195)** Re-reading `createVerticalSlice` to double-check a Cycle 194 claim turned up a real second bug: `EmergencyPatientService.updatePatient` — the handler behind the live, authenticated `PATCH /api/patients/:patientId` endpoint — does a completely raw merge with none of Cycle 192/193's `flags`/`vitals` normalization, meaning the exact same 2 already-fixed bugs were fully reachable again through this second write path. Checked all 3 other real internal callers before fixing to confirm no regression (a reception-escalation flags-append already sending plain strings, an OCR-intake demographics-only patch, and a roomId-only test patch), then applied the same normalization inside `updatePatient` itself. 6 new tests, zero regressions (259/259 suites, 2183/2183 tests).
- **(Cycle 196)** First cycle to actually render and look at the app rather than rely on static analysis — served the existing `dist/` build statically and drove the real system Edge browser via Playwright to screenshot 6 pages, since `npm run dev` is still fully blocked by the `esbuild` sandbox issue (re-confirmed fresh: `vite.config.ts` fails to load with `Error: spawn UNKNOWN`). Found a real visual bug on the Whiteboard: the header's status-chip rail hard-clips its last chip mid-word when its content overflows the rail's width, with no visual cue it's scrollable — measured via DOM inspection (346px of content in a 288px rail), not guessed. Fixed with a theme-agnostic `mask-image` edge fade and verified the fix actually renders correctly by patching the equivalent rule directly into the already-built (gitignored) CSS bundle, reloading through the same screenshot pipeline, and confirming by eye at 4x zoom that the clipped text now fades instead of hard-cutting — a real visual-verification path for CSS-only changes despite the Vite rebuild block. Opened roadmap item #22 for this as an ongoing thread.
- **(Cycle 197)** Continued item #22, screenshotted 12 more pages, and found a substantially higher-impact bug than Cycle 196's: the Copilot launcher button rendered as an unstyled, full-viewport-height bar down the entire right edge of every page, instead of the small rounded pill its own CSS designs it as. Root-caused rather than patched around: its styling lived in a stylesheet that only loads as a side effect of lazily importing the full Copilot panel component, but the button specifically renders when that panel is closed — meaning the lazy import, and its CSS, had genuinely never fired on a fresh page load. Confirmed via live `getComputedStyle` (`position: static` instead of the intended `fixed`) and via the actual `dist/assets/` chunk layout, not guessed — this reproduces for every real user's first session, not just the static-serve verification method. Fixed by relocating the CSS to the app shell's own eagerly-loaded stylesheet; verified by hand-moving the same rule between the two real build chunks and confirming both a live screenshot and computed styles matched the intended pill design exactly (165×47px, `border-radius: 999px`, `position: fixed`, bottom-right corner) before trusting the source fix.
- **(Cycle 198)** Continued item #22, screenshotted 12 more pages (30 total across 3 cycles), and found a third real bug on the Clinical Calculator Hub: single-word category badges like "GENERAL" broke mid-letter across 2 lines ("GENERA"/"L") on cards with a long title. Diagnosed via computed styles rather than CSS-reading alone: the badge is a flex sibling of the title in a `display: flex` header, had no `flex-shrink: 0`, and got squeezed below its natural content width by a long title with nothing to absorb the pressure — with no space in a single word to wrap at, `overflow-wrap: break-word` split it mid-letter instead. Fixed with `flex-shrink: 0; white-space: nowrap` on the badge, redirecting the space pressure onto the title, which already wraps gracefully at word boundaries. Verified via the same dist-patch-and-screenshot technique, confirming all 4 visible badges on the page — including the 11-character "RESPIRATORY" — rendered correctly afterward, and via grep that this is the class's only definition anywhere (no competing rule).
- **(Cycle 199)** Continued item #22, screenshotted 14 more pages (44 total across 4 cycles), and found the highest-blast-radius bug of the thread: a large, solid dark-navy rectangle with no content, appearing on every command-center-style page. Traced via DOM inspection to `.cdl-zone--primary-actions`, a layout section wrapping the real "Critical actions" cards, whose entire background was being painted dark by an unrelated, deliberate app-wide sweep rule for `--primary` *button* modifiers — a plain substring-match selector (`[class*='--primary']`) that, having no word-boundary check, also matched this *zone's* class name purely by coincidence of substring. Confirmed via grep this was the only accidental victim among 15 real `--primary`-containing classes (the other 14 are genuine buttons the rule should catch), and separately checked its `[class*='cta']` sibling selector for the same collision risk, finding none. Fixed with a one-line `:not(.cdl-zone--primary-actions)` exclusion, following the exact pattern the rule already used for one previously-known false positive — the deliberate invariant itself untouched. Verified via the same dist-patch-and-screenshot pipeline: confirmed via `getComputedStyle` (background now fully transparent) and a live screenshot that the entire dark rectangle is gone.
- **(Cycle 200)** Continued item #22, screenshotted 14 more pages (58 total across 5 cycles), and found the thread's first content/copy bug rather than a layout one: the Hospital Maturity Assessment page's questions read "How mature is your hospital's ai maturity?" and "...iot readiness?" — both wrong-cased. Root cause: the question template blindly lowercases the whole dimension label to read grammatically mid-sentence, correct for 4 of 6 dimensions but destructive to the 2 whose labels contain acronyms ("AI", "IoT"). Fixed with an explicit, already-correctly-cased `midSentenceLabel` field per dimension rather than a smarter-but-fragile detection heuristic, since the set is small and fully enumerable. Being a JS/data change rather than CSS, couldn't use the dist-CSS-patch verification technique (a JS rebuild hits the same blocked Vite path) — instead verified mechanically with a standalone Node script computing all 6 question strings before/after, confirming byte-for-byte that only the 2 broken ones changed and the other 4 are identical to before.

  Fixed at the identical choke point with `normalizeVitalsEntry()`/`normalizePatientVitals()`: `sbp = raw.sbp ?? raw.bpSystolic` (canonical wins if both present), same for `dbp`, `recordedBy` defaults through `assignedStaffId` then a hardcoded `'intake'` fallback (matching the same fallback value the default timeline entry already used). One subtlety verified with a standalone Node script before writing the test, not assumed: `normalizeSmartIntakeVitals()`'s own `parseNumber()` helper returns `null` (not `undefined`) for an empty form field, and `raw.sbp ?? raw.bpSystolic` needed to correctly fall through a `null` `bpSystolic` too (nullish coalescing treats `null` and `undefined` identically) while still ending up `undefined` in the final output rather than a stored `null` — confirmed with 5 real input combinations run through the actual expression before trusting it.

  Separately, and just as important as the fix itself: went back to the third divergence Cycle 191 found (`notes` shape) rather than leaving it unresolved, and found it doesn't need fixing at all — grepped for `note.text`/`note.body` consumption and found the app already defensively reads `note.text || note.body` in more than a dozen files (`PatientDetailPanel.tsx`, `SepsisBundleTracker.tsx`, `StrokeCodeProtocol.tsx`, `shiftSummaryData.ts`, `patientAiContext.ts`, and others) — a real, existing, working accommodation for exactly the shape mismatch Cycle 191 flagged. Confirming a finding doesn't need action, with evidence, closes the thread as surely as fixing it would — and avoids the mistake of "fixing" code that already handles the real-world case correctly.

  8 new tests (`emergency-patient-vitals-normalization.spec.ts`) covering canonical pass-through, the real `bpSystolic`/`bpDiastolic` mapping, both `recordedBy` fallback levels, the null-vs-absent edge case, single-object vs. array input (the pre-fix code's own dual-mode handling), independent multi-entry normalization, and empty-array preservation. Zero regressions (258/258 suites, 2177/2177 tests).
- **(Cycle 201)** First real, unmocked full-stack round trip this program has ever run for the AI chat feature — real compiled backend + real compiled frontend + a real browser — found and fixed a total, unconditional break: every real call to `POST /api/ai/node/conversational` had been failing 400 "property stream should not exist" since 2026-06-13 (~120+ cycles), invisible to every prior test because none exercised a real `class-validator` pass against this DTO (the one existing spec asserted against source text). Fixed by declaring the field; added a genuine regression test confirmed red-without/green-with the fix. Also re-ran the pre-existing `interactive-ai.spec.mjs` suite (IX12-14) fresh for the first time since 2026-07-18 — 8/8 clean. Zero regressions (260/260 suites, 2186/2186 tests, +1 suite +3 tests).
- **(Cycle 203)** Found and fixed a real bug in `/notifications` via a same-condition side-by-side comparison against `/security`/`/audit`: a raw browser `SyntaxError` was leaking directly into the UI on a failed API call instead of the clean, designed fallback message the app's own shared `parseApiResponse` helper already provides — 4 methods in `NotificationService.ts` called raw `response.json()` instead of the file's own already-correct pattern used by its other 4 sibling methods. Also found the real service had zero real test coverage (the same-named test file tests an unrelated fixture; the component test mocks the service wholesale) and added a dedicated suite mocking only the actual network boundary, closing the exact gap that let this bug hide.
- **(Cycle 204)** Found and fixed a real bug affecting 2 separate pages from a single root cause: `ClinicalAlertsPage` and `HospitalCommandCenter` both passed their actual page content to the shared `EmergencyRoutePage` wrapper via a prop (`activeWork`) the component never declared — only `children` — so it was silently dropped every time, with the surrounding summary cards (fed by a different, correctly-wired prop) rendering fine enough to mask it. Fixed at the component level (`activeWork ?? children`), restoring both pages with zero changes to either page's own source; added a 3-test regression suite plus a standalone logic proof since Vitest is still blocked.
- **(Cycle 205)** Found and fixed a real CSS Grid bug affecting `/competencies` and `/credentials`'s chart panels — a grid item with `overflow:hidden` collapsed to 42-158px instead of sizing to its real ~300px chart content, per a genuine, documented CSS Grid/Flexbox spec interaction. Fixed at the shared component CSS level with a scoped `:has(.dashboard-chart)` minimum height, protecting all 36 real pages using this shared chart-panel component rather than patching 2 pages individually. Being pure CSS, verified through the full dist-patch-and-restore pipeline with live `getComputedStyle` measurements and fresh screenshots confirming real bars now render.
- **(Cycle 206)** Chased down the visual-polish thread's scariest-looking finding yet (garbled overlapping patient-card text) and correctly identified it as a dist-staleness false positive rather than fixing something current source had already resolved — verified precisely via a source-vs-compiled-bundle comparison, not a guess. Found and fixed 2 real bugs instead: a CSS Grid default-value interaction inflating a 3-line status card to 711px on `/emergency/settings` (`align-content: start`, one line, verified via live dist-patch measurement), and a visible double-period typo on `/emergency/referrals` from concatenating an already-punctuated error message.
- **(Cycle 207)** Corrected a real accounting error in item #22's own running total (114→99 of 182 confirmed/likely covered, after finding ~12 route leaf-names had been silently double-audited across the pre-session/session boundary) before continuing the thread. Found and fixed a genuine WCAG contrast failure (1.41:1 → 6.47:1) on the ED's critical-alert "Acknowledge" button, caused by 3 independent CSS substring-match sweep rules all mis-firing on the same element — the identical footgun class Cycle 199 first found, now confirmed recurring in a second rule family. Also confirmed `/workspace`/`/start`'s ErrorBoundary crash is a stale-`dist/`-artifact false positive of an already-fixed Cycle 154 bug, not a new one.
- **(Cycle 208)** Closed out the `.profile-copilot-card__cta` contrast bug Cycle 207 flagged but didn't fix (1.41:1 → 6.60:1) — this time walked the live rule cascade before patching, found only 1 of the 2 usual sweep rules actually needed excluding (the other already resolved to the correct value for this component), and fixed precisely that one rather than reflexively copying the prior fix's shape. Attempted to also verify the 2 remaining unverified `__cta` candidates via this repo's own existing reception-role QA script, found the script's demo-auth payload is itself stale against the current user-hydration logic — left both honestly unfixed rather than guessing.
- **(Cycle 209)** Found and fixed 2 real bugs on `/live-map`: every "Tracked assets" card ran its title/meta/subtitle together with zero space (missing stacking layout on 3 inline children) — fixed with `display:flex; flex-direction:column`; and SVG marker labels rendered ~7x their own marker's diameter (never-set `font-size` defaulting to a browser default interpreted as raw SVG viewBox units) — fixed with an explicit `font-size`. Also found and fixed the data half of the same page's bug: a tautological ternary (`x ? y : y`) in `buildFleetTrackingMarkers` unconditionally duplicated the vehicle's status word, redundant with what the UI already showed in a sibling element.
- **(Cycle 210)** Found and fixed a real, previously-uncaught crash on `/legal/consent-history`: two call sites passed a plain `{label, onClick}` object as `EmptyState`'s `action` prop, which React can't render as a child ("Objects are not valid as a React child") — confirmed with a genuine `react-dom/server` reproduction proving the exact crash message, then the exact fix rendering clean. Also fixed a quieter sibling bug at the same call sites (`message` passed instead of the real `description` prop, silently dropping the error text). Added the component's first-ever test coverage (3 tests).
- **(Cycle 212)** Closed out the 17-cycle route-audit thread — all 179 of the app's distinct real routes now looked at at least once. Found and fixed a small real acronym-casing bug ("Api Health"/"Ai Health" → "API Health"/"AI Health") on `/saas-health`, the same bug shape Cycle 200 found (a blind case-transform over a small, fixed id set), fixed the same way (explicit correct labels, verified byte-for-byte via a standalone script).

---

## Prioritized roadmap

*Carried forward from the pre-rebuild archive. Status current as of Cycle 157 unless noted.*

| # | Status | Item | Note |
|---|---|---|---|
| — | ~~P1~~ DONE | 17 frontend test files (25 tests) failing with real named-assertion failures, not teardown noise | Closed 2026-07-23 (Cycle 159) — root-caused into ~8 distinct clusters (calculator lazy-loading refactor, backend route inventory gaps, renamed config file, intentional soft-fail behavior change, 2 stale doc/status fields, a new unmocked AI dependency, a dropped BEM modifier, an aria-role query mismatch) and fixed individually; full suite re-verified clean: **927/927 files, 12045/12045 tests.** |
| — | ~~P1~~ DONE (mostly) | Backend `npm audit` critical + frontend audit regression | Closed 2026-07-23 (Cycle 160) — frontend 2→0 vulnerabilities via `npm audit fix`; backend 8→2 via `npm audit fix` (non-force) + a targeted `protobufjs` override, verified with a real embedding smoke test. Remaining `sharp` high finding (item #5 below) needs native-binary verification this sandbox can't provide locally. |
| — | ~~P2~~ DONE | 1 backend test file failing: stale test double missing `AiGatewayService.attachUnifiedNode` | Closed 2026-07-23 (Cycle 159) — added the missing mock method to `test/tool-calling.spec.ts`'s `AIGatewayService` provider; backend suite now **245/245 suites, 1922/1922 tests.** |
| 1 | P1 OPEN | Untangle frontend circular deps centered on `emergencyStore.ts` | First `madge --circular` scan found 72; **re-scanned fresh Cycle 196: now 64** (`npx madge --circular --extensions ts,tsx src/`), still spanning many genuinely distinct cycles across different subsystems (arrival control layer, three-minute timer engine, unified knowledge graph engine, whiteboard privacy models) rather than one shared root cause — confirms this still needs a dedicated architecture pass, not a drive-by; not attempted this cycle. |
| 2 | P1 OPEN | Cut `EmergencyPatientService` reads to the database (Phase 2) | One of two blockers closed (entities exist); remaining reentrancy/race issue needs a dedicated design pass. |
| 3 | P1 OPEN | Get a real pass on the ported integration test suite (`tests/integration/emergency-os.test.ts`) | Blocked locally — **re-verified Cycle 166**: the real blocker is the same `esbuild` Application Control policy affecting all Vitest runs since Cycle 160 (Vitest's config load fails with `spawn UNKNOWN` before Mongo is ever reached), not a Mongo-specific issue as previously framed. Not blocked on real CI. |
| 4 | P2 OPEN (Cycle 191: barrel-hunting methodology fully exhausted) | Triage `ts-prune` unused exports — fresh counts: 4356 frontend / 1248 backend-owned remaining (was stale "~1,953 / ~702") | Investigation-only until Cycle 187. Cy187 built the methodology (backend-owned vs. shared-root split, weak/strong signal split, file-clustering) and closed `backend/src/index.ts` (36 findings). Cy188 finished verifying the other 18 backend barrels the methodology surfaced, closing 6 more (71 findings, -5 explained side effect). Cy189: `services/index.ts` (55 findings) confirmed genuinely live, backend barrel vein exhausted; extended to frontend, 6 barrel candidates confirmed live, `contracts/index.ts` confirmed an intentional migration bridge, `services/native/index.ts` surfaced item #20 instead of a deletion. **Cy191: checked the last remaining candidates — the shared `lib/` barrels (`lib/cig` 91 findings, `lib/native-ai` 57, `lib/patient-orchestration` 29) against real importers in both trees — all 3 confirmed genuinely, heavily live.** The barrel-hunting methodology is now confirmed fully exhausted everywhere it could apply (backend module barrels, frontend module barrels, shared lib barrels — all checked, only 7 files total were ever actually dead). Remaining findings (~1,212 backend, all 4356 frontend) are individual-export-tier — real but requiring per-export usage proof, a fundamentally slower category not well-suited to this program's per-cycle pace. |
| 20 | P2 OPEN (new, Cycle 189) | Biometric login has zero UI entry point — backend + client SDK both real and complete, page component missing | Found while investigating item #4's `services/native/` cluster. `src/services/native/biometric.ts` (WebAuthn helper, 62 lines, complete) plus 6 sibling native-capability helpers (voice, push, permissions, share, preferences, sync) have zero importers anywhere in the frontend. Traced via git history: their sole consumer, `src/pages/BiometricSetup.jsx`, was deliberately deleted in commit `ce80849c` ("open-access demo with SaaS profile bottlenecks and auth UI removal") — a deliberate product pivot, not an accidental orphan. Backend's `BiometricService`/`BiometricController` remain fully implemented and tested (Cycle 178, 25 tests) with no frontend caller. 4 internal inventory/documentation files (`frontendApiCallsInventory.ts`, `capabilityExposureMatrix.ts`, `segmentInventory.ts`, `backendRouteExposurePolicy.ts`) still reference `BiometricSetup.jsx` as if it exists — confirmed harmless (not read by any test that would fail), but factually stale. **Not fixed this cycle** — this is a product decision (restore the login UI using the still-real backend+client infra, or formally retire the feature and remove the now-dead `services/native/` layer + backend biometric module + stale doc references), not a hygiene item a QA cycle should decide unilaterally. |
| 5 | P2 OPEN | `sharp <0.35.0` (high) via `@xenova/transformers`'s vendored copy, and whether to migrate off `@xenova/transformers` entirely | Cycle 160: critical `protobufjs` finding in the same chain is now fixed via override (verified). `sharp`'s fix needs a fresh native binary this dev sandbox's Application Control policy blocks from loading — needs CI/Linux verification, not a local one. `@xenova/transformers` itself is still abandoned at 2.17.2 (no newer version exists); `@huggingface/transformers` is the maintained successor and a plausible drop-in (same `pipeline()` API) but not evaluated end-to-end this cycle. **Re-confirmed unchanged, Cycle 182, and again Cycle 190** (fresh CVE IDs this time — CVE-2026-33327/33328/35590/35591, same underlying libvips advisory chain): `npm audit fix --dry-run` still only offers `--force` → downgrade `@xenova/transformers` to 1.4.2 (older than currently installed 2.17.2, not a real fix) — same conclusion, no safe path found. |
| 19 | P2 OPEN (new, Cycle 182) | `react-router`/`react-router-dom` 2 moderate CVEs (open-redirect-via-backslash, SSR-hydration constructor injection) with no fix inside the 6.x line | Found during a fresh dependency/security audit sweep. Latest 6.x release is 6.30.4 (currently installed) — both advisories are only patched in the 7.x line (latest 7.18.1), a real framework migration (React Router 7 merged Remix's data-loader/route-definition patterns), not a drop-in bump. The SSR-hydration CVE is likely not exploitable in this deployment (a Vite SPA, no SSR) but the open-redirect one is a real client-side concern. Deliberately not attempted this cycle — needs its own scoped migration pass with real before/after route-behavior verification across the ~190+ route surface, not a dependency-audit drive-by. **Re-confirmed unchanged, Cycle 190**: still needs >=7.18.0, latest is 7.18.1, same conclusion. |
| 21 | P2 OPEN (new, Cycle 190) | `brace-expansion` DoS advisory (GHSA-mh99-v99m-4gvg, `<=5.0.7`) cascades through 3 separate un-fixable dev-tooling/SDK chains — 24 frontend + ~34 backend "high" findings, one root cause | First full re-audit in 8 cycles (last was Cycle 182) surfaced a much larger "high" count on both trees than previously measured. Traced to one shared advisory reached via 3 independent, each individually-confirmed-unfixable paths: **(1)** `eslint-plugin-jsx-a11y@6.10.2` (the actual latest release, confirmed via `npm view`) still declares `minimatch: ^3.1.2`; that line's newest release (`3.1.5`) requires `brace-expansion: ^1.1.7`; that line's newest release (`1.1.16`) is still inside the vulnerable range — no compatible patched release exists anywhere in the chain, on either tree. **(2)** `jest`'s internal `@jest/reporters`/`babel-plugin-istanbul` chain pins an old `glob@7.2.3`; `npm audit`'s suggested "fix" (`jest@25.0.0`) is actually a downgrade from the real installed version (`29.7.0` backend / `30.4.2` frontend) — confirmed by checking real installed versions rather than trusting the audit's version number at face value. **(3)** Backend only: `firebase-admin`'s `google-gax` and `mongoose`'s `gcp-metadata`→`gaxios` chains each pin their own internal `rimraf@5.0.10`→`glob@10.5.0`, invisible to and unfixable by any change to this repo's own `package.json`. All 3 need an upstream release from a third-party maintainer (`eslint-plugin-jsx-a11y`, `jest`, or Google's `google-gax`/`gaxios`) that this program can't force. Real-world exploitability is low across all 3 — every affected package is dev-only build/test tooling or a GCP SDK's internal file-cleanup utility, never reached by untrusted runtime input in this application's actual attack surface. One small, verified, real (if audit-count-invisible) improvement made this cycle: backend's own direct `rimraf` bumped 5.0.5→6.1.3 (genuine forward upgrade, not a downgrade), cleaning this repo's own `prebuild` script dependency to a modern `glob@13.0.6` — does not reduce the audit's reported count since chains (2) and (3) above persist independently of this repo's direct dependencies. |
| 22 | P2 OPEN (Cycle 213: 19th+20th real bugs found+fixed — the 2 reception `__cta` candidates Cycles 207-208 left unverified, both closed) | Ongoing visual/UI polish pass — hold the app to a genuinely high ("framer-quality") design bar, found via actually looking at rendered pages, not static analysis | Opened per an explicit user request to keep scanning for design-quality gaps, not just correctness bugs. **Cycle 196**: Whiteboard status-chip rail hard-clips its last chip mid-word when overflowing, no scroll cue — fixed with a `mask-image` edge fade. **Cycle 197**: Copilot launcher button rendered as a broken, unstyled full-height bar down the entire right edge of every page — its CSS lived in a lazy chunk that never fires when the button (which renders precisely when the panel is *closed*) is what's showing. Fixed by relocating the CSS to the eagerly-loaded app shell stylesheet. **Cycle 198**: calculator category badges ("GENERAL") broke mid-letter on cards with a long title — no `flex-shrink: 0` on the badge. Fixed with `flex-shrink: 0; white-space: nowrap`. **Cycle 199** (highest blast radius): a large solid dark-navy rectangle with no content on every command-center-style page — the section wrapping the "Critical actions" cards (`.cdl-zone--primary-actions`) was being painted dark by an unrelated, deliberate app-wide `[class*='--primary']` button sweep rule that, as a plain substring match, also caught this layout zone's unrelated class name. Fixed with a targeted `:not()` exclusion. **Cycle 200 finding** (first content/copy bug, not layout/CSS): the Hospital Maturity Assessment page asks "How mature is your hospital's ai maturity?" / "...iot readiness?" — `hospitalReadinessAssessment.ts`'s question template blindly calls `.toLowerCase()` on the whole dimension label to make it read grammatically mid-sentence, which also destroys the "AI"/"IoT" acronyms in 2 of the 6 dimensions. Fixed by adding an explicit, already-correctly-cased `midSentenceLabel` field per dimension (data correct by construction, not a smarter-but-still-fragile detection heuristic) rather than transforming `label` (left untouched, since it's used elsewhere as a real heading). Being a JS/data change rather than CSS, this fix couldn't use the dist-CSS-patch verification technique (a JS bundle rebuild hits the same blocked Vite path) — verified instead via a standalone Node script computing all 6 question strings before/after, confirming byte-for-byte that only the 2 broken ones changed. **Cycle 203** (first backend-service bug in the thread, found via a same-condition side-by-side comparison across 2 pages rather than one page in isolation): `/notifications` leaked a raw `SyntaxError` ("Unexpected token '<'...") straight into the UI on a failed API call, while `/security`/`/audit` handled the identical "backend returned HTML" condition with a clean, designed message via the shared `apiClient.ts` `parseApiResponse` guard. `NotificationService.ts` had 4 methods calling raw `response.json()` instead of the file's own already-correct `parseApiResponse` pattern (used correctly by its other 4 sibling methods) — fixed all 4 to match. Also found the real service had zero real test coverage (the same-named test file tests an unrelated legacy fixture; the component test mocks the service wholesale) — added a new suite mocking only the network boundary, the correct level to catch this bug class. Verified via `tsc`/`eslint` (0 errors) plus a standalone Node script reconstructing the real `parseApiResponse` logic verbatim, proving the exact before/after strings byte-for-byte. **Cycle 204** (highest-severity finding in the thread — real functional content, not styling, silently missing): `/alerts` showed correct summary numbers but zero actual alert list below them. Traced to `ClinicalAlertsPage.tsx` passing its real content to the shared `EmergencyRoutePage` wrapper via a nonexistent `activeWork` prop — the component only ever read `children`, and since its props are typed `any`, the mismatch was silently swallowed rather than caught at compile time. Grepping every usage found the identical mistake independently made in `HospitalCommandCenter.tsx` too (the Command Center's entire "Sentinel EMS command" panel), confirming a real API footgun rather than a one-off typo. Fixed at the component level (`activeWork ?? children`), restoring both pages with zero changes to either page's own source. Also confirmed the dark-rectangle pattern recurring on `/dashboard` is *not* a new instance — same class Cycle 199 already excluded in source, just still visible in the stale, never-rebuilt `dist/` artifact this whole pipeline screenshots from. **Cycle 205**: `/competencies` and `/credentials` both showed a "status mix" bar chart panel that cut off immediately after its title, with no bars visible. Root-caused to a genuine CSS Grid + `overflow:hidden` interaction: a grid item with `overflow:hidden` gets an implied automatic minimum size of 0 rather than sizing to its real content, so `.dashboard-visual-panel` collapsed to 42px/158px despite containing a 210-240px chart — confirmed by comparing against a working chart panel that happened to sit inside an extra intermediate grid wrapper the 2 broken pages lack. Fixed at the shared component level (`DashboardVisualizations.css`, `.dashboard-visual-panel:has(.dashboard-chart) { min-height: 300px }`) rather than patching either page — this protects all 36 real pages using the shared `VisualizationPanel` component, not just the 2 confirmed broken. Being pure CSS, verified through the full dist-patch-and-restore pipeline: live `getComputedStyle` confirmed the height jump, and fresh screenshots confirmed both charts now render real bars with axis labels, matching every other working chart. **Cycle 206**: chased down what initially looked like the thread's worst finding yet — badly overlapping/garbled text on `/emergency/patients`'s patient cards — and correctly ruled it out as a dist-staleness false positive (current source has already restructured this exact area into a collapsed disclosure; the compiled `dist/` bundle just hasn't caught up), recording it as a confirmed non-issue rather than fixing something already fixed. Found and fixed 2 real bugs in the same batch instead: `/emergency/settings`'s risk indicator card was inflating 3 short text lines across a 711px box (a genuine CSS Grid default-value interaction — a narrow card stretched tall by taller siblings, then its own default `align-content:stretch` spread its rows to fill that height) — fixed with one line, `align-content: start`, verified via the full dist-patch-and-restore pipeline with live measurements and a fresh screenshot. `/emergency/referrals` showed a visible double-period typo from concatenating an already-punctuated error message with more literal text — fixed by stripping trailing periods before appending, verified via a standalone script across 4 real message shapes. All 12 findings verified appropriately to their kind via the `dist/` + static-serve + system-Edge pipeline established Cycle 196 (7 visually incl. 2 dist-patch, 1 mechanically, 3 via standalone logic reconstruction/component test, 1 confirmed-non-issue via source/compiled-bundle comparison). This is a real, ongoing category of work distinct from items #7 (Playwright/axe-core/Lighthouse, machine-checkable a11y/perf) and #9 (inline-style CI gate, a lint rule) — subjective design-quality judgment applied by actually rendering and looking at pages. **Cycle 207**: before continuing, corrected a real double-counting error in this row's own running total — reconstructing exactly which routes Cycles 196–206 covered found ~12 route leaf-names (`triage`, `alerts`, `laboratory`, `simulation`, `billing`, `notifications`, `security`, `audit`, `dashboard`, `staff`, `reports`, `timeline`) had been independently screenshotted in both an earlier cycle (198–200) and a later one (203–204) without either recognizing the repeat — the "114" figure was double-counting, not 114 distinct routes. Rebuilt the true covered set (56 exact-path-verified from Cycles 203–206's on-disk screenshot batches, plus 43 best-effort fuzzy-matched from Cycles 196–200's named pages): **99 of 182 confirmed/likely covered**, corrected the same way Cycle 153 corrected the "4 overlapping registries" claim. Also found the crawl's own 182-route inventory has 3 literal path duplicates (`/emergency/whiteboard`, `/emergency/intake`, `/emergency/queues`, each counted twice), so the true denominator is 179 distinct routes. With that honest baseline, screenshotted 14 confirmed-fresh routes (`/emergency/intake`, `/emergency/patient-room`, `/emergency/copilot`, `/emergency/provincial-health`, `/emergency/federated-learning`, `/emergency/digital-twin`, `/emergency/ai-governance`, `/emergency/tools`, `/emergency/ed-readiness`, `/emergency/diagnostics`, `/emergency/integrations`, `/workspace`, `/operations-center`, `/documentation` — running total **113 of 179**). `/workspace`/`/start` threw the app's ErrorBoundary — traced via source grep + `git log` + diff to `PlatformEntryHub`'s exact Cycle 154 crash, already fixed 2026-07-22 (`a5bd6e2f`) with its own regression test; the static `dist/` build (2026-07-19) simply predates the fix by 3 days — confirmed false positive, no action needed. Found and fixed a real WCAG contrast failure on the critical-alert "Acknowledge" button (`.fj-critical-banner__cta`, live on 4 routes: `ed-readiness`/`diagnostics`/`handoffs`/`reports`): measured 1.41:1 contrast (WCAG AA needs 4.5:1) caused by 3 independent CSS substring-match sweep rules (`color-normalization.css`'s `[class*='cta']`, plus 2 rules in `text-normalization.css`) all overriding this button's deliberate white-on-red styling because its class name doesn't follow the `--solid`/`--primary` BEM-modifier convention those rules assume — the same footgun class Cycle 199 found, now confirmed in a second rule family. Fixed with 3 targeted `:not(.fj-critical-banner__cta)` exclusions; verified live via dist-patch (1.41:1 → 6.47:1, screenshot-confirmed) plus a new 4-assertion regression test proven red/green via a standalone script. Also found `.profile-copilot-card__cta` live-measured with the identical broken colors, but its fix needs a separate design judgment call (different, non-critical background intent) — flagged, not fixed. **Cycle 208**: closed out that flagged follow-up instead of opening a new route batch. Re-derived `.profile-copilot-card__cta`'s intended design from its own CSS (`profile-surface-normalization.css`): a light muted surface with link-colored text, a secondary/ghost button, not a solid one — confirmed the same 1.41:1 contrast live. This time walked the *full* matching-rule cascade before patching rather than assuming the same 3-rule shape as the prior fix: since this is a real `<button>` (not an `<a>`), `text-normalization.css`'s plain-anchor rule never matches it at all, and its `__cta`-specific rule *does* match but already resolves to the same `--medical-text-link` token the component wants — so only `color-normalization.css`'s background sweep was actually wrong. Verified by patching only that one rule and re-measuring live before adding anything further: contrast rose to 6.60:1, confirmed by both the number and a fresh screenshot of a clean, readable button. Fixed with one `:not(.profile-copilot-card__cta)` exclusion (not three), avoiding an unnecessary, potentially-confusing edit to a rule that was already correct for this element. Also attempted to verify the 2 other `__cta` candidates Cycle 207 left unverified (`reception-job-profile-card__cta`, `reception-skill-strip__cta` — both gated behind a reception-facing demo role) using this repo's own existing QA tooling (`scripts/capture-reception-screenshots.mjs`'s `localStorage`-seeded demo-auth pattern) — the storage keys still match current config, but the role switch silently didn't take, traced to `UserContext.tsx`'s `hydrateStoredDemoUser()` apparently needing a richer payload shape than that script provides. Concluded the script itself has drifted stale (the same QA-infrastructure-rot pattern this program has found before — stale `.jsx`-only orphan-detection filters, a legacy test fixture) rather than reverse-engineering the correct shape mid-cycle; left both candidates honestly unfixed rather than guessing. **Cycle 209**: resumed the route batch, screenshotting 14 more (`/discover`, `/recommendations`, `/automation`, `/automation-audit`, `/automation-analytics`, `/assistant`, `/ai-command-center`, `/tools`, `/operations`, `/integrations/hub`, `/cosmos`, `/knowledge-graph`, `/3d-viewer`, `/live-map` — running total **127 of 179**). Two pages (`/ai-command-center`, `/cosmos`) fell back to the shared command-center dashboard and showed the exact `.cdl-zone--primary-actions` dark rectangle Cycle 199 already fixed; one (`/integrations/hub`) fell back to `/emergency/settings` and showed the exact pre-fix tall risk card Cycle 206 already fixed — both re-confirmed via source checks as still-present fixes in current CSS, just not yet reflected in the stale `dist/`; correctly not re-fixed. Found and fixed 2 real bugs on `/live-map`, plus a 3rd contributing cause to one of them: **(1)** the "Tracked assets" list's `<strong>`/`<span>`/`<small>` children had no stacking layout at all (all inline by default), so every card ran its 3 lines together with zero space ("North routeFLEET · occupiedoccupied · CareDroid North Clinic · ETA 18m") — fixed with `display:flex; flex-direction:column; gap:2px` on `.live-map__marker-item`, verified live via dist-patch that the 3 lines now render separately. **(2)** the map canvas's SVG "F"/"I" marker labels rendered at roughly 7x the diameter of their own marker dot — traced to an SVG `<text>` element that never had a `font-size` set anywhere, defaulting to the browser's ~16px text size interpreted directly as raw viewBox user-units next to a radius-2.2 circle; fixed with an explicit `font-size: 2.4px`, verified live via the same dist-patch pipeline showing small, legible glyphs. **(3)**, the word-level half of finding (1) — "occupiedoccupied" isn't just a missing space, the word is genuinely repeated — traced to a tautological ternary in `buildFleetTrackingMarkers` (`FLEET_STATUS_TONE[vehicle.status] ? vehicle.status : vehicle.status`, both branches identical) unconditionally duplicating the status already shown in a sibling UI element; fixed by deleting the dead ternary and the redundant subtitle field entirely (confirmed `FLEET_STATUS_TONE` remains meaningfully used elsewhere first, so nothing orphaned). Being a JS/TS logic change, verified via a standalone Node script reconstructing the real old/new logic verbatim against all 4 real vehicle statuses from the actual screenshot — old always duplicated, new never does — plus 2 new tests added to the existing `liveTrackingMapModel.test.ts`. **Cycle 210**: continued the batch, screenshotting 14 more (`/auth`, `/auth-callback`, `/reset-password`, `/verify-email`, `/legal/privacy-policy`, `/legal/terms`, `/legal/gdpr`, `/legal/hipaa`, `/legal/consent-history`, `/medical-iot`, `/fleet/command`, `/fleet/map`, `/digital-twin`, `/version` — running total **141 of 179**), finally covering this app's untouched auth/legal category. The 4 auth-prefixed routes all redirect cleanly to `/emergency/reception`, consistent with this app's documented no-login-UI design; `/digital-twin` deep-links into `/emergency/tools`, an intentional redirect; the rest render clean, real content. Found and fixed a genuine crash on `/legal/consent-history`: both its error-state and empty-state branches called the shared `EmptyState` component with `action={{label, onClick}}` — a plain object — but `EmptyState` renders `action` directly as `{action}`, and React throws "Objects are not valid as a React child" for a non-ReactNode value; confirmed with a real `react-dom/server` reproduction (not just read the code) that the exact pre-fix shape throws the exact real error, and the exact post-fix shape (a real `<button>` element) renders clean. Also fixed a quieter sibling bug at the same 2 call sites — `message` was passed instead of `EmptyState`'s real `description` prop, so the actual error text was silently dropped even before the crash. This component had zero prior test coverage; added 3 tests (`ConsentHistory.test.tsx`) covering the disabled-capability path, the fetch-failure path, and a real successful render. **Cycle 211**: continued the batch, screenshotting 14 more (`/auth/forgot-password`, `/auth/magic-link`, `/auth/invite`, `/surveillance/nexus`, `/digital-twin-intelligence`, `/customer-portal`, `/knowledge-base`, `/enterprise-readiness`, `/trackmind`, `/trackmind-maturity`, `/enterprise-platform`, `/platform-intelligence`, `/platform-admin`, `/workflow-mining` — running total **155 of 179**). Most fell back cleanly to shared pages, as expected. `/workflow-mining`'s "Journey frequency" chart showed clearly clipped diagonal axis labels ("ED triage calculator to dashboard" → "...ulator to dashboard") — real, reproducible, and traced to the shared `CategoryBarChart` component used by **42 real pages**, so investigated carefully before touching anything. Found a genuinely multi-layered root cause: the chart's own zero `margin.left` gives its rotated (`-20°`, `textAnchor="end"`) labels no room to swing left into; the shared `.dashboard-visual-panel` container (touched once before, Cycle 205) independently clips via `overflow: hidden`; and the app shell's own top-level scroll containers add a further boundary that's unsafe to touch at all. Tried 2 fix directions, verified each empirically: bumping the chart's internal `margin.left` (via the compiled-JS-literal-patch-and-restore technique) shifted the layout as expected but caused category bands to compress enough that adjacent rotated labels started overlapping each other at the values needed to meaningfully help — trading one clipping bug for a different one. Overriding the panel/chart container's `overflow: hidden` fixed 2 of 3 labels cleanly with no visible side effects, but the 3rd (longest-reaching) label still clipped against the untouchable app-shell boundary. Concluded a *safe*, fully-verified fix isn't achievable this cycle given this component's 42-consumer blast radius and the blocked JS-rebuild path's limits on testing across genuinely different label-length data — documented the full root-cause chain and both partial attempts for a dedicated future cycle rather than ship something half-understood, the same discipline as Cycle 208's honestly-unverified `__cta` candidates. No source files changed. **Cycle 212**: closed out the thread — screenshotted the final 24 previously-unaudited routes (`/start`, `/workspace-dependency-graph`, `/tools/catalog`, `/plugins`, `/dependency-map`, `/dependency-graph`, `/data-lineage`, `/self-diagnostics`, `/saas-health`, `/ai-governance`, `/regulatory`, `/artifacts`, `/memory`, `/fleet/predictive-maintenance`, `/fleet/route-optimizer`, `/ai-evaluation`, `/platform-learning-engine`, `/business-brain`, `/asset-packs`, `/expansion-opportunities`, `/integrations-marketplace`, `/integration-readiness`, `/solution-builder`, `/configuration-studio`) — **all 179 of the app's distinct real routes have now been screenshotted and looked at through this lens at least once, across 17 cycles (196-212)**. `/start` re-confirmed the same already-fixed `PlatformEntryHub`/Cycle 154 dist-staleness crash first found on `/workspace` in Cycle 207 — no new investigation, already thoroughly documented. `/dependency-graph` showed another live instance of Cycle 211's already-investigated-and-deliberately-unfixed `CategoryBarChart` label-clipping bug (same shared component, same failure shape) — recorded as a further confirmed instance, not re-investigated. Found and fixed a real, small acronym-casing bug on `/saas-health`: health-check cards read "Frontend Health," "Backend Health"... but "**Api** Health" and "**Ai** Health," missing their correct acronym capitalization. Traced to `saasHealthApi.ts`'s `SAAS_HEALTH_FALLBACK` constant (rendered whenever the real backend is unreachable — unconditional in this static-serve environment, and a real production scenario too), which derived labels via a blind `${id.charAt(0).toUpperCase()}${id.slice(1)} Health` transform over a small, fixed set of 7 ids — correct for 5 ordinary words, wrong for the 2 that are themselves acronyms. The exact same bug *shape* Cycle 200 found (a blind case transform blind to acronyms), fixed the same way: explicit, correctly-cased labels per id rather than a smarter detection heuristic, verified byte-for-byte via a standalone script (confirmed only `api`/`ai` changed, the other 5 identical). Added the component's first-ever test coverage (3 tests). **Route-audit thread now formally closed** — future cycles on this item should look for genuinely new visual-polish issues on already-seen pages, or re-audit as source continues to change, rather than treating "still need to find an unaudited route" as the driving question. **Cycle 213**: closed the 2 candidates left flagged since Cycle 207 (`reception-job-profile-card__cta` on `/profile`, `reception-skill-strip__cta` on `/emergency/reception`). Root-caused Cycle 208's "role switch silently didn't take" observation precisely: `scripts/capture-reception-screenshots.mjs`'s seeded demo profile had no `authMode`/`demoPersona` marker, so `demoPersonaModel.ts`'s `isDemoPersonaUser()` never recognized it, and `hydrateStoredDemoUser()` silently discarded the entire payload — including the requested role — falling back to the app's default role every time. Fixed the script (`authMode: 'open-access'`). But found this alone still couldn't render either CTA: both components were added 2026-07-24, 5 days after `dist/`'s 2026-07-19 build, so the served bundle contains none of this code at all — a harder blocker than ordinary dist staleness (which affects whether a *fix* shows, not whether a *feature* exists). Verified instead with a new technique: a hand-built HTML harness loading the real current source CSS directly via native `@import`/`<link>` (no bundler, sidesteps the blocked Vite/esbuild path) against markup matching each component's real JSX, run through the same Playwright+system-Edge pipeline. Measured **~1.02:1** contrast — the worst instance of this sweep-rule bug found yet — confirmed by the same 2-3 CSS files as Cycles 207-208 not recognizing either class as an already-solid button. Fixed with the same `:not()` exclusion pattern (re-measured live: 4.10:1 — still short of AA). That uncovered a second, genuinely separate pre-existing defect once the masking sweep was excluded: both components' own `--cdl-brand-600` background token itself only reaches 4.10:1 (light) / 2.77:1 (dark) with white text — neither passes AA, and no prior cycle had ever seen the component's *true* unmasked colors to notice. Fixed by switching to a fixed, theme-constant `#075985` (7.56:1 in both themes, verified live) instead of a theme-swapping token. Added 5 new regression assertions, `tsc`/`eslint` both 0 errors. |
| 23 | P2 OPEN (Cycle 201: 1st real bug found+fixed; Cycle 202: blast-radius check, none found) | Real full-stack (live backend + live frontend, unmocked) integration verification — a new technique this program had never applied to the AI chat feature before | Opened per a direct request to verify the AI is actually interactive, not just rendering correctly. **Cycle 201**: drove real compiled backend (`nest build` + `node dist/backend/src/main.js`, served together with the frontend's static `dist/` via `NODE_ENV=production`'s `registerProductionFrontendAssets`) + a real Playwright/system-Edge browser through an actual `POST /api/auth/dev-session` bootstrap and a real Copilot chat send — something no unit test (mocks HTTP), hermetic e2e spec (stubs the network before any request reaches a server), or static-analysis cycle had ever done. Found **every real call to `POST /api/ai/node/conversational` failed with 400 "property stream should not exist,"** unconditionally, since 2026-06-13 (`git log -S`, commit `9434922c`) — the browser client always sends a `stream` key the backend's whitelist-validated DTO never declared. Fixed by adding the field to `UnifiedNodeConversationalDto`; wrote a genuine `class-validator`-based regression test (confirmed red without the fix, green with it) since the DTO's only prior spec was a source-text `toContain` check that could never have caught this. Also confirmed, with real evidence (not assumption), that the entitlement 403s encountered immediately after (`PROFESSIONAL` tier + `pack-required` for `agent-clinical`) are deliberate, documented SaaS monetization gates (`entitlements.config.ts`), not bugs — correctly left untouched. Also confirmed a raw self-registration path hits a separate, expected dead end (no org auto-provisioned) since this app has no exposed login UI post-"auth UI removal" — the real supported local/demo entry point is `dev-session`, which is what surfaced the actual bug. Also re-ran the pre-existing, real `e2e/interactive-ai.spec.mjs` suite (IX12-14) for the first time since 2026-07-18 (predates the Cycle 160 `esbuild` block) — 8/8 clean, confirming the streaming-phases/keyboard-operable/collaboration/command-palette interaction layer is solid in the mode it's designed to run in. This blocked the platform's single most visible AI-interactive feature, completely, for anyone who ever reached a real backend. **Cycle 202**: checked whether the same bug class (a hand-duplicated backend DTO silently drifting from its own frontend caller's request shape) recurs on the app's other "canonical" AI entry point, `POST /api/ai/node` (structured/intent-based) — confirmed clean, both statically (its DTO and the frontend's `CareDroidAIRequest` interface declare the identical 3 fields, no independent duplication) and via a real live request through the real backend (validation passed, reached the same already-known-legitimate entitlement gate, no new 400). Root cause is now precisely scoped: the risk is specifically hand-duplicated request/DTO shapes with no compile-time link across the frontend/backend boundary, not strict validation itself (which is working correctly everywhere checked). Auditing all ~146 real POST/PATCH/PUT endpoints for the same risk is out of scope for one cycle — downgraded to P2 (the acute, platform-wide-blocking instance is fixed; what remains is a lower-urgency pattern to stay alert to, not an active break) rather than claimed as exhaustively swept. |
| 6 | P2 OPEN | Wire Prometheus/Grafana into `docker-compose.app.yml` | Real, consumed stack just missing from the compose file; needs Docker to verify. |
| 7 | P2 OPEN (partially advanced) | Run Playwright/axe-core + Lighthouse profiling in a real browser | System-Edge workaround works; axe-core + Web Vitals run for real; suite coverage grown 8→15 pages. |
| 8 | P2 OPEN | Fix TBT/page-weight finding from perf measurement | Root cause found and fixed (dev JSX runtime shipping in prod), -27.5% JS; row not marked fully closed. |
| 9 | P2 OPEN (sub-phase closed) | Reduce remaining inline-style violations toward zero | Hand-classification phase formally closed after 31 cycles; remaining occurrences found genuinely non-extractable. Fresh count now 110/690 (Cycle 158). |
| 10 | ~~P2~~ DONE (Cycle 176) | Decide backend `strictNullChecks: true` adoption | **Closed 2026-07-24 (Cycle 176) — blast radius reached 0 and the flag is now permanently `true` in `backend/tsconfig.json`, enforced by every future `tsc` run, not just a re-measured snapshot.** Cycle 161: blast radius is now a real, categorized number — was 166. Cycle 163 closed 13 (`test/tool-calling.spec.ts`). Cycle 164 closed 7 more (`test/encryption.e2e-spec.ts`). Cycle 166 closed 5 more (`workspaces.service.ts` — a genuine TypeORM `create()` overload-resolution bug). Cycle 167 closed 8 more (`intent-classifier.service.ts` 4, `device-token.service.ts` 4). Cycle 168 closed 7 more — the Cycle 166 `create()`-overload bug recurred in 3 more files. Cycle 169 closed 11 more — the same bug recurred in 3 memory services, plus a genuinely new TypeORM `FindOptionsWhere`-vs-`IsNull()` typing issue in `integration-hub.service.ts` (verified against a real SQLite DB). Cycle 170 closed 10 more across `administrative-automation-orchestration.lib.ts`+spec, `emergency-os.services.ts` (including a real PHI-adjacent fix in `EmergencyPatientService`'s DB write-through), and `sofa-calculator.service.ts` (a clinical-scoring `.reduce()` type-inference fix, verified against its 54-test dedicated suite). Cycle 171 closed 12 more across `biometric-config.entity.ts`/`biometric.service.ts` (direct-assignment `lockedUntil` widening + a return-type narrowing that reflects the method's real all-or-throw control flow), `two-factor.entity.ts`/`two-factor.service.ts` (direct-assignment `secret`/`backupCodes` widening + an explicit `otpauth_url: true` fixing a latent reliance on an undocumented library default), and `asset-access.service.ts` (a real property-name bug, `recommendedAssetIds` vs. the entity's actual `preferredAssetIds`, that silently zeroed the role-recommended priority signal for every user, plus an optional-chaining-in-comparison narrowing gap). Cycle 172 swept the entire remaining tail of scattered 1–2-error files (10 files) — a mistyped `let inbound = null` in `sentinel.controller.ts`, an `AuditLog.userId` widening for its deliberate HIPAA-anonymization write path, a real webhook-security gap in `subscriptions.controller.ts` (Stripe signature verification had no guard against a missing raw body or webhook secret), plus 6 more single-site fixes across `tenant-context`/`entitlement`/`cache`/`pinecone`/`feature-flag`/`platform-assets.seed` services and 2 test files. Cycle 173 closed the first of the 4 large deferred clusters — `subscriptions.service.ts` (10) + its spec (2) — including a real gap where 2 core billing methods (`createCheckoutSession`, `createCustomerPortalSession`) called Stripe's API with no guard against the client being unconfigured, fixed with a `ServiceUnavailableException` guard and 2 new tests that actually exercise it (the existing spec force-injects a mock client, so this path had never been tested). Cycle 174 closed `smart-handover-v2.service.ts` (5) — all 5 errors traced to one root cause (`buildPatientContext()`'s declared return type didn't capture the guarantee its own implementation always provides) fixed with a single new type rather than 5 defensive coalesces, verified against the one real unmocked test exercising this exact path. Cycle 175 closed `auth.service.ts` (11) — a `createDevSession` re-fetch-after-create step with no guard against the (extremely unlikely, but real) case of the just-created user vanishing before rehydration, fixed with a fail-closed `InternalServerErrorException`; and 4 `User` entity columns (email-verification/password-reset token+expiry) widened to their real DB-nullable types, which surfaced and fixed a genuine parity gap — `verifyEmail`'s expiry check was missing the same null guard its sibling `resetPassword` already had (confirmed behavior-neutral: JS's null→0 coercion in relational comparisons meant the unguarded version already treated a null expiry as expired). Cycle 176 closed the last cluster, `ai.service.ts` (9) — all 9 errors traced to one root cause (`AIQuery.response`, a real DB-nullable column typed non-nullable, breaking `save()`'s overload resolution the same way Cycles 166-172's `create()` bugs did), fixed with one field widening. **0 errors remain — `strictNullChecks: true` is now permanently enforced**, not just measured. |
| 18 | P1 OPEN (Cycle 195: 2nd unguarded write path found+fixed) | Reception patient-creation robustness: no server-side duplicate check (closed), near-zero body validation (open) | Found during the Reception Desk backend audit (2026-07-24): `SmartIntakeService.createFromIntake`/`createVerticalSlice` (backing `POST /emergency/patients`, `/emergency/intake`, `/emergency/intake/vertical-slice`) type their body as a plain interface, not a class-validator DTO, so Nest's global `ValidationPipe` (`whitelist`/`forbidNonWhitelisted`/`transform`, already configured app-wide) enforces nothing on this route today — missing fields silently default (`firstName` → `'Unknown'`) instead of rejecting. **Duplicate-check sub-item closed Cycle 180** — see Cycle log. **Cycle 180 re-scoping: the third sub-item originally listed here ("fire-and-forget DB write") is not actually a standalone bug** — folded into already-tracked item #2. Validation sub-item remains open — building a `whitelist:true`/`forbidNonWhitelisted:true`-safe DTO needed "enumerating every field real callers send today" first (Cycle 180's own words). **Cycle 191 did exactly that** (investigation only, zero code changes): traced all 5 real frontend callers to their actual wire payloads and confirmed real divergence from the canonical `EmergencyPatient`/`EmergencyVitals`/`JourneyEvent` types a naive DTO would be built from — `notes` sent as `{id,type,body,authorId,createdAt,metadata}` by 2 callers vs. canonical `{id,text,authorId,timestamp}`; `flags` sent as `string[]` by most callers but as `{type,reason,severity,detectedAt}` objects by `SmartIntake.tsx`'s vertical-slice path — an inconsistency between callers, not just vs. canonical; `vitals` sent with `bpSystolic`/`bpDiastolic` (no `recordedBy`) vs. canonical `sbp`/`dbp`/required `recordedBy`; `timeline` has 3 different real shapes including one (`SmartIntake.tsx`'s vertical-slice) missing the canonical-required `staffId` entirely; plus 7 extra fields (`phone`, `healthCardNumber`, `healthCard`, `source`, `lastAssessedTime`, `quickSafetyFlags`, `waitingRoomStatus`) sent by real callers with no home in the canonical interface at all. **Cycle 192 fixed `flags`** (`normalizePatientFlags()` at the `createPatient` choke point — a real bug, silently broke `.includes()` checks in 11+ reassessment/EMS-high-risk/escalation call sites for P1/P2 patients). **Cycle 193 fixed `vitals`** (`normalizeVitalsEntry()`/`normalizePatientVitals()` — an even more severe real bug, silently broke sepsis-screening/critical-alert/reassessment BP reads) and confirmed `notes` needs no fix (already read via a `note.text` then `note.body` fallback in 12+ files). **Cycle 194 confirmed `timeline`/`staffId` also needs no fix** — traced every real consumer and found the one UI-facing reader of backend journey events (`patientTimeline.ts`'s `mapJourneyEvent`) already falls back through `staffId`, then `actorStaffId`, then `by`, then an empty string, into an optional display field, and the one unguarded `event.staffId` reader (`emergencyStore.ts`'s `workflowLogFromJourneyEvent`) only ever runs on the frontend store's own natively-created events, never the backend-sourced ones. **Item #18's evidence-map triage is now fully closed** — all 4 field-shape questions resolved (2 real bugs fixed, 2 confirmed already-safe). **Cycle 195 found the fixes had a real gap: `EmergencyPatientService.updatePatient` — the handler behind the live `PATCH /api/patients/:patientId` endpoint — did a raw unguarded merge with none of Cycle 192/193's normalization, meaning the same 2 bugs were fully reachable again through this second write path** (frontend currently doesn't call this endpoint — the one client wrapper for it, `updateEmergencyPatientRecord`, has zero real callers — but the backend API surface itself was live, authenticated, and permission-gated regardless). Fixed by applying the same normalization inside `updatePatient`, checked against all 3 other real callers to confirm no regression. The remaining open scope — the 7 extra unhomed fields, and the actual DTO-building/whitelist-enforcement work itself — remains a separate, dedicated future pass, exactly as Cycle 191 originally scoped it. |
| 17 | ~~P2~~ DONE (Cycle 186) | 9 backend services/controllers had no dedicated unit test spec file | Found while verifying `strictNullChecks` fixes: ~~`DeviceTokenService`~~ (Cy167), ~~`PersonalizationService`~~ (Cy168), ~~`BiometricService`~~/~~`BiometricController`~~/~~`AssetAccessService`~~ (Cy171), ~~`CacheService`~~/~~`SentinelController`~~/~~`PineconeService`~~ (Cy172), and ~~`ERPulseHandoverService`~~ (Cy174, `smart-handover-v2.service.ts`) all had zero dedicated spec file — confirmed via `find`, not assumed. Closed Cy177 — `PineconeService` (20 tests, incl. a fake of the untested "real Pinecone" SDK branch). Closed Cy178 — `BiometricService`/`BiometricController` (25 tests, real SHA-256/`timingSafeEqual` crypto, the full 5-attempt lockout state machine, no leaked `passwordHash`/challenge-token in responses). Closed Cy179 — `CacheService` (29 tests covering `connect()`'s auto-disable branches, a real client connection, graceful fallback on connect failure, and every cache operation both connected and disabled). Closed Cy181 — `DeviceTokenService` (16 tests covering create-vs-update-in-place branching, the soft-delete/hard-delete distinction, OR-matching lookups, multi-user token grouping, and date-cutoff arithmetic under fake timers). Closed Cy183 — `AssetAccessService` (39 tests covering the full `resolveAccess` state-machine cascade in priority order, all 4 admin-only governance signal sources, the pinned>workspace-recommended>role-recommended>recent priority ordering, and orchestration-level tests). Closed Cy184 — `PersonalizationService` (16 tests covering get-or-create preference defaulting, partial-update semantics, allow-list filtering, saved-prompt CRUD, and recent-prompt history). Closed Cy185 — `ERPulseHandoverService` (20 tests covering the full SBAR-summary defaulting/context-override behavior, every individual risk-alert branch in isolation, pending-action composition, education/warning-indicator content, mocked-timing verification, and `fineTunePrompt`/`getPromptFeedback`'s ordering and defensive-copy behavior). **Cycle 186 closed the last instance, `SentinelController`** (424 lines, 17 endpoints, 4 injected services — repeatedly deferred for size across Cy172–185, finally tackled now that it was the only one left and no longer competing with a smaller candidate). 53 tests covering every endpoint: the 2 aggregation endpoints (`health`/`commandSnapshot`, including coordinate/ETA null-handling and the pending-only AI-recommendation filter), 6 one-line delegations, the `unitPositions` limit-clamping edge cases (including the non-obvious `limit='0'` → 50, not 1, because `Number('0')` is falsy), the `ingestCad` clinical-field/patient-block branching, the `upsertInbound` field-coercion defaults, the `alarmAction` 13-way short-verb-to-canonical-status mapping plus its unknown-action rejection path, and the `analytics` endpoint's real dispatch-to-arrival/ETA-error averaging math (independently verified with a standalone Node snippet first). **All 9 candidates on this item are now closed — 0 remain.** |
| — | ~~P1~~ DONE | Backend e2e test tier (`npm run test:e2e`, `test/*.e2e-spec.ts`, 10 files) has never been run by any cycle in this program's history | Discovered Cycle 164 while verifying a strictNullChecks fix: `.e2e-spec.ts` files match a *separate* Jest config (`test/jest-e2e.json`), not the main `npm test`. First-ever real baseline (Cy164): 2 suites failing (42 tests), 1 skipped, 7 passing. **Closed 2026-07-23 (Cycle 165)** — both open findings resolved (below). Current state: **9/9 real suites passing, 202/202 real tests** (32 skipped are an intentional, correctly-implemented Pinecone-credential gate, not a bug — see below). |
| — | ~~P1~~ DONE (Cycle 164) | `test/encryption.e2e-spec.ts` had 2 genuinely failing tests (`reEncryptWithNewKey` throwing "Unsupported state or unable to authenticate data") | Root-caused, not just observed: `EncryptionModule` imports bare `ConfigModule` (no `.forRoot()`), and this test's own `Test.createTestingModule({...})` never calls `ConfigModule.forRoot()` either — unlike production's `app.module.ts`, which does (`isGlobal: true, load: [..., encryptionConfig, ...]`). With no config source registered, `ConfigService.get('encryption')` returns `undefined`, so `EncryptionService` silently fell through to its hardcoded **insecure dev-fallback key** (`'0'.repeat(64)`, guarded by an `isDevelopment` check that defaults `nodeEnv` to `'development'` when unset) instead of the test's own `ENCRYPTION_MASTER_KEY` — two different keys, so cross-key re-encryption failed its GCM auth-tag check exactly as it should. **Confirmed production is unaffected** (`app.module.ts` correctly wires `ConfigModule.forRoot({isGlobal:true})` with the encryption config loaded) — this was purely a test-isolation gap. Fixed by adding a scoped `ConfigModule.forRoot({isGlobal:true, ignoreEnvFile:true, load:[encryptionConfig]})` to the test's own module, matching production's real wiring. **23/23 tests now pass** (was 21/23). |
| — | ~~P1~~ DONE (Cycle 165) | `test/tool-orchestrator-api.e2e-spec.ts` failed to even compile its testing module: `Nest can't resolve dependencies of ToolOrchestratorService... WellsDvtService at index [37] is not available` | Root-caused: `ToolOrchestratorService`'s real constructor takes 2 tool-executor services (`WellsDvtService`, `AbgInterpreterService`) that this test's manually-curated 37-service provider list never included — both were added to the real service by an earlier cycle's clinical-tool-executor work, and this e2e file (never run until Cycle 164) was never updated to match. Added both to the imports and providers list; this immediately surfaced 2 more failures — 2 hardcoded assertions expecting exactly `37` registered tools, now genuinely `39` with the 2 real tools counted. Verified `39` is correct (not assumed): confirmed via the live HTTP response from the actual running test app, not just a source-code read. **40/40 tests now pass** (was a total compile failure, 0/40 could even run). |
| — | ~~P1~~ DONE (Cycle 165) | `test/rag.e2e-spec.ts` shows as skipped (32 tests) | Investigated: `describeIfLiveRagConfigured = process.env.PINECONE_API_KEY ? describe : describe.skip` — an intentional, correctly-implemented conditional gate for a suite that needs a real live Pinecone vector DB credential this sandbox doesn't have. **Not a bug** — closing the open question with a confirmed answer rather than leaving it unexplained. |
| — | ~~P2~~ DONE | Backend `eslint` had 21 real pre-existing errors (all `prettier/prettier`, 12 files — Cycle 161's "7 files" note undercounted; the full list is 2 migration files, `ai.service.ts`, the CIG projection/entity-mapper files + specs, `cig-node.entity.ts`, `cig-nest-projection.ts`, `intent-classifier.service.ts`, `document-chunker.spec.ts`) | Closed 2026-07-23 (Cycle 162) — `npm run lint:fix` (a plain `eslint --fix`) resolved all 21 in one pass; verified formatting-only via a whitespace-stripped diff per file plus manual review of every non-trivial hunk (import-line collapsing, trailing-comma normalization, one `??`-precedence clarifying paren pair that doesn't change evaluation order). Backend `tsc` 0 errors and full suite **245/245 (1922/1922)** re-confirmed unchanged after the fix. |
| 11 | P3 | Re-verify "This pass's headline change" / methodology narrative gap | The pre-rebuild archive's dated cycle-log sections had themselves gone stale (frozen at Cycle 61 and Cycle 106 respectively) while the rest of the document referenced cycles into the 140s–150s. This rebuild does not attempt to backfill that gap — treat the archive as a historical snapshot, and project memory as the authoritative full history. |
| 12 | ~~P1~~ DONE | 6 fresh `tsc` errors found Cycle 159, not caught by Cycle 158's "0 errors" claim | Closed 2026-07-23 (Cycle 159) — `smartIntakeApi.ts`'s `postJson`/`getJson` had an under-typed return inferred as a narrow union across 3 return paths, breaking 5 call sites in `SmartIntake.tsx`/itself; a separate, unrelated latent gap in `AiRouteMetadata.tsx` (missing `routePlan` prop type) also surfaced. Both fixed with explicit type annotations. Root cause of why Cycle 158 didn't catch these not conclusively determined — flagged honestly rather than guessed. |

*Closed items (P0–P3, ~29 rows spanning Cycles 41–112) are preserved in the archive and are not reproduced here — nothing on that list is currently open.*

---

## Methodology

Scores are this review's holistic judgment from cited evidence, not an automated formula. For the full cycle-by-cycle history (157+ cycles), see project memory `project-quality-baseline-cycle0.md`.

Every cycle entry — going forward, logged below as its own dated section rather than prepended into one growing line — should follow the same reusable three-bucket pattern:

- **Re-verified from scratch this pass** — things actually re-run this cycle, each with a fresh count.
- **Carried forward, not re-verified this pass** — things asserted true but not re-checked this cycle.
- **Not evaluated** — things out of scope / environment-blocked, stated honestly rather than silently assumed.

**Recurring verification toolkit:** `tsc --noEmit` (frontend + backend, full scope incl. tests), `eslint` (frontend + backend), `npm audit` (both trees), `npm run check:inline-styles` (the repo's own inline-style CI gate), a repo-wide ARIA bare-boolean/expression violation scan, the full Jest backend suite (`npm test` in `backend/`) and Vitest frontend suite (`npm run test:run`) with exact pass counts each time, a production build check (chunk size/gzip), real compiled-boot smoke tests against a live JWT hitting real endpoints, `madge --circular` (dependency-cycle scans), `ts-prune` (unused-export inventory), a Playwright/axe-core a11y suite routed through the system's installed Edge binary (working around a sandboxed Chromium block), a native-`PerformanceObserver`-based performance suite (`e2e/performance.spec.mjs`), and the repo's various `scripts/audit-*.mjs` tools built up cycle over cycle (routes/nav, full-app crawl, backend-frontend exposure, etc. — see project memory for the full list).

**File-structure rule going forward (the reason this file was rebuilt):** never prepend a new cycle's narrative into an existing line. Each cycle gets its own `###` heading under "Cycle log" below. When this section grows unwieldy again, rotate older entries into `docs/archive/` exactly as done here — don't let it regrow into a single-line file.

---

## Cycle log

### Cycle 158 (2026-07-23) — rebuilt this file from scratch; it had become structurally unreadable

The "Last updated" line at the top of the old `SCORECARD.md` had grown, by prepending each cycle's summary for 157 cycles, to 162,783 characters on one line — unreadable by the Read tool, and almost certainly by any other tooling or editor that assumes reasonable line lengths. A second section ("This pass's headline change") had the same accumulation shape. Rather than keep patching a file with this structural defect, archived the full prior version verbatim to `docs/archive/SCORECARD-archive-2026-07-23-cycles-1-157.md` (nothing lost — git history and project memory both also retain the full record) and rebuilt this file with real section headers instead of run-on lines.

While extracting the old file's content, found it was internally stale in ways not previously flagged: the "This pass's headline change" narrative log stopped at Cycle 106 (2026-07-18) even though the Category detail and Roadmap sections referenced work up to the 140s; the "Methodology" dated log stopped at Cycle 61 (2026-07-15). Neither gap is backfilled here — noted honestly in the roadmap above as a known limitation of the archive, not silently smoothed over.

Rather than just reformat, re-ran the checks that are cheap to re-run today rather than copy stale numbers forward unverified: `tsc --noEmit` (frontend + backend, both 0 errors), `eslint` (frontend, 0 errors), `npm run check:inline-styles` (110/690, improved from the last-recorded 137/690), and `npm audit` on both trees. The audits surfaced a real, previously unflagged regression: frontend dependency vulnerabilities went from a recorded 0 to 2 (1 high), and backend went from a recorded "4 known, unchanged" to 8, including **1 critical** — a `protobufjs` remote-code-execution family of advisories reachable via the already-tracked `@xenova/transformers` migration item, now meaningfully more urgent than its prior WARN framing suggested. Also kicked off full fresh runs of both the frontend Vitest suite and backend Jest suite for an honest, dated pass/fail count (see the Testing gauge above and cycle addendum below) rather than carrying forward the last-recorded 1688/1688 and 11547/11556 figures unverified.

Did not attempt to fix the dependency-audit regressions in this cycle — `npm audit fix --force` on the backend is a breaking downgrade of `@xenova/transformers` and deserves its own verified pass, not a drive-by inside a file-restructuring cycle. Logged as a top roadmap item instead.

Also ran both full test suites fresh rather than trust the last-recorded counts. Backend: 244/245 suites, 1917/1922 tests — 1 file failing (`test/tool-calling.spec.ts`, a stale test double missing `AiGatewayService.attachUnifiedNode`, a method that genuinely exists on the real class). Frontend: 910/927 files, 12020/12045 tests — 25 real named-assertion failures across 17 files (self-referential contract/consistency checks: `backendOrphanAudit`, `wiringAuditConsistency`, `platformCapabilityMatrix`, `segmentInventory`, `pr4aComprehensive`, `pr5Consistency`, and others), plus 43 separate `EnvironmentTeardownError` occurrences matching a previously-documented Vitest teardown-flakiness class. The last-recorded frontend figure in the archive ("832/836 files, remaining 4 confirmed pure teardown noise") had gone stale for an unknown number of cycles — the real current state is a genuine 17-file contract-drift backlog, not 4 harmless artifacts. Checked whether the most recent commit (`70cef8c5`, Collaboration Hub/smart-intake/reception-theme work) explains it via `git show --stat`: it doesn't touch any of the 17 failing files, so the drift predates that commit and wasn't introduced by it. Root-causing all 17 is a dedicated cycle's work, not attempted here — logged as the new top roadmap item.

### Cycle 159 (2026-07-23) — root-caused and fixed all 18 test failures Cycle 158 found, plus 6 fresh tsc errors it missed

Picked up the top item Cycle 158 left open: 17 frontend files (25 tests) + 1 backend file, all genuinely failing. Rather than treat it as one blob, re-ran just the 17 failing files in isolation for full assertion-diff detail, then grouped the 25 failures into distinct root causes before touching anything — a mix of real refactors the tests hadn't caught up to, a few stale hardcoded values, and one real backend wiring gap:

- **Calculator lazy-loading refactor (4 files, 8 tests: `pr4aComprehensive`, `pr5Consistency`, `mentalHealthToolsUxAccessibility`, `wiringAuditConsistency`).** `Calculators.tsx` was refactored to import specialty calculators through a new `lazySpecialtyCalculators.tsx` barrel (dynamic `import()`, so the calculators route doesn't execute every specialty chunk up front) instead of importing `pr4aCalculators.tsx`/`mentalHealthCalculators.tsx` directly — a legitimate bundle-size optimization, not a break (all 6 affected calculators — ASCVD, CKD staging, STOP-BANG, AUDIT-C, PHQ-9, GAD-7 — are still real and wired). Updated the 4 test files to check the real two-hop path.
- **Backend route inventory gap (3 files, 5 tests: `backendControllerRouteScan`, `backendOrphanAudit` ×3, `emergencyOsApi.contract`).** 20 real backend routes existed and worked (AI proposals/models/tools/providers-health from `AIController`, a 3rd parallel bare-`/governance/*` controller, `POST /api/copilot/query`) but were never added to `backendHttpRouteInventory.ts`; a real, already-called `emsHandoff` endpoint was similarly missing from `frontendApiCallsInventory.ts`. Added all 21 entries after verifying each against the actual controller source (`@Get`/`@Post` decorators), not just trusting the test's diff.
- **Renamed file (`buildConfigConsistency`).** Test still pointed at `backend/src/config/database.config.ts`; real file is `database-url.config.ts` (same `buildPostgresOptions` export). One-line path fix.
- **Intentional soft-fail behavior (`smartIntakeApi`).** The concurrent session's `70cef8c5` deliberately changed `SmartIntakeApi.createSession` to soft-fail to a local demo session when offline instead of rejecting ("reception identity check must not hard-fail offline" — real UX intent, confirmed in the source comment). Test still expected the old hard-rejection. Updated to assert the new, correct behavior.
- **Stale status field (`segmentInventory`) + stale test-coverage path (`platformCapabilityMatrix`).** The `assistant` segment's own `recoveryBridge` note said its fragmentation cause (a duplicate `ChatInterface.tsx`) was "Resolved 2026-07-17: ... confirmed dead ... removed" — verified that's still true (file doesn't exist) — but `status` was never flipped from `FRAGMENTED` to `COMPLETE`. Separately, `platformCapabilityMatrix.ts`'s `assistant` row still cited `ChatInterface.nlu.test.tsx` as test coverage; replaced with the real current NLU-launch test (`src/data/nluLaunchPaths.test.ts`).
- **Live page-count drift (`pageDispositionFixture`).** A real `import.meta.glob`-driven scan, not a static fixture — total page-inventory count had drifted from a hardcoded 244/151/93/57/47 to a real, gap-free 242/150/92/58/48 (`getPageDispositionGaps()` already passed with 0 gaps, confirming no orphans, just an unrefreshed snapshot). Updated the 5 expected-total constants.
- **New unmocked dependency (`aiCommandCenterApi`, 2 tests).** `fetchAiCommandCenterSnapshot` gained a 5th real data source (`fetchUnifiedAiNodeModelsHealth` from `./unifiedAiNodeApi`) that the test never mocked, so it hit the real implementation — which itself called the shared, differently-shaped `apiFetchJson` mock, corrupting both the health-label computation and the "cost API was skipped" assertion. Added a proper mock matching the file's existing per-dependency mocking pattern.
- **New response envelope (`careDroidUnifiedAiNode`).** `invokeUnifiedAiStructured` now attaches a `unifiedAiEnvelope` field to structured responses (the same unified-envelope pattern the conversational path was already tested for) — a real, deliberate parity feature, not a regression. Updated the stale `toBe(response)` reference-equality check.
- **Dropped BEM modifier (`clinicalGraphicLayer`).** `patient-card__priority-strip--graphic` was folded into the plain `patient-card__priority-strip` once the graphic acuity-ring treatment became the only treatment — `PatientAcuityRing` itself is still rendered and tested; only the retired modifier class needed removing from the assertion.
- **ARIA role mismatch (`WhiteboardView`).** Sortable column headers are explicit `role="columnheader"` (correct grid semantics), not `role="button"` despite being `<button>` elements under the hood — the test's `getByRole('button', ...)` query was wrong, not the component.
- **Cross-test pollution (`pilotWalkthrough`).** Passed cleanly in isolation from the start; re-running the full original 17-file batch after the other 16 fixes landed made it pass too (335/335), confirming it was never an independent bug — it was order-dependent state leaking from one of the other 16 failing files.
- **Backend `attachUnifiedNode` (P2 item).** `test/tool-calling.spec.ts`'s `AIGatewayService` mock was missing the `attachUnifiedNode` method `chat.service.ts` now calls; added a simple pass-through mock. Backend suite: **245/245 suites, 1922/1922 tests.**

Ran `tsc --noEmit` after all fixes as a sanity check and found **6 fresh errors Cycle 158's "0 errors" claim hadn't caught** — `smartIntakeApi.ts`'s `postJson`/`getJson` had no explicit return type, so TS inferred an overly-narrow union across their 3 distinct return shapes (real payload / demo session / demo-ok), breaking property access at 5 call sites in `SmartIntake.tsx` and the file itself; separately, an unrelated latent gap in `AiRouteMetadata.tsx` (a destructured `routePlan` param with no default, making it structurally required even though the component already handles it being absent) surfaced too. Fixed both with explicit type annotations rather than casts. Could not conclusively determine why Cycle 158's tsc run — against the same `HEAD` commit — didn't report these; noted honestly rather than guessed at (possible incremental-build-cache staleness, not reproduced).

**Final state, all fresh this cycle:** frontend `tsc` 0 errors, backend `tsc` 0 errors, frontend `eslint` 0 errors, backend suite 245/245 (1922/1922), frontend suite **927/927 files, 12045/12045 tests** — the first fully clean full-suite run recorded in this file's history (the 43 `EnvironmentTeardownError` occurrences are unchanged from Cycle 158's count, pre-existing async-teardown noise, not test failures). Did not touch the still-open backend `npm audit` critical/high findings (Cycle 158's other top item) — a dependency-upgrade decision, not a test-drift fix, left for its own cycle.

### Cycle 160 (2026-07-23) — closed the dependency-audit regression: frontend 2→0, backend 8→2 (critical cleared and verified)

Picked up the other top item Cycle 158 left open. Started with the safe path: `npm audit fix` (non-force) on both trees. Frontend went straight to 0 vulnerabilities — `axios`, `protobufjs`, `js-yaml`, `shell-quote`, and `brace-expansion` all had patch/minor fixes within the ranges already declared in `package.json`, confirmed via `tsc`/`eslint` staying clean afterward. Backend dropped from 8 to 5 (clearing `typeorm`, `brace-expansion`, `dd-trace` and dependents), leaving only the `@xenova/transformers` chain — a critical `protobufjs` RCE (via `onnx-proto`'s vendored `protobufjs@6.11.6`) and a high `sharp` finding, both requiring `npm audit fix --force`'s breaking `@xenova/transformers@1.4.2` downgrade per npm's own suggestion.

Investigated before accepting that downgrade. `@xenova/transformers` is genuinely capped at 2.17.2 (checked `npm view ... versions` — no newer release exists, confirming the roadmap's "abandoned package" note) and the maintained successor, `@huggingface/transformers`, exists at 4.2.0 — but a full library migration is a separate, larger body of work, not evaluated end-to-end here. Instead tried the smaller, more surgical fix: an `overrides` entry forcing `protobufjs` to `^7.6.5` repo-wide. A nested override (`{"onnx-proto": {"protobufjs": "..."}}`) silently failed to apply through incremental `npm install` (left the tree in an "invalid" state); a blanket top-level override worked cleanly, deduping `onnx-proto`'s copy onto the same `7.6.5` already used elsewhere in this exact tree via `firebase-admin`/`google-gax` — zero new packages, zero version conflicts.

Did not stop at a clean `npm ls`. Since this override changes which `protobufjs` build actually parses ONNX model definitions at runtime, wrote a smoke script calling the real `pipeline('feature-extraction', 'Xenova/all-mpnet-base-v2')` against the already-cached model weights with real text — got back a genuine 768-dimension float vector, confirming the override doesn't break the actual inference path, not just its dependency tree.

Attempted the same override approach for the remaining `sharp <0.35.0` finding and hit a real environment wall: forcing a new `sharp` version requires npm to write a fresh native `.node` binary, and this dev sandbox's Windows Application Control policy blocked it outright — confirmed two ways (through the wrapped `ERR_DLOPEN_FAILED` error, and via a direct `node -e "require(...)"` `dlopen()` call on the file itself, both citing the same policy). The exact same policy separately re-flagged the frontend's `esbuild` binary after its own `npm audit fix` touched `node_modules`, blocking `npm run test:run` for the rest of this session — confirmed by retrying after a wait, not assumed permanent. Rather than ship a native-dependency version bump nobody had verified, reverted `sharp` to its original, already-trusted `0.32.6` — re-ran the smoke test to confirm the revert didn't undo the real fix (it didn't; the 768-dim vector still came back clean) — and left the `sharp` finding open, flagged for a cycle with CI or Linux-container verification access, where this Windows-only local restriction doesn't apply and production (`FROM node:20-alpine`) was never affected by it either way.

Verified nothing else regressed: backend `tsc` 0 errors and full suite 245/245 (1922/1922) re-run twice across the changes. Frontend `tsc` and `eslint` confirmed clean before the sandbox blocked further frontend runtime verification; frontend's own test suite was not re-run this cycle for that reason, reported honestly rather than assumed passing.

### Cycle 161 (2026-07-23) — turned "strictNullChecks blast radius unknown" into a real number, closed 44 of 166, found a genuine type-predicate bug along the way

Confirmed the Cycle 160 `esbuild` Application Control block was still active before picking a lever (`npm run test:run` still fails with `spawn UNKNOWN`), so scoped this cycle to backend-only work fully verifiable via `tsc` and `jest`, both of which work fine in this sandbox. Picked roadmap item #10 — `strictNullChecks: true` adoption, open since Cycle 38 with "blast radius unknown" as its entire status for dozens of cycles.

Ran `tsc --noEmit -p tsconfig.eslint.json --strictNullChecks true` (the real full-scope config with the flag force-overridden via CLI) to get a first-ever real count: **166 errors.** Rather than treat that as one blob or attempt a blind global flip, grouped by TS error code first: 44 errors (`TS2345`/`TS2339`) contained the literal text `type 'never'` in their message — a strong signal of one specific, well-understood TypeScript inference quirk rather than 44 independent null-safety findings. Confirmed by reading `lib/operational-intelligence/buildSnapshot.ts` (15 of the 44, the single largest concentration): `const anomalies = [];` and `const recommendations = [];` have no type annotation, and TypeScript infers empty `const`-declared arrays as `any[]` when `strictNullChecks` is off (this repo's current setting) but as `never[]` when it's on — breaking every later `.push()` call and property read. Fixed by annotating both against the already-existing `OperationalIntelligenceSnapshotOutput['anomalies'/'recommendations']` contract types from `./types`, instead of inventing new shapes or duplicating the existing ones.

The same pattern repeated, one root cause each, across 9 more files: `analytics.service.ts` (return-type-annotated function, reused the signature), `chat.service.ts` (2 separate spots, plain `string[]`/`{normal,caution,critical: string[]}`), `organization-onboarding.service.ts` and `tenant-provisioning.service.ts` (both accumulate `WorkspacesService['createWorkspace']` results, fixed with `Awaited<ReturnType<...>>`), `asset-recommendation.service.ts`, `platform-assets.service.ts`, `sentinel-tracking.service.ts` (reused an inline return-type annotation already on the method), and two e2e spec files (`audit-logging.e2e-spec.ts` reusing `Awaited<ReturnType<AuditService['log']>>`, `tls-enforcement.e2e-spec.ts` with a plain inline shape). Every fix either reused an existing declared type or matched an idiom already present elsewhere in the same file — no new type vocabulary invented.

Fixing `tenant-provisioning.service.ts`'s one error surfaced 3 more, different ones nearby, all in the same function. Root cause: `resolveWorkspaceDefaults()`'s `.filter()` callback declared a type predicate — `workspace is ReturnType<TenantProvisioningService['normalizeWorkspaceDefault']>` — but `normalizeWorkspaceDefault` can return `null`, so `ReturnType<...>` includes `null`, meaning the predicate never actually told TypeScript that `null` was filtered out, even though the runtime `.filter(Boolean)` genuinely does drop it. A **type-annotation-only bug**, not a runtime one — the array never actually contained nulls, but its static type claimed it might, breaking 3 downstream call sites once `strictNullChecks` was on. The fix, `NonNullable<ReturnType<...>>`, was already used correctly two methods away in the exact same file (line 259) — just not applied consistently. Fixed to match the file's own established pattern.

**Verification:** re-ran `tsc --noEmit -p tsconfig.eslint.json --strictNullChecks true` after all 10 files — the `type 'never'` errors are gone entirely, and the total dropped from 166 to **122** (44 closed, matching the 41 identified plus the 3 bonus tenant-provisioning errors exactly). Confirmed the *real* project config (without the experimental flag) still reports 0 errors on both frontend and backend `tsc`, and ran the full backend jest suite once: **245/245 suites, 1922/1922 tests**, including `test/operational-intelligence-build-snapshot.spec.ts` which directly exercises the most heavily-touched file.

While verifying, ran backend `npm run lint` for the first time this cycle series (prior cycles' "eslint 0 errors across both trees" claim, on inspection, had only ever actually re-run *frontend* lint) and found **21 real, pre-existing `prettier/prettier` errors** across 7 files, none touched by this cycle and none touched by any command in this cycle's `git status` — confirmed via `git status --short` that these files aren't part of any in-progress concurrent-session edit either, just a genuinely stale claim. Corrected the "What's genuinely solid" line rather than leave it standing, and added it as a fresh, separately-scoped roadmap item rather than fix it as a drive-by (the flagged files show real recent activity from 2026-07-21, and a `prettier --fix` sweep deserves its own verified pass).

**Deliberately not attempted:** flipping `backend/tsconfig.json`'s `strictNullChecks` to `true` globally. Of the remaining 122 errors, roughly 40 are genuine "possibly null/undefined" findings (not the mechanical never[] pattern) concentrated in `auth.service.ts` (11), `subscriptions.service.ts` (10), `ai.service.ts` (9), and a different cluster in `chat.service.ts` (6 more) — each needs a real judgment call about that call site's actual nullability invariant, in some of the most sensitive services in the codebase, without the ability this session to cross-check against a live frontend integration test run. Matches this project's established discipline (see the `EmergencyPatientService` DB-read-cutover precedent in project memory) of scoping large, judgment-heavy changes down rather than rushing them inside one cycle.

**Final state, all fresh this cycle:** backend `tsc` (full scope) 0 errors, frontend `tsc` 0 errors, backend jest 245/245 (1922/1922), `strictNullChecks` blast radius 166→122 with a real category breakdown replacing "unknown." Diff: 10 files, all type-annotation-only changes plus the one type-predicate correctness fix — zero behavior changes to any runtime code path, confirmed by the unchanged jest pass count.

### Cycle 162 (2026-07-23) — closed the backend `eslint` finding Cycle 161 caught: 21 pre-existing errors, all formatting-only

User said "let's go the next cycle" with no further specifics. Re-checked environment state first (same discipline as every cycle this series): `git status` clean except the usual harmless `qa/emergency-nav-coverage-report.json` timestamp diff, backend `npm run lint` still showing the 21 errors Cycle 161 found and deliberately left open. Picked it up as this cycle's lever — small, well-scoped, and safe: every one of the 21 errors was `prettier/prettier` (pure formatting), none in files this session had touched, and `git status` confirmed no concurrent-session activity on any of them.

Re-running `npm run lint` first (rather than trusting Cycle 161's tail-truncated summary) corrected an inherited miscount: Cycle 161 said "7 files"; the real list is **12** — 2 migration files (`CreateCigOperationalGraph.ts`, `CreateAiActionProposalAuditEntries.ts`), `ai.service.ts` (5 errors), and 9 more across the CIG projection/entity-mapper module and its specs, `intent-classifier.service.ts`, and `document-chunker.spec.ts`. The undercount was a byproduct of only having read the tail of a long lint-output capture last cycle — corrected in the roadmap rather than silently carried forward.

Ran `npm run lint:fix` (a plain `eslint --fix`, no manual edits). One pass resolved all 21 — `npm run lint` immediately after confirmed 0 errors, 0 warnings. Verified the fix was genuinely formatting-only rather than trusting `prettier/prettier`'s reputation alone: for each of the 12 changed files, stripped all whitespace from both the pre- and post-fix versions and confirmed the non-whitespace character streams matched wherever possible; for the handful where they didn't (trailing-comma additions/removals during multi-line↔single-line reformatting, and one `??`-precedence clarifying paren pair `mode === 'A' ? input.board?.durability ?? 'session' : 'session'` → `mode === 'A' ? (input.board?.durability ?? 'session') : 'session'`), read every hunk by hand to confirm each was a token-preserving reformat with no semantic change — trailing commas in destructuring/array literals are inert, and the added parens don't change `??`'s already-tighter precedence relative to the ternary.

**Verification:** backend `tsc --noEmit -p tsconfig.eslint.json` (full scope) still 0 errors; full backend jest suite re-run clean: **245/245 suites, 1922/1922 tests**, identical to Cycle 161's count — confirming zero behavioral change. Frontend `tsc` re-confirmed 0 errors as a sanity check (none of the 12 files are frontend-reachable, expected to be unaffected and were).

Corrected the "What's genuinely solid" line one more time — it had already been partially corrected in Cycle 161 to note backend `eslint` wasn't actually verified; now it can honestly say both trees are 0 errors, verified fresh this cycle. Closed the roadmap item Cycle 161 opened. Committed and pushed; memory updated.

**Final state, all fresh this cycle:** backend `tsc` 0 errors, backend `eslint` 0 errors (was 21), frontend `tsc` 0 errors, backend jest 245/245 (1922/1922) unchanged. Diff: 12 files, entirely `eslint --fix` output, individually verified formatting-only.

### Cycle 163 (2026-07-23) — closed the largest remaining `strictNullChecks` cluster: 13 errors, one root cause, one test file

User said "let's do the next round then the next cycle" — two cycles back to back. Re-confirmed environment state first (`git status` clean except the usual timestamp diff), then re-ran the `strictNullChecks` baseline check from Cycle 161 to get current numbers rather than trust the stale 122 figure: still 122, unchanged since Cycle 161 (as expected — Cycle 162 only touched formatting). Broke it down by file again and picked the single largest cluster: **13 errors, all in `test/tool-calling.spec.ts`**, ahead of `auth.service.ts` (11) and `subscriptions.service.ts` (10) — deliberately chosen over the larger sensitive-service clusters both for size and for risk: a test file's own assertions are the safety net for any type-level change made to it, unlike touching `auth.service.ts`'s actual runtime logic without a working frontend integration check available this session.

All 13 errors were one pattern, verified by reading the file rather than assumed from the error codes alone: `const sofaTool = tools.find((t) => t.name === 'sofa_calculator');` followed by `expect(sofaTool).toBeDefined();` then direct property access (`sofaTool.description`, etc.) on later lines. Jest's `toBeDefined()` is a **runtime** assertion — it correctly fails the test if the tool is missing, but it does nothing to TypeScript's **static** type, which stays `Tool | undefined` for every line after it. Same shape, 3 tool names (`sofaTool`/`drugTool`/`labTool`), each appearing in 2 separate `it()` blocks — 6 call sites total, 13 error lines.

Fixed by adding a non-null assertion (`!`) directly at each `.find(...)` call site rather than at each property-access line — this is safe specifically because `!` is erased at compile time and changes nothing about runtime behavior: if a tool genuinely goes missing in the future, `expect(sofaTool).toBeDefined()` on the very next line still fails the test exactly as before. Considered restructuring to `if (!sofaTool) throw ...` instead (which would give TypeScript real narrowing without an assertion) but rejected it as unnecessary churn for a test file — the existing `expect().toBeDefined()` pattern is already this codebase's established idiom for asserting a required value exists before using it, matched here rather than replaced.

**Verification:** re-ran the experimental `--strictNullChecks true` flag — `tool-calling.spec.ts` fully clean, total dropped **122 → 109** (all 13 closed, none newly introduced). Ran the file in isolation first (`npx jest test/tool-calling.spec.ts`): **16/16 tests passing**. Then the full backend suite: **245/245 suites, 1922/1922 tests**, identical to Cycle 162's count. Backend `tsc` (real project config, full scope) still 0 errors.

**Final state, all fresh this cycle:** backend `tsc` 0 errors, backend jest 245/245 (1922/1922) unchanged, `strictNullChecks` blast radius 122→109. Diff: 1 file, 6 non-null assertions added, zero behavior change (confirmed by the identical jest pass count and the file's own 16/16 in isolation).

### Cycle 164 (2026-07-23) — closed 7 more `strictNullChecks` errors, then found a whole untested test tier while verifying the fix

Second half of "let's do the next round then the next cycle." Re-measured the `strictNullChecks` baseline rather than trust Cycle 163's carried-forward 109 (confirmed unchanged) and picked the next-largest cluster: **7 errors in `test/encryption.e2e-spec.ts`**, same general shape as Cycle 163 (`repository.findOne()` results and `process.env.ENCRYPTION_MASTER_KEY` used after a runtime-only `toBeDefined()`/setup check, never narrowed for TypeScript). Fixed with the same non-null-assertion idiom — `(await repo.findOne({...}))!` for the 3 `findOne()` sites, `process.env.ENCRYPTION_MASTER_KEY!` for the 2 `Buffer.from()` sites (verified safe: the test's own `beforeAll` sets that env var unconditionally before any test runs).

Tried to verify the fix by running the file directly — `npx jest test/encryption.e2e-spec.ts` reported **0 matches**. Investigated instead of assuming a path-quoting issue: `test/encryption.e2e-spec.ts` doesn't match the main suite's `testRegex` (`.*\.spec\.ts$`, which requires a literal `.spec.ts` — `e2e-spec.ts` has a hyphen before `spec`, not a dot, so it never matches). This file only runs under a **separate** Jest config, `test/jest-e2e.json` (`testRegex: ".e2e-spec.ts$"`), invoked via `npm run test:e2e` — a command no cycle in this program's 163-cycle history had ever run. Every "backend jest 245/245" claim this whole session (and, per `git log`, long before it) was scoped to the main suite only; this entire 10-file e2e tier had been silently absent from every verification pass.

Ran it for the first time to get a real baseline: **2 suites failing (42 tests), 1 skipped (32 tests), 7 passing (160 tests)**, `test/encryption.e2e-spec.ts` among the 2 failures — 21/23 tests, both failures in `reEncryptWithNewKey` throwing `"Unsupported state or unable to authenticate data"` (a GCM authentication-tag mismatch, meaning encrypt and decrypt used different derived keys).

Root-caused rather than patched around it. Isolated the pure crypto logic in a standalone Node script outside NestJS entirely (same `encrypt`/`decryptWithSpecificKey` algorithm, hand-copied) — it round-tripped correctly, proving `EncryptionService`'s actual cryptography is sound. That narrowed the bug to how the test's key and the service's internal key could differ despite both nominally reading the same `ENCRYPTION_MASTER_KEY`. Found it: `EncryptionModule` imports bare `ConfigModule` (no `.forRoot()`), relying on some other module in the tree having already called `.forRoot()` to register a config source — but this test's own `Test.createTestingModule({imports: [TypeOrmModule.forRoot(...), EncryptionModule]})` never does either. With no config registered, `ConfigService.get('encryption')` returns `undefined`, so `EncryptionService`'s constructor falls through its `configuredKey || (isDevelopment ? DEV_FALLBACK_KEY : '')` guard straight to the hardcoded insecure fallback (`'0'.repeat(64)`, gated by an `isDevelopment` check that defaults to `true` whenever `nodeEnv` is unset) — a completely different key from the test's own `ENCRYPTION_MASTER_KEY = '1'.repeat(64)`. Two different keys, so any operation crossing between "what the service actually used" and "what the test thinks it used" fails GCM's authentication check exactly as designed.

**Checked production before treating this as any kind of security incident**: `backend/src/app.module.ts` correctly calls `ConfigModule.forRoot({ isGlobal: true, load: [..., encryptionConfig, ...] })` — the real app always has a config source registered, so `EncryptionService` always reads the genuine `ENCRYPTION_MASTER_KEY` in production. This was purely a test-isolation bug: an e2e test's hand-built NestJS module didn't replicate the app's real bootstrap, so it was silently testing against a fallback key it never intended to exercise. Fixed by adding `ConfigModule.forRoot({ isGlobal: true, ignoreEnvFile: true, load: [encryptionConfig] })` to the test's own module — matching production's real wiring, and setting `ignoreEnvFile: true` so the test stays hermetic regardless of whether a `.env` file happens to exist in a given environment. Re-ran: **23/23 passing** (was 21/23).

Re-ran the full e2e suite once more to get a clean final count: **1 suite still failing (`tool-orchestrator-api.e2e-spec.ts`, a DI resolution error — `WellsDvtService` not registered in that test's manually-built providers list, not yet investigated), 1 still skipped (`rag.e2e-spec.ts`, reason not yet investigated), 8 passing.** Logged both as new, separately-scoped roadmap items rather than chase them in an already-large cycle.

**Verification:** backend `tsc` (full scope) 0 errors; main jest suite unchanged at **245/245 (1922/1922)**; `strictNullChecks` blast radius **109→102**. Diff: 2 files (`tool-calling.spec.ts` untouched this cycle; `encryption.e2e-spec.ts` got both the 7 non-null-assertion fixes and the `ConfigModule.forRoot()` test-isolation fix).

**Final state, all fresh this cycle:** backend `tsc` 0 errors, main backend jest 245/245 (1922/1922) unchanged, e2e suite encryption spec 23/23 (was 21/23), `strictNullChecks` blast radius 109→102. A real e2e baseline now exists where none did before, with 2 of its 3 open questions (the DI error and the skip) explicitly flagged rather than silently left unknown.

### Cycle 165 (2026-07-23) — closed both e2e findings Cycle 164 left open: a real missing-provider bug, and a confirmed non-bug

Picked up the 2 flagged questions from Cycle 164's e2e-tier discovery — `tool-orchestrator-api.e2e-spec.ts`'s DI resolution error, and why `rag.e2e-spec.ts` shows as skipped.

**The DI error, root-caused, not just patched:** the error named `WellsDvtService` as an unresolvable constructor argument. Read `ToolOrchestratorService`'s real constructor and found it takes 39 tool-executor services total; this e2e test's manually-curated `providers: [...]` array — built by hand, one line per service — only listed 37. Checked whether `WellsDvtService` was the only gap by reading the rest of the constructor rather than fixing one and waiting for the next error to surface: it wasn't — `AbgInterpreterService` was also missing, undetected because NestJS's DI resolver only reports the *first* unresolvable dependency it hits. Both services genuinely exist in the real module (`tool-orchestrator.module.ts`) and were added by an earlier cycle's clinical-tool-executor work (see the "Wells DVT + ABG interpreter" entry in Category detail above) — this e2e file, never run until Cycle 164, was simply never updated to match. Added both imports and both to the providers array.

That immediately surfaced 2 *different* failures: two assertions hardcoded to expect exactly `37` registered tools, now genuinely `39` once the 2 real tools were counted. Didn't just trust the test's own error message that the real count was 39 — cross-checked that the live HTTP response (`GET /tools`, `GET /tools/statistics`) actually returned 39 from the running application, not a source-read assumption. Updated both hardcoded values. **Result: 40/40 tests passing** (was a total compile failure — 0 of 40 tests could even run before this cycle).

**The skip, investigated and confirmed as correct, not "fixed":** `rag.e2e-spec.ts` gates its entire suite behind `const describeIfLiveRagConfigured = process.env.PINECONE_API_KEY ? describe : describe.skip` — a deliberate, correctly-implemented conditional for a suite that needs a real live Pinecone vector-DB credential this sandbox doesn't have. Not a bug; closed the open question with a confirmed answer (matching this project's established pattern of not treating every open question as something to force-fix — see the Mongo-blocked integration-suite roadmap item, same shape of honest environment-gated limitation).

**Verification:** re-ran the full e2e suite: **9/9 real suites passing, 202/202 real tests** (32 skipped, all the intentional Pinecone gate). Backend `tsc` (full scope) 0 errors; main jest suite unchanged **245/245 (1922/1922)**.

**Final state, all fresh this cycle:** backend `tsc` 0 errors, main backend jest 245/245 (1922/1922) unchanged, backend e2e tier 9/9 real suites (202/202 real tests, was 7/10 with 2 failing + 1 unexplained skip at the start of Cycle 164). The entire e2e tier — silently absent from every verification pass across 164 prior cycles — went from undiscovered to fully green in exactly 2 cycles once found.

### Cycle 166 (2026-07-23) — closed a real TypeORM overload-resolution bug in workspaces.service.ts; corrected a stale roadmap claim

User said "let's go to the next cycle" with no further specifics. Before picking a lever, re-verified roadmap item #3 ("ported integration test suite... blocked by sandbox Application Control policy on in-memory Mongo") rather than trust a carried-forward claim, since it directly related to the e2e-tier work just closed in Cycles 164–165. Traced it to `tests/integration/emergency-os.test.ts` (run via `vitest.integration.config.ts`, a real Express/Mongoose/Socket.IO integration test using `mongodb-memory-server`) and tried running it — it failed with the exact same `Startup Error: spawn UNKNOWN` from the `esbuild` Application Control block that has affected all Vitest work since Cycle 160, failing before Vitest even loads its config file, let alone reaches Mongo. The roadmap's "blocked by... Mongo" framing was stale and more specific than the real cause — corrected item #3 to name the actual, already-tracked blocker instead. Still blocked either way, so no functional progress, but a more accurate record.

Re-measured the `strictNullChecks` baseline fresh (confirmed unchanged at 102) and picked the next cluster deliberately: **`workspaces.service.ts` (5 errors)**, chosen over `smart-handover-v2.service.ts` (also 5 errors, but its errors are `TS18048` "possibly undefined" on `patient.vitals`/`input.imaging` — clinical data requiring careful per-site nullability judgment, not a mechanical fix) and well ahead of `auth.service.ts`/`subscriptions.service.ts` (11 and 10, the largest and most sensitive remaining clusters).

Read the errors rather than assume they matched Cycles 161–164's "untyped array" or "runtime-check-doesn't-narrow" patterns — they didn't. All 3 were `TS2769`/`TS2345` at `repository.create({...})` call sites, with `create()`'s return type resolving to `Workspace[]` instead of `Workspace`, cascading into 2 more errors downstream. Root-caused properly: `Workspace.organizationId`, `WorkspaceMembership.department`, and `UserWorkspaceState.activeWorkspaceId` are all declared as plain `string` in TypeScript despite being `@Column({ nullable: true })` at the DB level — a real entity-type/DB-nullability mismatch, invisible under `strictNullChecks: false`. The service code explicitly passes `X || null` at each of these 3 fields to satisfy the (DB-correct) nullable columns, but under `strictNullChecks: true`, assigning `null` to a `DeepPartial<Entity>` property typed as non-nullable `string` doesn't type-check — TypeORM's `create()` overload resolution silently fails on the single-entity overload and falls back to matching the array-argument overload's shape instead, producing the confusing `Workspace[]` cascade.

Considered two fixes: widen the entities' TS types to `string | null` (more "correct" long-term, but an exported type change with unknown blast radius across every consumer of these 3 entities), or swap `|| null` to `|| undefined` at just the 3 call sites (narrower, no entity-type change, and — verified, not assumed — behaviorally equivalent at the database level: for a `nullable: true` column with no declared `default`, TypeORM omits `undefined` properties from the generated INSERT/UPDATE entirely, which the DB then fills with its own default of NULL for a nullable column with no other default — the identical end state as explicitly writing `null`). Chose the narrower fix, matching this session's established preference for minimal-blast-radius changes over broader refactors when both close the same error.

One unit test (`workspaces.service.spec.ts`) asserted the mocked `save()` call received `organizationId: null` — a real, expected break, not a false alarm: the test was checking the exact JS value passed to a mocked repository, which legitimately changed from `null` to `undefined`. The test's actual intent (verifying personal workspaces skip organization-entitlement checks) was unaffected — updated the one assertion to match the new-but-equivalent value.

**Verification:** `strictNullChecks` blast radius **102→97**; backend `tsc` (real project config) 0 errors; `workspaces.service.spec.ts` + `workspace-context.service.spec.ts` re-run clean (5/5); full main jest suite unchanged **245/245 (1922/1922)**; full e2e suite re-run clean **9/9 real suites, 202/202 real tests** (`WorkspacesService` is used inside `tenant-provisioning.service.ts`'s e2e-exercised flows, so this was a real cross-check, not a formality).

**Final state, all fresh this cycle:** backend `tsc` 0 errors, main jest 245/245 (1922/1922) unchanged, e2e suite 9/9 (202/202) unchanged, `strictNullChecks` blast radius 102→97. Diff: 2 files (3 call-site fixes + 1 test assertion update), zero production behavior change (verified, not assumed, via the DB-nullable-column equivalence argument above).

### Cycle 167 (2026-07-23) — closed 2 more strictNullChecks clusters, found a real test-coverage gap along the way

User said "keep going" with no further specifics. Re-measured the `strictNullChecks` baseline fresh (confirmed unchanged at 97) and picked the next 2 tractable clusters: **`intent-classifier.service.ts` (4 errors)** and **`device-token.service.ts` (4 errors)** — both non-sensitive relative to the remaining `auth`/`subscriptions`/`ai`/`smart-handover-v2` clusters, both clean single-root-cause shapes on inspection.

**`intent-classifier.service.ts`:** all 4 errors traced to one variable, computed once and reused in 4 separate return statements. `getHighestSeverity()` (a real utility with exactly one caller, confirmed via a repo-wide grep) returns `EmergencySeverity | null`, but the consuming DTO's `emergencySeverity` field is declared as an optional property (`?:`, i.e. `EmergencySeverity | undefined`) — a genuine null/undefined mismatch invisible under `strictNullChecks: false`. Fixed once at the single call site (`?? undefined`) rather than touch the utility function's own `T | null` contract, which is a normal, idiomatic return shape on its own and didn't need changing.

**`device-token.service.ts`:** a different shape — 3 direct property assignments (`deviceToken.deviceModel = dto.deviceModel`, etc.) onto an entity whose fields are declared plain, non-optional `string` despite being real `@Column({nullable: true})` DB columns (the same category of entity/DB-nullability mismatch as Cycle 166's `workspaces.service.ts` finding, but a different fix shape: `repository.create({...})` calls with the same DTO values were unaffected, since `DeepPartial<Entity>` already makes every property optional — only the *direct* instance-property assignments broke, because a fully-typed instance's property must exactly match its declared type). Chose to widen the 3 entity fields to optional (`deviceModel?: string`) rather than coerce at each assignment site — checked the blast radius first (only 2 consumer files, both read-only pass-throughs), confirmed this is a pure type-annotation change with zero behavior difference, and it's the standard, idiomatic TypeORM convention for a nullable column. A 4th, unrelated error in the same file (`tokenMap.get(userId).push(...)` after an `if (!tokenMap.has(userId)) tokenMap.set(userId, [])` two lines above) was a textbook safe non-null-assertion case — the preceding control flow guarantees the key exists, TypeScript just can't see across the `.has()`/`.set()` boundary.

**Found, not fixed:** while verifying the `device-token.service.ts` change, checked for a dedicated test file to run against it and found none exists — `DeviceTokenService` (push-notification device registration) has zero direct unit test coverage. Not this cycle's scope to add (a type-safety cluster fix isn't the place to also write new test coverage from scratch) — flagged as a fresh, honestly-reported roadmap item instead of silently noted and dropped.

**Verification:** `strictNullChecks` blast radius **97→89**; backend `tsc` (real project config) 0 errors; `intent-classifier.service.spec.ts` re-run clean (48/48, alongside its sibling pattern-matching spec); full main jest suite unchanged **245/245 (1922/1922)** (the closest available regression signal for `device-token.service.ts` itself, given no dedicated spec exists).

**Final state, all fresh this cycle:** backend `tsc` 0 errors, main jest 245/245 (1922/1922) unchanged, `strictNullChecks` blast radius 97→89. Diff: 3 files, all type-annotation-only or single-line-utility-boundary changes — zero production behavior change.

### Cycle 168 (2026-07-23) — the Cycle 166 TypeORM overload bug recurs in 3 more files; a second test-coverage gap found

User said "keep reading the scorecard and modifying to score high and to score full" — a continuation of the ongoing directive, no new specifics. Re-measured the `strictNullChecks` baseline fresh (confirmed unchanged at 89) and pulled the full per-file breakdown to look for the next tractable cluster.

Recognized the pattern immediately this time rather than re-deriving it from scratch: `notification.service.ts` (3 errors), `personalization.service.ts` (2), and `user-activity.service.ts` (2) all showed the exact same `TS2769 "No overload matches this call"` + `TS2345 "Type 'X[]' is not assignable to parameter of type 'X'"` shape as Cycle 166's `workspaces.service.ts` finding — TypeORM's `repository.create()` silently picking the wrong overload. Read each site rather than assume identical fixes apply mechanically, since Cycle 167 had already shown the *same category* of entity/DB-nullability mismatch can require *different* fixes depending on how the value reaches the entity:

- **`personalization.service.ts`** (`savePrompt`) and **`user-activity.service.ts`** (`record`, 2 fields: `workspaceId` and `route`): both textbook `create({..., field: dto.field || null, ...})` call-site cases — fixed with `|| undefined`, the Cycle 166 pattern, after confirming both entities' fields are genuinely `@Column({nullable:true})`.
- **`notification.service.ts`**: a mixed case. The `create()` call (`createNotificationRecord`) had one `expiresAt: dto.expiresIn ? new Date(...) : null` — same call-site fix. But a *separate*, unrelated error in the same file — `notification.fcmMessageId = successfulSends[0].messageId` — was a **direct property assignment**, not a `create()` call, matching Cycle 167's `device-token.service.ts` shape instead: `messageId` is genuinely `string | undefined` (an optional field on Firebase's own send-response type, confirmed by reading `firebase.service.ts`), assigned onto an entity field typed as plain `string` despite being a real `nullable: true` column. Checked blast radius (grepped for `.fcmMessageId` — exactly 1 consumer, this same line) before widening the entity field to `fcmMessageId?: string`, matching the Cycle 167 idiom rather than force-fitting the call-site coercion pattern where it didn't actually apply.

**Found, not fixed, while verifying:** ran the relevant spec files for all 3 touched services and discovered `personalization.service.ts` has no dedicated test file either — the second such gap this program has found in as many cycles (`DeviceTokenService` in Cycle 167, `PersonalizationService` now). Rather than treat this as two isolated one-off findings, folded them into a single roadmap item flagging both and suggesting a dedicated future audit for the pattern's true extent, rather than waiting to stumble on a third instance by accident.

**Verification:** `strictNullChecks` blast radius **89→82**; backend `tsc` (real project config) 0 errors; `notification.service.spec.ts`, `user-activity.service.spec.ts`, `user-activity.controller.spec.ts` re-run clean (5/5 — `personalization.service.ts` has no spec to run, the gap noted above); full main jest suite unchanged **245/245 (1922/1922)**; full e2e suite unchanged **9/9 real suites, 202/202 real tests**.

**Final state, all fresh this cycle:** backend `tsc` 0 errors, main jest 245/245 (1922/1922) unchanged, e2e suite 9/9 (202/202) unchanged, `strictNullChecks` blast radius 89→82. Diff: 4 files (3 service call-site/assignment fixes + 1 entity-field widening) — zero production behavior change, verified via the same DB-nullable-column and blast-radius arguments established in Cycles 166–167.

### Cycle 169 (2026-07-23) — the recurring create()-overload bug hits 3 more files; a genuinely new WHERE-clause typing bug caught and verified against a real database

Re-measured the baseline fresh (unchanged at 82). This time the pattern-recognition paid off immediately: `clinical-memory.service.ts`, `long-memory.service.ts`, `short-memory.service.ts` (6 errors total) were the identical Cycle 166/168 `create()`-overload shape — `dto.field || null` at call sites feeding entity fields that are genuinely `@Column({nullable:true})` but plain-typed in TypeScript. Confirmed nullability against each entity before fixing (not assumed from the file-naming similarity alone) and applied the established `|| undefined` fix to all 4 affected fields across the 3 files (`workspaceId` ×3, `patientId`, `source`).

`memory-fabric.service.ts` (2 errors) was a variant of the Cycle 167 boundary-conversion shape: a local `TenantContextLike` type declares `organizationId`/`workspaceId` as `string | null` (correctly modeling that tenant context can be absent), but the call passes them straight into `AuditService.log()`, whose parameters only accept `string | undefined`. Fixed with `?? undefined` at the two call sites, matching the `intent-classifier.service.ts` idiom from Cycle 167.

**`integration-hub.service.ts` (2 errors) was genuinely different, not a pattern match.** Initial investigation found the entity fields (`organizationId`, `workspaceId` on both `IntegrationSourceEntity` and `IntegrationEventRecordEntity`) were *already* correctly typed as `string | null` — ruling out the usual "entity type doesn't match DB nullability" cause. Tried annotating the `where` variables with an explicit `FindOptionsWhere<Entity>` type to see if that would resolve a possible inference issue — it didn't; it made things worse, revealing the real cause: TypeORM's `FindOptionsWhere<T>` mapped type expects `T[K] | FindOperator<T[K]> | undefined` for a query value, and does not accept a bare `null` even for a `string | null`-typed column. This is a genuine, documented TypeORM typing limitation, not a bug in this codebase's own types.

The fix (`IsNull()`, TypeORM's own operator for this exact case — already an established idiom elsewhere in this codebase, confirmed via grep) had a wrinkle: `findOrCreateSource()` builds one `where` object and *reuses it* via spread (`...where`) as the payload for `repository.create()` two lines later. `IsNull()` is a `FindOperator` instance, not a valid entity column value — spreading it into `create()` would have silently written a `FindOperator` object into the database instead of `null`. Restructured to compute `organizationId`/`workspaceId` once as plain `string | null` (used correctly in `create()`, unchanged from before), and separately expressed the WHERE clause using `organizationId ?? IsNull()` — same values, no shared mutable object, both call sites correct for their own context.

**This was the one fix this cycle with genuine behavioral stakes**, unlike every `create()`/assignment fix in Cycles 166–169 so far: in a WHERE clause, `null` means "match rows where this column IS NULL" while `undefined` means "omit this filter entirely" — those are not interchangeable, and getting this wrong could have silently matched idempotency keys across different organizations. Rather than trust the reasoning that `IsNull()` reproduces TypeORM's historical literal-`null` behavior, wrote a throwaway smoke test against a **real, unmocked SQLite database** (the existing spec file only exercises mocked repositories and never tests the null-organization path at all): created a row with `organizationId: null`, confirmed `IsNull()` finds it, confirmed a query for `organizationId: 'org-1'` does *not* match it. Ran clean, deleted the temporary spec file afterward — nothing left in the tree beyond the real fix.

**Verification:** `strictNullChecks` blast radius **82→71**; backend `tsc` (real project config) 0 errors; `memory.services.spec.ts` + `integration-hub.service.spec.ts` re-run clean (13/13); the real-SQLite `IsNull()` smoke test passed before being deleted; full main jest suite unchanged **245/245 (1922/1922)**; full e2e suite unchanged **9/9 real suites, 202/202 real tests**.

**Final state, all fresh this cycle:** backend `tsc` 0 errors, main jest 245/245 (1922/1922) unchanged, e2e suite 9/9 (202/202) unchanged, `strictNullChecks` blast radius 82→71. Diff: 5 files, all verified zero-behavior-change except the `integration-hub.service.ts` WHERE-clause fix, which was verified correct (not just unchanged) against a real database.

### Cycle 170 (2026-07-23) — a real PHI-adjacent fix, a clinical-scoring type quirk, and two pre-existing minimal test fixtures

Re-measured the baseline fresh (unchanged at 71). Picked 4 files spanning a shared `Record<string, string | number | boolean | null>` boundary-conversion pattern, a genuine `EmergencyPatientService` nullability gap, a clinical-scoring type-inference quirk, and 2 pre-existing test fixtures — deliberately not the top 4 sensitive clusters (`auth`/`subscriptions`/`ai`/`smart-handover-v2`), but each of the 4 picked here touches real patient-adjacent code paths and got read carefully rather than pattern-matched blindly.

**`administrative-automation-orchestration.lib.ts` + `emergency-os.services.ts` (4 of 6 errors, shared root cause):** both call `workflowLogService.record({..., metadata: {...}})`, whose `metadata` field is deliberately typed `Record<string, string | number | boolean | null>` — no `undefined` allowed, a sensible constraint for a structured audit-log record meant to serialize predictably to JSON (unlike `undefined`, `null` survives `JSON.stringify` losslessly). Two call sites passed `tenant?.organizationId`/`tenant?.workspaceId` (optional-chained, so `string | undefined`) and two passed `input.unitId`/`input.unitName` the same way. Fixed with `?? null` at all 4 sites — the correct direction for this specific target type, distinct from every earlier cycle's `?? undefined` fixes, because this target type explicitly excludes `undefined` but allows `null`.

**`emergency-os.services.ts`'s 3rd error was the recurring TypeORM `create()`-overload bug, but this time inside `EmergencyPatientService.persistPatientToDatabase` — the real, live write-through path that keeps the actual `patients` database table current from every patient mutation.** Read the full 24-field object literal against the `Patient` entity rather than guess which field was the culprit (the entity itself already correctly uses optional-property syntax throughout, ruling out the usual "entity type wrong" cause) — traced it to the *source* type instead: `EmergencyPatient.triageAssistGeneratedAt` and `.firstContactAt` are both declared `string | null` on the domain type (genuinely can carry an explicit `null`), while the entity's corresponding optional fields only accept `string | undefined`. Fixed with `?? undefined` at the 2 read sites. This is real patient data flowing into the actual database, so verified specifically: `emergency-os.controller.spec.ts` (21 tests, exercises `EmergencyPatientService` directly) re-run clean, not just the full-suite aggregate count.

**`sofa-calculator.service.ts` (3 errors, real clinical scoring math — SOFA, used for ICU mortality/organ-failure severity):** `Object.values(scores).reduce((sum, score) => sum + (score || 0), 0)` — despite an explicit `0` initial value, TypeScript inferred the accumulator as `number | null` rather than `number`, a known `.reduce()` type-inference limitation. Fixed with an explicit generic parameter, `reduce<number>(...)` — a pure type annotation, the arithmetic itself (`sum + (score || 0)`) is byte-for-byte unchanged. Given this is real clinical math, didn't rely on "it's just a type annotation" as the only evidence: ran the file's existing dedicated 54-test suite (`test/sofa-calculator.spec.ts`) and confirmed all 54 still pass, which would have caught any arithmetic drift immediately given the suite's exact-value score assertions.

**`administrative-automation-orchestration.lib.spec.ts` (2 errors, test fixtures):** two hand-written test `Patient` objects, deliberately minimal (missing several genuinely-required fields like `dob`, `arrivalTime`, `complaintCategory`), cast directly `as Patient`. TypeScript's stricter overlap check under `strictNullChecks` correctly flagged that a direct cast looked unsafe given how little the fixture actually has in common with the real interface. Verified this wasn't masking an actual bug (not a typo'd field, genuinely a minimal by-design fixture) by reading the full `Patient` interface and confirming which fields were legitimately absent, then applied the standard `as unknown as Patient` escape hatch — a compile-time-only change with no effect on what the object actually contains at runtime.

**Verification:** `strictNullChecks` blast radius **71→61**; backend `tsc` (real project config) 0 errors; `test/sofa-calculator.spec.ts` (54/54) + `administrative-automation-orchestration.lib.spec.ts` + `emergency-os.controller.spec.ts` (21/21) all re-run clean; full main jest suite unchanged **245/245 (1922/1922)**; full e2e suite unchanged **9/9 real suites, 202/202 real tests**.

**Final state, all fresh this cycle:** backend `tsc` 0 errors, main jest 245/245 (1922/1922) unchanged, e2e suite 9/9 (202/202) unchanged, `strictNullChecks` blast radius 71→61. Diff: 4 files — 2 real nullability fixes (one touching the live patient-persistence path), 1 clinical-math type-inference fix verified against its full dedicated test suite, 2 pre-existing test-fixture casts corrected.

### Cycle 171 (2026-07-23) — a real role-recommendation bug hiding behind a typo'd property name

Re-measured the baseline fresh (unchanged at 61). Picked the next tier of clusters by size (3 errors each) rather than the 4 large deferred ones (`auth`/`subscriptions`/`ai`/`smart-handover-v2`): `biometric.service.ts`+entity, `two-factor.service.ts`+entity, `biometric.controller.ts`, and `asset-access.service.ts` — all auth/entitlement-adjacent, so read carefully rather than pattern-matched.

**`biometric-config.entity.ts` / `biometric.service.ts` (4 errors):** `BiometricConfig.lockedUntil` was declared plain `Date` despite being a real `@Column({nullable: true})` field, and `verifyBiometric()`'s success/reset/admin-reset paths all explicitly assign it `null` (the deliberate "unlock after cooldown or on success" semantics for the failed-attempt lockout counter) — a direct-instance-assignment variant of the recurring pattern from Cycles 167–169, so the fix is entity-field widening (`Date | null`), not a call-site coercion. Separately, `verifyBiometric()`'s declared return type had `accessToken?`/`refreshToken?`/`user?` all optional with a plain `success: boolean`, but reading the full method shows every non-throwing path returns all three together — the method either returns the complete success payload or throws (never both `success: true` and a missing `user`). Narrowed the return type to `{ success: true; accessToken: string; refreshToken: string; user: User }`, which is what the function actually guarantees and resolved 3 downstream "possibly undefined" errors in `biometric.controller.ts` at the root instead of adding redundant guards there.

**`two-factor.entity.ts` / `two-factor.service.ts` (3 errors):** same direct-assignment entity-widening pattern — `secret`/`backupCodes` are DB-nullable but declared plain `string`/`string[]`, and `disable()` deliberately wipes both to `null` (correct security behavior: don't retain a stale TOTP secret or backup codes once 2FA is turned off). Widened both to `| null`. The 3rd error was different: `QRCode.toDataURL(secret.otpauth_url)` — `otpauth_url` is typed optional (`string | undefined`) by `speakeasy`'s own `.d.ts` unless the caller passes `otpauth_url: true`, which selects a different overload with a non-optional return field. Checked the library's actual runtime source (`node_modules/speakeasy/index.js`) to confirm it already defaults to generating the URL (`options.otpauth_url != null ? options.otpauth_url : true`) — so this was always working correctly at runtime, just relying on an undocumented default the types didn't reflect. Fixed by passing `otpauth_url: true` explicitly: makes the intent self-documenting and picks the overload with the accurate return type, rather than papering over it with a null check. Updated the one mocked-call-assertion test that checked the exact `generateSecret()` argument object.

**`asset-access.service.ts` (3 errors, one a real functional bug):** `roleRecommended` — the set used to give a user's role-recommended assets a priority boost when ranking their available tools — was built from `ctx.roleProfile?.recommendedAssetIds`, a property that does not exist on the `RoleProfile` entity. Grepped every other consumer of role-profile asset lists in the codebase (`asset-recommendation.service.ts`, `memory-fabric.service.ts`, `platform-assets.service.ts`) and all of them correctly use the entity's real field, `preferredAssetIds`. This one file had the wrong name, meaning `roleRecommended` was silently always empty and role-based recommendations never contributed to a user's asset ordering — a real, live behavioral bug, not just a type-checking nit. Fixed the property name. The remaining 2 errors were a `strictNullChecks` narrowing gap: `assetIds?.length > 0 ? assetIds : [...]` doesn't narrow `assetIds` to non-undefined in the ternary's true branch (TS only narrows on the bare optional-chain expression, not a comparison built from it) — rewrote as `assetIds && assetIds.length > 0 ? assetIds : [...]`, which narrows correctly.

**Test-coverage gap, found not fixed:** none of `BiometricService`, `BiometricController`, or `AssetAccessService` have a dedicated spec file (confirmed via `grep -rl` across the whole `src` tree, not assumed) — folded into roadmap item #17, now covering 5 known instances of this pattern. Notable: a single test asserting that a role-recommended asset actually gets boosted priority would have caught this cycle's `asset-access.service.ts` bug directly.

**Verification:** `strictNullChecks` blast radius **61→49**; backend `tsc` (real project config) 0 errors; `two-factor.service.spec.ts` re-run (1 mock-assertion test updated for the new `otpauth_url: true` arg, then **16/16**); `platform-assets.service.spec.ts` + `platform-context.service.spec.ts` re-run clean (8/8, cover the `roleProfile` shape the property-name fix touches); full main jest suite unchanged **245/245 (1922/1922)**; full e2e suite unchanged **9/9 real suites, 202/202 real tests**. (One earlier full-suite run in this cycle showed 4 spurious suite failures from a stale/interrupted background process left over from session continuity — re-ran clean from a fresh process before trusting the result, a reminder not to treat a background task's output as final without confirming it actually completed.)

**Final state, all fresh this cycle:** backend `tsc` 0 errors, main jest 245/245 (1922/1922) unchanged, e2e suite 9/9 (202/202) unchanged, `strictNullChecks` blast radius 61→49. Diff: 6 files — 1 real functional bug fixed (`asset-access.service.ts`'s role-recommendation property-name mismatch), 2 security-critical entities widened to their real nullable types, 1 return-type narrowing that resolved 3 downstream errors at the root, 1 library-default clarification, 1 test-mock assertion updated to match.

### Cycle 172 (2026-07-23) — closed the entire scattered tail; caught a real webhook-security gap

Re-measured the baseline fresh (unchanged at 49). Rather than start into the 4 large deferred clusters (`auth`/`subscriptions.service`/`ai`/`smart-handover-v2` — 35 errors combined, still deliberately not rushed), picked off the remaining 10 files carrying 1–2 errors each — small enough to read fully, varied enough to force real investigation over pattern-matching.

**`sentinel.controller.ts` (1 error):** `let inbound = null;` with no type annotation, later reassigned to a real object inside an `if` block. TypeScript infers a bare `let x = null` as declared type `null` (not widened), so the later reassignment was a type error even though the runtime behavior was always correct — a pure type-inference gap, not a behavior bug. Fixed with an explicit type annotation derived from the injected service's real return type (`Awaited<ReturnType<SentinelInboundService['upsertFromCadOrNemsis']>>['validation']` for the nested field) rather than a loose `any`.

**`audit-log.entity.ts` / `audit.service.ts` / `compliance.service.ts` (2 errors, 1 real HIPAA-relevant path):** `AuditLog.userId` was declared plain `string` despite being DB-nullable, and `compliance.service.ts`'s GDPR/HIPAA data-deletion flow deliberately anonymizes audit logs by setting `userId: null` (retaining the log entry for audit-retention requirements while removing the personal reference — the correct compliance pattern, not a bug). Widened the entity to `string | null` after checking every consumer (`audit.controller.ts`, `customer-success.service.ts`, `value-tracking.service.ts` all already null-guard). That widening surfaced one downstream error in `audit.service.ts`'s tamper-detection hash function — traced it and found the function *already* normalizes `userId` via `?? undefined` before hashing, so passing `null` vs `undefined` through the one new call site produces an identical hash either way; fixed there too.

**`subscriptions.controller.ts` (2 errors, a real security gap):** the Stripe webhook handler unconditionally called `this.stripe.webhooks.constructEvent(req.rawBody, signature, webhookSecret)` with no guard against `this.stripe` being unset, `req.rawBody` being missing, or `webhookSecret` being unconfigured — all three are typed optional/nullable for real reasons (Stripe client construction is already skip-if-unconfigured in the sibling `SubscriptionsService`, `rawBody` requires specific Express raw-body middleware that isn't guaranteed, and the webhook secret is separate env config). Before this fix, any of those three being absent would have thrown an uncontrolled runtime `TypeError` from inside the Stripe SDK rather than a clean, informative rejection — for a webhook signature-verification path, failing loudly-but-uncontrolled vs. failing closed-and-clearly matters. Matched the constructor to the existing `SubscriptionsService` pattern (`Stripe | null`, warn-and-disable if unconfigured) and added explicit guards for all three preconditions in the webhook handler.

**6 more single-site fixes, each independently verified against real code, not assumed from shape alone:** `tenant-context.service.ts` (a `string | null` optional-chain result reaching a `string | undefined`-only parameter — `|| undefined` normalization); `entitlement.service.ts` (same boundary-conversion shape into `AutomationAuditTenantContext.workspaceId`); `cache.service.ts` (`RedisClientType` widened to `| null` — the class already sets `this.client = null` on connection failure and every method already null-guards; a direct-instance-assignment case identical in shape to Cycles 167–171's entity findings, just on a plain class field instead of a TypeORM entity); `pinecone.service.ts` (real RAG retrieval logic — `match.score` from Pinecone's SDK is typed optional; changed the minimum-score filter to `(match.score ?? 0) >= minScore`, treating an unscored match as failing the relevance bar rather than silently passing it through, since letting an unscored/irrelevant chunk into the LLM's context would degrade citation quality); `feature-flag.service.ts` (a `.map().filter()` chain losing positional tuple typing because the map callback had no explicit tuple return type, causing the filter callback's destructured value to widen into a 3-way union — fixed with an explicit tuple return annotation); `platform-assets.seed.service.ts` (the by-now-familiar `create()`-overload `|| null` → `|| undefined` fix, same root cause as Cycles 166–169).

**2 test-fixture fixes:** `asset-registry.service.spec.ts` (`normalizeRegistryType()` can return `null` for an unrecognized type, but this test iterates real seed data specifically to prove every seed asset has valid registry metadata — cross-checked all 17 `PlatformAssetType` enum values against the function's `TYPE_ALIASES` map and confirmed every one resolves, so added `expect(registryType).not.toBeNull()` before a justified non-null assertion, preserving the test's power to catch a real future gap rather than loosening it away); `drug-checker.spec.ts` (same `toBeDefined()`-doesn't-narrow shape as Cycle 163, plus added a missing `toBeDefined()` check this specific test lacked, matching its sibling tests in the same block, before the justified `!` assertion).

**Test-coverage gap, found not fixed:** `CacheService`, `SentinelController`, and `PineconeService` all have zero dedicated test coverage — folded into roadmap item #17, now 8 known instances. Checked whether `PineconeService`'s fix was covered indirectly by `retrieval.service.spec.ts` (the one file that references it) and found it fully mocks the class (`{ query: vectorQuery } as unknown as PineconeService`) — the real fix is verified only by `tsc` and manual reading, stated honestly rather than credited to a test run that doesn't actually exercise it.

**Verification:** `strictNullChecks` blast radius **49→37**; backend `tsc` (real project config) 0 errors; 9 targeted spec suites re-run clean (`audit.service`, `compliance.service`, `tenant-context.service`, `entitlement.service`, `feature-flag.service`, `platform-assets.seed.service`, `subscriptions.controller` — 73/73 tests) plus the 2 directly-edited test files (`asset-registry.service.spec.ts`, `drug-checker.spec.ts` — 50/50 tests); full main jest suite unchanged **245/245 (1922/1922)**; full e2e suite unchanged **9/9 real suites, 202/202 real tests**.

**Final state, all fresh this cycle:** backend `tsc` 0 errors, main jest 245/245 (1922/1922) unchanged, e2e suite 9/9 (202/202) unchanged, `strictNullChecks` blast radius 49→37. Diff: 13 files — 1 real webhook-security gap closed, 1 HIPAA-anonymization path's entity type corrected, 1 pure type-inference gap fixed, 6 single-site boundary/widening fixes, 2 test-fixture fixes that preserved (didn't loosen) their tests' real assertion power. Everything remaining in the roadmap item is now exactly the 4 large, previously-deferred sensitive clusters plus their 1 dependent spec file.

### Cycle 173 (2026-07-23) — the first of the 4 deferred clusters, and its own webhook-adjacent security gap

Re-measured the baseline fresh (unchanged at 37). With the scattered tail fully closed as of Cycle 172, picked the smallest of the 4 remaining large clusters — `subscriptions.service.ts` (10 errors) + its spec file (2) — deliberately reading the entire 613-line file end to end before touching anything, given its billing-critical nature and the real security gap Cycle 172 had just found one file over in the sibling controller.

**`this.stripe` unconditionally called in 2 core billing methods (3 errors, a real gap):** `createCheckoutSession` and `createCustomerPortalSession` both called `this.stripe.customers.create(...)`/`.checkout.sessions.create(...)`/`.billingPortal.sessions.create(...)` directly, with zero guard against `this.stripe` being `null` — which it legitimately can be, since the constructor already gracefully disables Stripe (logs a warning, sets `this.stripe = null`) when `stripe.secretKey` isn't configured, exactly mirroring the pattern Cycle 172 added to the sibling controller. Without a guard, any environment lacking Stripe config would hit an uncontrolled runtime `TypeError` on the very first checkout or billing-portal request, rather than a clear, typed rejection. Added an early `if (!this.stripe) throw new ServiceUnavailableException(...)` guard in both methods, then captured a local `const stripe = this.stripe;` right after — necessary because TypeScript's narrowing of a `this.field` doesn't survive an intervening `await` to another method, so without the local capture the same "possibly null" error would resurface at the actual call sites a few lines later.

**`getPriceIdForTier` return-type mismatch (5 errors):** the private helper's declared return type is `string | null`, but every branch returned `this.configService.get<string>(...)` directly, which is typed `string | undefined` — the opposite-direction mismatch from the usual pattern in this sub-project (here the *declared signature* was the stricter side, not the assignment). Fixed with `?? null` (or `|| ... || null` for the 2 branches with fallback chains) at each of the 5 return sites, matching the existing, already-correct signature rather than loosening it.

**`Subscription.trialStart`/`.trialEnd` (2 errors):** both declared plain `Date` despite being real DB-nullable columns, and `convertTrial()` deliberately sets both to `null` when a trial converts to a paid plan (correct semantics — the trial window is over). The one other consumer, `subscription-lifecycle.engine.ts`, already treats both fields as nullable (`subscription?.trialEnd ? ... : null`), confirming the widening is safe. Widened to `Date | null`.

**Spec file (2 errors):** `subscriptions.service.spec.ts` builds a `Stripe.Checkout.Session` test fixture with a guaranteed-present `metadata` object, then reads `session.metadata.userId`/`.tier` — the same `toBeDefined()`-adjacent shape as prior cycles' test fixes, resolved with a justified non-null assertion (`session.metadata!.userId`) rather than loosening the type.

**Didn't stop at the type-checker for the new `ServiceUnavailableException` guards:** confirmed the existing spec file mocks Stripe by force-injecting a mock client onto `(service as any).stripe` in a `beforeEach`, meaning the constructor's real "Stripe unconfigured" branch — and therefore the new guards — were never exercised by any existing test. Added 2 new tests (`(service as any).stripe = null` then assert the exception) directly targeting the new code path rather than letting it ride on `tsc` alone.

**Verification:** `strictNullChecks` blast radius **37→25**; backend `tsc` (real project config) 0 errors; `subscriptions.service.spec.ts` re-run with the 2 new tests included (**19/19**); `subscriptions.controller.spec.ts` + `subscription-lifecycle.engine.spec.ts` + `usage-metering.service.spec.ts` re-run clean (**36/36** combined before the 2 additions); full main jest suite **245/245 (1924/1924** — up 2 from the new tests, otherwise unchanged**)**; full e2e suite unchanged **9/9 real suites, 202/202 real tests**.

**Final state, all fresh this cycle:** backend `tsc` 0 errors, main jest 245/245 (1924/1924, +2 new tests), e2e suite 9/9 (202/202) unchanged, `strictNullChecks` blast radius 37→25. Diff: 3 files — 1 real production gap closed with both a guard and new test coverage for it (not just a type fix), 1 return-type/assignment direction mismatch, 1 entity widening verified safe against its one other consumer, 1 test-fixture assertion. 3 clusters remain: `auth.service.ts` (11), `ai.service.ts` (9), `smart-handover-v2.service.ts` (5, clinical vitals/imaging data).

### Cycle 174 (2026-07-23) — one root-cause fix closes an entire clinical-data cluster; a concurrent session sharing the repo

Re-measured the baseline fresh (unchanged at 25) and found a much larger concurrent diff than usual in `git status` — a parallel session had substantial in-progress work across `emergency-os.controller.ts`, `emergency-os.services.ts`, `ocr-intake.service.ts`+spec+types (475 insertions across 5 backend files), plus many frontend reception-module files. Before picking a lever, cross-referenced the 3 remaining `strictNullChecks` clusters (`auth.service.ts`, `ai.service.ts`, `smart-handover-v2.service.ts`) against that diff and confirmed zero file overlap — safe to proceed without any risk of colliding with in-progress work, per this program's standing rule to never touch files a concurrent session is actively editing.

Picked `smart-handover-v2.service.ts` (5 errors, the smallest remaining cluster) — real SBAR (Situation-Background-Assessment-Recommendation) clinical handover summary generation, including risk-alert logic for hypoxia, tachycardia, and sepsis screening. Read the entire 275-line file before touching anything, given the clinical content.

**All 5 errors traced to a single root cause, not 5 independent issues:** `buildPatientContext()` — a private method that normalizes a caller-supplied `Partial<ERPulsePatientContext>` into a complete context, filling in realistic defaults for any missing field (default vitals, default imaging, default consult note, etc.) — was declared to return the general `ERPulsePatientContext` interface, where `vitals`/`imaging`/`consults`/`labs`/`pmh`/`medications`/`allergies` are all optional. But reading the implementation shows every one of those fields is *always* populated by the function's own logic (`context.vitals?.length ? context.vitals : [{...default}]`, same pattern for the rest) — the optionality was real for the *input*, not the *output*. Every downstream consumer (`generateStructuredSummary`, `deriveRiskAlerts`, the two `patient.vitals[0]` call sites in `generateHandoverSummary`) was working with data that was actually always present, but typed as if it might not be.

Fixed by adding `HydratedPatientContext extends ERPulsePatientContext`, overriding the 7 array-typed fields to non-optional, and changing `buildPatientContext`'s return type to it. `StructuredSummaryInput` (already `extends ERPulsePatientContext` for the separate `latestVital` addition) now extends `HydratedPatientContext` instead — a one-line change that propagated the guarantee through every function that consumes it, closing all 5 errors without touching a single call site's logic. This is the more correct fix for a function whose entire purpose is turning "maybe present" into "definitely present" — coalescing at each of the 5 read sites instead would have left the function's real contract undocumented in its type signature.

**Didn't stop at `tsc` for a fix touching clinical-summary generation:** found a genuinely relevant existing test — `emergency-os.research.controller.spec.ts`'s "generates ER-Pulse SBAR handover summaries" instantiates the real, unmocked `ERPulseHandoverService` (not a test double) and calls `generateHandoverSummary` with a patient object that omits `vitals`/`imaging`/`consults` entirely, exercising exactly the defaulting path this refactor touches, then asserts on the resulting `summary.situation` string (which embeds `formatKeyVitals(patient.vitals[0])`, one of the two direct fix sites) and `summary.abnormalLabs`. Ran it specifically rather than relying on the full-suite aggregate alone.

**Found, not fixed:** `ERPulseHandoverService` has no dedicated spec file of its own — folded into roadmap item #17 (now 9 known instances) — but flagged the nuance honestly: unlike most of that list, this one has real (if thin) unmocked coverage via the sibling controller spec, which is exactly what caught this cycle's fix was behavior-neutral.

**Verification:** `strictNullChecks` blast radius **25→20**; backend `tsc` (real project config) 0 errors; `emergency-os.research.controller.spec.ts` re-run clean (**6/6**, including the exact handover-summary test); `service-registry.spec.ts` re-run clean (**3/3**); full main jest suite **245/245 (1926/1926** — the +2 over Cycle 173's 1924 traces to the concurrent session's own new tests in `ocr-intake.service.spec.ts`, confirmed via the file's own diff, not this cycle's work**)**; full e2e suite unchanged **9/9 real suites, 202/202 real tests**.

**Final state, all fresh this cycle:** backend `tsc` 0 errors, main jest 245/245 (1926/1926) — 2 more than Cycle 173 from the concurrent session's own additions, not this cycle's — e2e suite 9/9 (202/202) unchanged, `strictNullChecks` blast radius 25→20. Diff: 1 file, 1 root-cause type fix closing all 5 errors at once. 2 clusters remain: `auth.service.ts` (11), `ai.service.ts` (9).

### Cycle 175 (2026-07-24) — closed `auth.service.ts`; widening a nullable field surfaced a real guard-parity gap in email verification

User said "let's now read the scorecard and keep upgrading up to full mark" — re-read the full current file rather than trust the summarized state, then re-ran the `strictNullChecks` baseline fresh: unchanged at 20, split exactly as recorded (`auth.service.ts` 11, `ai.service.ts` 9). `git status` was clean with no concurrent-session activity this time — no cross-referencing needed before starting, confirmed rather than assumed.

Picked the larger cluster, `auth.service.ts` (11 errors), the authentication service — read the surrounding code for every error line before changing anything, given what the file is.

**Two distinct root causes, not one:**

1. **7 errors in `createDevSession`** (lines 360–377): a dev/demo-only session bootstrap path. When no dev user exists yet, it creates one, saves a profile and subscription for it, then re-fetches the user via `findOne` purely to rehydrate the `profile`/`subscription`/`twoFactor` relations the in-memory object doesn't have. `findOne` always returns `T | null`, and nothing guarded the 7 reads/writes after it (`user.lastLoginAt = ...`, `this.userRepository.save(user)`, `this.auditService.log({ userId: user.id, ... })`, `this.sanitizeUser(user)`, etc.) against the null case. In practice this re-fetch essentially always succeeds (it's looking up a row this same request just inserted), but "essentially always" is exactly the gap `strictNullChecks` exists to catch — added `if (!user) throw new InternalServerErrorException(...)` immediately after the re-fetch, matching this program's established fail-closed idiom (same shape as Cycles 172–173's Stripe/webhook guards) rather than assuming the happy path.

2. **4 errors from real DB-nullable `User` entity columns typed as non-nullable TS fields** (`emailVerificationToken`/`Expiry`, `passwordResetToken`/`Expiry`) — the same entity/DB mismatch pattern that's recurred since Cycle 166, now in the `users` module. Checked for other call sites before widening (`grep` across `src/`): only `auth.service.ts` and migration files reference these 4 columns, so the widen is fully self-contained. Widened all 4 to `| null`.

**Widening surfaced a new error the original 11 didn't include**, worth explaining rather than treating as a surprise: `verifyEmail`'s expiry check (`if (new Date() > user.emailVerificationExpiry)`) had no null guard, and widening the field to `| null` made that a real `strictNullChecks` violation. Its sibling method, `resetPassword`, already guards the equivalent field (`if (!user || !user.passwordResetExpiry)`) — `verifyEmail` was simply missing the matching guard. Added `if (!user.emailVerificationExpiry || new Date() > user.emailVerificationExpiry)` to match. **Confirmed this is behavior-neutral, not just type-safe:** JavaScript's relational operators coerce `null` to `0` (`ToNumber(null) === 0`), so `new Date() > null` was already evaluating to `true` at runtime before this fix — a null expiry was already being treated as "expired" even in the old, unguarded code. The fix makes an already-correct runtime behavior explicit and type-checked, it doesn't change what the endpoint does.

**Test coverage check, not just `tsc`:** `auth.service.spec.ts` has a `createDevSession` describe block, but it only covers the two `ForbiddenException` rejection paths, not the success/re-fetch path this cycle touched; `verifyEmail` and `resetPassword` have no dedicated tests at all in that file or in `test/auth.e2e-spec.ts`. Considered adding tests for the new guard (matching Cycle 173's precedent, which added tests specifically because the gap it found was live and reachable), but concluded the two precedents aren't the same shape: Cycle 172/173's Stripe gaps were live in any misconfigured or unconfigured environment — any real deployment without Stripe set up would hit that path. This cycle's `createDevSession` guard only fires if a row this same request just inserted fails to be found moments later, which needs a genuine race or data-corruption condition to trigger, not a realistic environment state. Flagging the thin coverage honestly here rather than either overstating it as a live gap or silently leaving it unmentioned.

**Verification:** `strictNullChecks` blast radius **20→9** (only `ai.service.ts` remains); backend `tsc` (real project config, full scope) 0 errors; backend `eslint` on both changed files 0 errors; auth module suite re-run in isolation, **4 suites / 18 tests passing**; full main jest suite **245/245 (1926/1926)**, unchanged from Cycle 174; full e2e tier **9/9 real suites, 202/202 real tests**, unchanged; `test/auth.e2e-spec.ts` re-run specifically, **7/7**. Confirmed via `grep` that no file outside `auth.service.ts` and `user.entity.ts` references the 4 widened fields, so the widen carries zero blast radius beyond the file already fixed.

**Final state, all fresh this cycle:** backend `tsc` 0 errors, backend `eslint` 0 errors, main jest 245/245 (1926/1926) unchanged, e2e 9/9 (202/202) unchanged, `strictNullChecks` blast radius 20→9. Diff: 2 files (`auth.service.ts`, `user.entity.ts`) — 1 fail-closed guard, 4 entity-field widenings, 1 guard-parity fix. **1 cluster remains: `ai.service.ts` (9)** — the natural next-cycle target.

### Cycle 176 (2026-07-24) — MILESTONE: `strictNullChecks` blast radius reaches 0; the flag is now permanently on

User said "let's go the next cycle." Re-checked `git status` (clean, no concurrent-session activity at the start) and re-ran the `strictNullChecks` baseline fresh: unchanged at 9, all in `ai.service.ts` as expected — the last remaining cluster from the roadmap item opened at Cycle 38 and quantified at Cycle 161.

**All 9 errors traced to one root cause**, read in full before touching anything given this is the AI orchestration path: `logQuery()`'s `this.aiQueryRepository.save({...})` call includes `response: data.response === null ? null : this.redactedAiQueryValue(...)` — a ternary whose true branch is the literal `null`. `AIQuery.response` is declared plain `string` despite being a real `@Column({ type: 'text', nullable: true })` DB column, so this single field mismatch was enough to make TypeScript's overload resolution silently pick `save()`'s *array* overload instead of the single-entity one — the identical failure shape as Cycles 166-172's recurring `create()`-overload bug, just on `save()` this time. That misresolution is what produced the other 8 errors: every downstream read of `saved.organizationId`/`.workspaceId`/`.userId`/`.assetId`/`.model`/`.totalTokens`/`.cost` (used seconds later to record AI usage against a subscription) failed because `saved` was typed `DeepPartial<AIQuery>[]`, not `AIQuery`.

Before fixing, checked whether the codebase already treats `.response` as nullable at the value level (it would confirm the field itself is the bug, not the ternary) — found `postAiChiefRecommendation`'s `(query.response || '').slice(0, 800)`, a defensive read that already assumes `.response` can be falsy/null. Widened the one field to `string | null`, matching its real DB nullability and the existing defensive-read pattern. This closed all 9 errors at once — confirmed the array-overload misresolution was exactly the mechanism, not a coincidence.

**With the count genuinely at 0 rather than just small, treated this as the actual conclusion of the roadmap item** ("Decide backend `strictNullChecks: true` adoption," open since Cycle 38) rather than another incremental reduction: flipped `backend/tsconfig.json`'s `"strictNullChecks": false` to `true` for real. Verified carefully before committing to it, since this is a repo-wide behavior change, not a single-file fix:
- `tsc --noEmit -p tsconfig.json` (production scope, tests excluded) — **0 errors**.
- `tsc --noEmit -p tsconfig.eslint.json` (full scope incl. tests, the same config this whole 16-cycle sub-project measured against) — **0 errors**.
- Checked whether `ts-jest` (which compiles test files at run time via an inline `tsconfig` override in `package.json`) would raise new errors under the flipped flag, since `tsc --noEmit` and `ts-jest`'s per-file transform aren't guaranteed to behave identically — ran the `ai`+`auth` module suites first as a fast smoke check (15/15 suites, 74/74 tests), then the full suite fresh.

**Also caught, while re-running a full (not per-file) `eslint` sweep for the first time in several cycles rather than just linting this cycle's own changed files:** 10 pre-existing `prettier/prettier` errors across 3 files (`feature-flag.service.ts`, `sentinel.controller.ts`, `subscriptions.service.spec.ts`) that trace back, via `git log`, to this program's own Cycles 172 and 173 — introduced by those cycles' own edits and never caught since, because every cycle since has only lint-checked the specific files it touched that cycle, not the whole tree. Fixed with `eslint --fix`; manually reviewed both non-whitespace-identical diffs (the other 1 was byte-identical after stripping whitespace) and confirmed each was a pure line-wrap reformat — same function calls, same arguments, same assertions, zero semantic change.

**Verification, run twice — before and after the lint fix, and once more after everything settled:** `strictNullChecks` blast radius **9→0**; `tsc --noEmit -p tsconfig.json` 0 errors; `tsc --noEmit -p tsconfig.eslint.json` 0 errors; backend `eslint` (full `src` tree) **0 errors**; full main jest suite **245/245 (1926/1926)**, unchanged, confirmed clean on a final run after the entity widening, flag flip, and lint fix were all settled together (not mid-run against a partially-changed tree); full e2e tier **9/9 real suites, 202/202 real tests**, unchanged, re-run twice (once right after the flag flip, once at the end). A concurrent parallel session resumed activity partway through this cycle (`ocr-intake.service.ts`+spec, `asset-access.service.ts`) — confirmed zero file overlap with this cycle's 5 files before staging.

**Final state, all fresh this cycle:** `strictNullChecks` blast radius **0** (was 166 at Cycle 161, closed across 16 cycles and 51 files) — **and now permanently enforced**, not a re-measured snapshot. Backend `tsc` 0 errors under both configs, backend `eslint` 0 errors full-tree, main jest 245/245 (1926/1926) unchanged, e2e 9/9 (202/202) unchanged. Diff: 6 files (`ai-query.entity.ts`, `tsconfig.json`, plus 3 lint-only files this cycle also cleaned up). Roadmap item #10 marked DONE.

### Cycle 177 (2026-07-24) — first real test coverage for PineconeService, the untested RAG retrieval path

With the `strictNullChecks` sub-project closed, picked the next well-scoped, environment-unblocked lever: roadmap item #17, the 9 backend services/controllers flagged since Cycle 167 as having zero dedicated unit test coverage. Re-confirmed the list was still accurate via `find` (all 9 still lack a sibling `.spec.ts`) rather than trust the roadmap note unverified, then picked the single entry explicitly flagged across 4 prior cycles as the most concerning: `PineconeService`, the real vector-similarity retrieval path backing RAG — Cycle 172's own minimum-score-filter fix there had genuinely zero test coverage exercising the real code, verified only by `tsc` and manual reading at the time.

Read the full 510-line file before writing anything. It's more testable than its "external SDK wrapper" framing suggests: it has a fully real, dependency-free in-memory fallback backend (`InMemoryVectorStore`, real cosine-similarity math, real on-disk JSON persistence) that activates automatically whenever Pinecone/pgvector aren't configured, plus deterministic private helpers (`buildFilter`, `flattenMetadata`, `mapToVectorMatch`) that don't need a live index at all.

**Wrote 20 real tests** (`pinecone.service.spec.ts`), split across what they actually exercise rather than mocking the whole class into a shell:

- **Backend auto-selection** (5 tests) — falls back to in-memory when unconfigured; selects the real Pinecone SDK when an API key is present; an explicit `RAG_VECTOR_BACKEND=memory` override wins even with a key configured; falls back to in-memory if the real Pinecone init throws (index verification failure); `initialize()` is idempotent.
- **In-memory backend** (4 tests) — a genuine upsert→query→delete round trip against a real temp-file-backed store (no mocks at all); `minScore` correctly excludes a dissimilar vector using real cosine-similarity math; `deleteByFilter` removes only matching records; `healthCheck`/`getStats` reflect real state.
- **Real Pinecone SDK backend** (11 tests) — this whole branch had never been exercised by any test before, since a live Pinecone API key isn't available in this sandbox. Replaced `@pinecone-database/pinecone` with a lightweight, controllable fake (`jest.mock`, matching the codebase's own `pgvector.store.spec.ts` harness style) exposing exactly the surface `PineconeService` calls — `index()`, `query()`, `upsert()`, `deleteMany()`, `describeIndexStats()`, `namespace()`. This let the actual production code run for real: the Cycle 172 minScore fix (confirmed a missing `score` field is treated as `0`, not skipped or crashed on); `buildFilter()`'s array→`$in`/primitive→`$eq` conversion, observed via the request actually sent to the mocked SDK rather than asserted on the private method directly; `upsertBatch()`'s 100-record batching (150 records → exactly 2 calls, 100 + 50); `flattenMetadata()`/`mapToVectorMatch()`'s round trip, including the JSON-stringify/parse of nested `metadata.metadata`; namespace routing; and 3 distinct error paths (a query failure wrapped in a clear message rather than leaking the raw SDK error, a health-check failure returning `false` rather than throwing, and empty-array upsert/delete being genuine no-ops).

**The first real run against real logic (not a mock echoing back what I fed it) caught a genuine wrong assumption in my own test**, which is exactly the kind of signal that confirms these tests exercise real behavior: I'd assumed `flattenMetadata()` strips the chunk's `text` field before writing to Pinecone, but reading `upsertBatch()`'s actual code shows `text` is deliberately included in the metadata blob on write (Pinecone only supports metadata storage, so there's no other place to put it) — it's `mapToVectorMatch()`, on the *read* side, that strips `text` back out into its own field. Fixed the test's assertion to match the real behavior rather than weakening it to pass; a mock-heavy version of this test would never have caught the mistake, since a mock only ever confirms what the test author already believes.

**Verification:** full RAG module suite **12/12 suites, 79/79 tests** (was 11/11 suites, 59/59 tests before this cycle's new file); full backend suite **246/246 suites, 1946/1946 tests** (was 245/245, 1926/1926 — +1 suite, +20 tests, zero regressions elsewhere); full e2e tier unchanged **9/9 real suites, 202/202 real tests**; backend `tsc` (full scope) 0 errors; backend `eslint` (full `src` tree) 0 errors. A concurrent parallel session continued activity on `ocr-intake.service.ts`+spec and `asset-access.service.ts` throughout this cycle — confirmed zero file overlap (this cycle only added one new, untracked file) before staging.

**Final state, all fresh this cycle:** `strictNullChecks` still 0 (unchanged, no type-level work this cycle), backend `tsc`/`eslint` both 0 errors, main jest 246/246 (1946/1946, +1 suite/+20 tests), e2e 9/9 (202/202) unchanged. Diff: 1 new file (`pinecone.service.spec.ts`). Roadmap item #17: 1 of 9 closed, **8 remain** (`DeviceTokenService`, `PersonalizationService`, `BiometricService`, `BiometricController`, `AssetAccessService`, `CacheService`, `SentinelController`, `ERPulseHandoverService`) — the natural next-cycle target, continuing this as a multi-cycle sweep rather than rushing all 8 into one pass.

### Cycle 178 (2026-07-24) — closing the biometric-auth coverage gap; a concurrent edit reshapes this cycle's scope mid-pick

Continued the roadmap item #17 sweep. Checked `git status` before picking a target, as always — and this time it mattered directly: `AssetAccessService`, one of the 8 remaining candidates, had active uncommitted changes from the concurrent parallel session (`backend/src/modules/platform-assets/asset-access.service.ts`, a small ongoing diff). Writing a new test file against a service mid-edit by another session risks the two changes colliding or the new tests being written against a version of the code that's about to change out from under them — so it was deliberately skipped this cycle, staying on the roadmap list rather than being picked blind.

Picked the next-most-sensitive pair instead: `BiometricService` + `BiometricController` (`src/modules/auth/services/biometric.service.ts`, `src/modules/auth/biometric.controller.ts`) — real biometric enrollment, challenge/response verification, and account-lockout logic, genuinely security-critical and previously covered by neither a dedicated spec nor any incidental coverage from another test. Read both files in full first.

**`biometric.service.spec.ts` (18 tests).** Followed this codebase's own established pattern for security-service tests (`two-factor.service.spec.ts`'s `Test.createTestingModule` + mocked-repository style) rather than inventing a new one. Deliberately did **not** mock Node's built-in `crypto` module — `hashToken()`'s SHA-256 hashing and `verifyToken()`'s `crypto.timingSafeEqual()` comparison run for real in every test, replicated in the test file via the same `crypto.createHash('sha256')` call the service itself uses, so tests assert against genuinely-computed hashes rather than a stubbed "looks right" value. Covered:
- **Enrollment**: a fresh device creates a new `BiometricConfig` via `create()`+`save()` and returns a plaintext challenge token whose SHA-256 hash matches what's actually stored (confirming the plaintext token is never itself persisted); re-enrolling an already-known device updates in place (no `create()` call) and resets `failedAttempts`/`lockedUntil`, with an unspecified `deviceName` correctly falling back to the existing stored value rather than being overwritten with `undefined`.
- **The lockout state machine**, tested at every transition rather than just the happy path: not-enrolled rejects immediately; a still-locked account rejects without even touching `failedAttempts` (confirming the lockout check short-circuits before the crypto comparison); a lockout whose window has already elapsed correctly allows verification to proceed again; a wrong challenge response below the 5-attempt threshold increments `failedAttempts` and rejects without locking; reaching exactly the 5th failed attempt sets `lockedUntil` to a real future timestamp and rejects with the lockout message; a correct response resets `failedAttempts`/`lockedUntil` to zero/`null`, bumps `usageCount`, stamps `lastUsedAt`, rotates to a fresh single-use challenge token (confirmed different from the one just consumed), and issues real access/refresh JWTs via the mocked `JwtService`.
- **A defensive resilience test, not just a happy-path/failure-path pair**: `crypto.timingSafeEqual()` throws a `RangeError` (not returns `false`) when its two buffers differ in length — genuinely possible if a stored hash were ever corrupted or came from a legacy hashing scheme of a different output length. Constructed exactly that scenario (a stored `challengeToken` of the wrong length) and confirmed the service's outer `catch` converts the raw `RangeError` into a clean `UnauthorizedException('Biometric verification failed')` instead of letting an unrelated internal error type leak to the caller — the same "fail closed with a clear message, not an uncontrolled crash" idiom this whole `strictNullChecks` era of cycles established for Stripe/webhook gaps, here applying to a genuinely different failure class (a runtime crypto exception, not a null/undefined value).
- **The remaining CRUD-shaped methods**: `getBiometricConfig`'s query narrows correctly with/without an optional `deviceId` filter; `disableBiometric` flips `isEnabled` and throws `BadRequestException` when the config doesn't exist; `deleteBiometricConfig` scopes its delete to all of a user's devices or just one; `resetFailedAttempts` issues the correct `update()` call; `getBiometricStats` aggregates `totalUsages`/`lastUsed`/per-device stats correctly, including the empty-state case (zero devices, `lastUsed: null`) rather than crashing on an empty array.

**`biometric.controller.spec.ts` (7 tests).** A thin pass-through layer, tested by direct construction (`new BiometricController(mockService)`, matching this codebase's existing `auth.controller.spec.ts` pattern rather than a full Nest `TestingModule` for a plain-constructor class) with the service mocked. The point of these tests isn't re-testing the service's logic — it's confirming the controller's response-shaping never leaks internal fields it explicitly reshapes to avoid exposing: `enrollBiometric`'s response strips the hashed challenge token and `failedAttempts` from the returned `config` object; `verifyBiometric`'s response strips the `User` entity's `passwordHash` from the returned `user` object; `getBiometricConfig` reshapes each entry the same way. Also confirmed each endpoint correctly forwards `req.user`/path params to the service (e.g. `disableBiometric`/`deleteBiometricConfig` scope to `req.user.id`, not a client-supplied user id, which would otherwise be a real IDOR risk) and that `checkBiometricAvailable` reports the real enrolled-device count and the 3 supported biometric types.

**Verification:** full auth module suite **6/6 suites, 43/43 tests** (was 4/4, 18/18 before this cycle — the 2 new spec files, run alongside the existing `auth.service.spec.ts`/`auth.controller.spec.ts` etc.); full backend suite **248/248 suites, 1971/1971 tests** (was 246/246, 1946/1946 — +2 suites, +25 tests, zero regressions elsewhere, confirmed via an isolated auth-module run first); full e2e tier unchanged **9/9 real suites, 202/202 real tests**; backend `tsc` (full scope) 0 errors; backend `eslint` (full `src` tree) 0 errors, including both new spec files after an `eslint --fix` pass cleaned up pure line-wrap/quote-style formatting. A concurrent parallel session continued its own activity on `ocr-intake.service.ts`+spec and (still) `asset-access.service.ts` throughout — confirmed zero file overlap before staging (this cycle only added the 2 new files).

**Final state, all fresh this cycle:** `strictNullChecks` still 0 (unchanged), backend `tsc`/`eslint` both 0 errors, main jest 248/248 (1971/1971, +2 suites/+25 tests), e2e 9/9 (202/202) unchanged. Diff: 2 new files (`biometric.service.spec.ts`, `biometric.controller.spec.ts`). Roadmap item #17: 3 of 9 closed (`PineconeService`, `BiometricService`, `BiometricController`), **6 remain** (`DeviceTokenService`, `PersonalizationService`, `AssetAccessService`, `CacheService`, `SentinelController`, `ERPulseHandoverService`) — `AssetAccessService` is the natural next pick once the concurrent session's edits there settle; otherwise `CacheService` or `SentinelController` are unblocked alternatives.

### Cycle 179 (2026-07-24) — CacheService coverage; cycle interrupted by a large Reception/platform audit request

`AssetAccessService` was still blocked (same concurrent-session diff, unchanged in size). Compared the two remaining unblocked candidates: `SentinelController` (425 lines, 17 endpoints, 4 injected services, real business logic in several routes — analytics computation, CAD-ingest clinical-field detection, alarm-action verb mapping) versus `CacheService` (159 lines, clear method boundaries). Picked `CacheService` as the better single-cycle scope, deferring `SentinelController` to its own future cycle rather than rushing a shallow pass over a large, RBAC-gated real-time monitoring surface.

Mocked the `redis` package's `createClient` export (no existing pattern for this in the codebase, so built one) and tested both the connection lifecycle and every cache operation. **29 tests**: `connect()`'s three auto-disable branches (`REDIS_ENABLED=false`, missing `REDIS_HOST`, missing `redis` config namespace) each confirmed to never call `createClient`; a real connected client verified to receive the exact configured host/port/password/db; a `connect()` throw confirmed to fall back to a disabled (`client: null`) state rather than crashing `onModuleInit()`; the `reconnectStrategy` closure's linear-backoff-then-give-up-after-3-retries logic independently recomputed and checked at 4 points. Every public method (`get`/`set`/`del`/`exists`/`getOrSet`/`clear`/`close`) tested twice — once against a connected mock client (real behavior: JSON round-tripping, TTL application, error-to-`false`/`null` conversion) and once with no client at all (confirming every method degrades gracefully instead of throwing when Redis is disabled, which is this service's actual production behavior in any environment without `REDIS_HOST` set). One correctness-focused test: `getOrSet`'s cache-hit check is `cached !== null`, not a truthiness check — verified a cached `0` is correctly treated as a hit and the factory is never called, which would silently break if that check were ever "simplified" to `if (cached)`.

**Verification:** cache module suite **1/1, 29/29**; full backend suite **249/249 suites, 2000/2000 tests** (was 248/248, 1971/1971 — +1 suite, +29 tests, zero regressions); full e2e tier unchanged **9/9 real suites, 202/202 real tests**; backend `tsc`/`eslint` both 0 errors full-tree. Concurrent session unchanged (`ocr-intake.service.ts`+spec, `asset-access.service.ts`) — zero overlap.

**This cycle was interrupted mid-verification by two large user messages** commissioning a comprehensive Reception Desk audit/redesign and, separately, a platform-wide "become the enterprise EDOS" reconstruction — both recognized as continuations of the already-tracked `project-full-platform-reconstruction` initiative (commissioned 2026-07-13, 10-stage program, Stage 1-2 closed, Stage 3+ not started) and the `project-reception-ccdl-audit`/`project-reception-desk-rebuild` history, not a fresh ask. Finished and committed this cycle's `CacheService` work cleanly before switching context, per this program's standing discipline of never leaving verified work in a dangling, uncommitted state. See project memory for the grounded response to that request — not duplicated in this scorecard, which stays scoped to the backend quality-baseline program.

**Final state, all fresh this cycle:** `strictNullChecks` still 0 (unchanged), backend `tsc`/`eslint` both 0 errors, main jest 249/249 (2000/2000, +1 suite/+29 tests), e2e 9/9 (202/202) unchanged. Diff: 1 new file (`cache.service.spec.ts`). Roadmap item #17: 4 of 9 closed, **5 remain** (`DeviceTokenService`, `PersonalizationService`, `AssetAccessService`, `SentinelController`, `ERPulseHandoverService`).

### Cycle 180 (2026-07-24) — closed the top Reception Desk finding: no server-side duplicate-patient check on the live create path

Three parallel research agents plus direct code verification audited Reception Desk's real state against the two interrupting mega-briefs (full detail in project memory, not duplicated here). The single most patient-safety-relevant finding: `SmartIntakeService.createFromIntake()`/`createVerticalSlice()` — backing `POST /emergency/patients`, `/emergency/intake`, and `/emergency/intake/vertical-slice`, the endpoints the primary NestJS/TypeORM patient store actually uses by default — ran zero duplicate-patient check. A real fuzzy-match engine (`mpi.service.ts`) exists, but only inside the Mongoose intake path, which is off by default (`ENABLE_MONGOOSE_EMERGENCY_OS` unset).

**Before implementing, re-verified the finding against the actual frontend code rather than trusting the backend-only audit at face value** — this caught a real overstatement. The reception UI already has a genuine, working, blocking client-side duplicate gate in three separate places: `ReceptionWorkspace.tsx`'s `createAndRoute()` (opens a confirm modal and returns early on a ≥85% match before ever calling create), `SmartIntake.tsx`'s identity-verification step (`onCreatePatient` only fires after staff explicitly chooses "Create patient" over "Link patient" having reviewed match candidates), and `QuickIntake.tsx`'s `duplicateAcknowledged` checkbox gate. So the real gap was narrower than "no duplicate detection at all" — it was "no *server-side* enforcement," meaning only a caller that bypasses the UI entirely (a script, a different client, a future integration) was actually at risk. Fixed the correct, narrower problem rather than the initially-reported one.

**Implementation.** New `patient-duplicate-detection.ts` — pure functions only (`scorePatientDuplicate`, `findPatientDuplicateCandidates`), deliberately with zero import back into `emergency-os.services.ts` (an earlier draft that included an `@Injectable` wrapper class created a real module-load cycle that would have broken Nest's constructor-param DI metadata — caught before it shipped, not after). Same field weights and score bands as the two existing implementations (frontend `patientDuplicateDetection.ts`, backend `mpi.service.ts`) for consistency, minus phone/email/healthCard — `EmergencyPatient`/the `Patient` TypeORM entity don't carry those fields today.

Wired into `SmartIntakeService` via a new `guardAgainstUnconfirmedDuplicate()`: a ≥85% match with no `confirmDuplicateOverride: true` on the request throws `ConflictException` (409) with the candidate list in the response body, and logs a new `patient_duplicate_flagged` workflow-audit entry either way (blocked or overridden) — so even a caller that ignores the 409 leaves a durable audit trail. Anything below high-confidence is logged but non-blocking, matching the existing 4-band system (`link_after_staff_confirmation` / `possible_duplicate_review` / `manual_review` / `create_new_patient`).

**Preserving the golden path.** Since the reception UI's own client-side gate already resolves the human decision before its API call fires, threaded `confirmDuplicateOverride: true` through every known trusted internal caller so none of them regress: `receptionIntakeOrchestrator.ts`'s `syncReceptionPatientToBackend` (covers `ReceptionWorkspace`'s `createAndRoute`/`handleProvisionalUnknown` paths), `QuickIntake.tsx`'s submit handler, `SmartIntake.tsx`'s vertical-slice fallback path, and `emergencyStore.ts`'s `addPatient({syncToBackend: true})` branch (currently unreachable in practice — no live caller passes `syncToBackend: true` today — but fixed for whenever it is). Found one dead code path along the way (`receptionQuickIntakeService.ts`'s `persistReceptionQuickIntakePatient` is only referenced by its own test, not by any live component) — left untouched, out of scope, noted for a future cleanup pass.

**Tests.** `patient-duplicate-detection.spec.ts` (11 tests): exact score-band boundaries verified precisely rather than loosely (e.g. a matching MRN alone lands at *exactly* 35, the manual-review floor, not just "somewhere above a threshold"), case/whitespace insensitivity, conflicting-field score penalties, the `limit`/`minScore` options. `smart-intake.service.spec.ts` (7 tests): the 409-block path, the override-and-log path, the non-blocking low-confidence path, and `createVerticalSlice` never reaching `movePatientToState` when blocked.

**Verification.** Full backend suite **251/251 suites, 2018/2018 tests** (was 249/249, 2000/2000 — +2 suites, +18 tests, zero regressions); backend `tsc` both configs 0 errors; backend `eslint` full-tree 0 errors (1 genuine unused-import warning caught and fixed — `EmergencyPatient` in `emergency-os.controller.ts`, orphaned after switching its DTO type). Frontend `tsc --noEmit` 0 errors; frontend `eslint` clean on all touched files. Frontend Vitest run could not execute — same pre-existing `esbuild`/Windows Application Control sandbox block documented since Cycle 160, unrelated to this change; checked every test file that references the touched API functions (`receptionIntakeOrchestrator.test.ts`, `emergencyOsApi.test.ts`, `receptionQuickIntakeService.test.ts`, `R12EndToEndWiring.test.tsx`) by hand and confirmed none assert exact call-argument counts that the new optional second parameter would break. Checked `git status` before staging — a concurrent parallel session's unrelated diff (`ocr-intake.service.ts`+spec, `asset-access.service.ts`) confirmed untouched and excluded.

**Final state, all fresh this cycle:** backend `tsc`/`eslint` both 0 errors, main jest 251/251 (2018/2018, +2 suites/+18 tests), frontend `tsc` 0 errors. Diff: 3 new backend files (`patient-duplicate-detection.ts`, `patient-duplicate-detection.spec.ts`, `smart-intake.service.spec.ts`), 3 backend files edited (`emergency-os.services.ts`, `emergency-os.controller.ts`, `emergency-os.types.ts`), 5 frontend files edited (`emergencyOsApi.ts`, `receptionIntakeOrchestrator.ts`, `QuickIntake.tsx`, `SmartIntake.tsx`, `emergencyStore.ts`). Roadmap item #18 opened and its duplicate-check sub-item closed same-cycle; validation and fire-and-forget-write sub-items remain open for a future cycle.

### Cycle 181 (2026-07-24) — closed `DeviceTokenService` on roadmap item #17; corrected a self-introduced overstatement from Cycle 180

User said "let's do the next cycle." Re-checked `git status` first, as always: `AssetAccessService` still carries the identical small uncommitted diff from the concurrent parallel session — unchanged since Cycle 179, still off-limits. With roadmap item #18's duplicate-check sub-item closed last cycle, considered continuing straight into item #18's remaining "near-zero body validation" sub-item, since the context was still warm. Looked closely enough to size it first rather than assume it's a quick follow-on: `main.ts`'s global `ValidationPipe` already runs with `whitelist: true, forbidNonWhitelisted: true, transform: true` — meaning the moment `SmartIntakeCreateInput` becomes a real class-validator DTO instead of a plain interface, **any field the frontend sends that isn't explicitly declared on the DTO gets rejected with a 400**, not silently stripped. `EmergencyPatient`'s full shape includes several nested arrays/objects (`vitals[]`, `notes[]`, `timeline[]`, the `arrival` block, `triageAssist`, `quickSafetyFlags`, `highRiskComplaintFlags`) that would all need their own nested DTOs to whitelist correctly, and at least one real caller (the "unknown patient" chooser flow) deliberately sends minimal data expecting the service to backfill defaults — a DTO that's too strict would break that flow outright. Concluded this needs its own dedicated, carefully-scoped pass (enumerate every field every real caller sends today before turning on enforcement), not a same-cycle continuation — left it open on the roadmap rather than rush it.

Also re-examined item #18's third original sub-item, "fire-and-forget DB write" (`persistPatientToDatabase()` not awaited), before deciding whether it was a fast win instead. Reading the method's own doc comment settled it: *"Phase 1 of the DB migration: best-effort, non-blocking write-through... Reads still come from the in-memory array... Never throws; a failed write here must not break the synchronous in-memory response."* This is a deliberate, already-documented design choice, not an overlooked bug — and it's the exact mechanism already tracked as open roadmap item #2 ("Cut `EmergencyPatientService` reads to the database (Phase 2)... needs a dedicated design pass"). Awaiting the write wouldn't even improve correctness today, since nothing reads from the DB yet — it would just add latency to every mutation for no behavioral benefit. Corrected item #18 to remove this sub-item rather than let last cycle's overstatement stand; the underlying concern lives correctly under item #2.

With both #18 sub-items now correctly re-scoped as "needs its own pass, not this cycle," picked the next roadmap item #17 candidate instead: `DeviceTokenService` (`backend/src/modules/notifications/services/device-token.service.ts`, 209 lines) — push-notification device-token registration and lifecycle management, flagged since Cycle 167 and the longer-standing of the 2 remaining unblocked candidates (`PersonalizationService`, flagged Cycle 168, is the other). Read the full file before writing anything: real register-or-update-in-place branching, a soft-delete (`deactivateToken`) vs. hard-delete (`removeDeviceToken`) distinction with an OR-matching lookup (token or device id), a multi-user token-grouping aggregation (`getTokensForUsers`), and a `QueryBuilder`-based bulk cleanup (`cleanupOldTokens`) with date-cutoff arithmetic.

**Wrote 16 tests** (`device-token.service.spec.ts`), following the established `Test.createTestingModule` + `getRepositoryToken` mocked-repository pattern (matching `biometric.service.spec.ts`'s style) rather than inventing a new one:
- **`registerDeviceToken`**: confirms a genuinely new token calls `create()` with the right shape; confirms re-registering an existing token updates the row in place (`create()` not called) and correctly flips `isActive` back to `true` even if it had been previously deactivated; confirms a repository failure propagates rather than getting silently swallowed (the method's own `try/catch` re-throws after logging, so this asserts that re-throw actually happens, not just that logging occurred).
- **`getUserDeviceTokens`/`getActiveTokens`**: verifies the exact `where`/`order` query shape (active-only, newest-used-first) and that `getActiveTokens` correctly projects down to just the token strings.
- **`deactivateToken`/`removeDeviceToken`**: both `NotFoundException` guard paths; confirmed `deactivateToken` never calls `delete()` (soft-delete only) and `removeDeviceToken` never calls `save()` (hard-delete only) — these are easy to accidentally conflate since both "remove a device" conceptually; confirmed `removeDeviceToken`'s OR-array `where` clause is constructed exactly as the source builds it (token-match OR id-match), not just that some matching call happened.
- **`markTokenAsInvalid`/`updateLastUsed`**: both are direct `update()` calls with no read first — confirmed via `expect(repo.findOne).not.toHaveBeenCalled()` on the invalid-token path, since an unnecessary read-before-write here would be a real (if minor) inefficiency worth catching.
- **`getTokensForUsers`**: the one method with real aggregation logic — built a fixture with 2 users, one having 2 devices, and confirmed the resulting `Map` correctly accumulates both of that user's tokens into one array rather than the second device overwriting the first (`tokenMap.get(userId)!.push(...)` is correct, but a `tokenMap.set(userId, [...])` typo one line up would silently break this and every test would still "pass" on the single-device case).
- **`cleanupOldTokens`**: mocked the full `createQueryBuilder().delete().where().andWhere().execute()` chain; under `jest.useFakeTimers().setSystemTime(...)`, verified the computed cutoff date to the **exact ISO timestamp** (not just "before now") for both the 90-day default and an explicit 30-day override — cross-checked the expected values with a standalone `node -e` date computation in the same environment before writing the assertion, rather than trusting mental arithmetic on month-boundary date rollover; also verified the `result.affected ?? 0`-shaped fallback returns `0` rather than `null`/`undefined` when the driver reports no count.

**Verification.** New suite **1/1, 16/16**. A first full-suite run showed **1 failing test** unrelated to this change: `encryption.service.spec.ts`'s `should hash values quickly` performance assertion (1000 hashes expected `<100ms`, measured `343.93ms`) — this ran concurrently with several other `tsc`/`eslint` commands in the same session, a plausible source of CPU contention. Rather than wave it off, isolated the one spec file (`37/37` clean) and then re-ran the entire suite alone with nothing else contending for CPU (**252/252 suites, 2034/2034 tests**, fully clean) — confirming it was a transient load-sensitivity flake in a hard-coded wall-clock threshold, not a regression from this cycle's change (a device-token test file has no relationship to the encryption module's hashing throughput). Backend `tsc` (both configs) 0 errors; backend `eslint` 0 errors on both the new spec file and the untouched source file. No frontend files touched this cycle. `git status` re-checked before staging — the concurrent session's diff (`ocr-intake.service.ts`+spec, `asset-access.service.ts`) confirmed still untouched and excluded.

**Final state, all fresh this cycle:** backend `tsc`/`eslint` both 0 errors, main jest 252/252 (2034/2034, +1 suite/+16 tests). Diff: 1 new file (`device-token.service.spec.ts`), plus this cycle's own `SCORECARD.md` roadmap correction (no source changes for the correction itself). Roadmap item #17: 5 of 9 closed, **4 remain** (`PersonalizationService`, `AssetAccessService` [still blocked], `SentinelController` [still deferred, size], `ERPulseHandoverService`). Roadmap item #18: duplicate-check sub-item stays closed (Cycle 180); fire-and-forget-write sub-item removed from scope (folded into item #2); validation sub-item remains open, now explicitly scoped as "needs its own dedicated pass," not a quick follow-on.

**Lesson: correcting your own prior cycle's roadmap entry, in public, in the same file, is exactly the kind of "verify before trusting — including self-generated docs" discipline this whole program has applied to the codebase's own audit artifacts (the Reception Health Report contradiction from the cycle before this thread even started) — it applies just as much to this file's own claims from one cycle ago. A doc comment inside the code itself (`persistPatientToDatabase`'s "Phase 1 of the DB migration") was the actual ground truth the whole time; the fix was re-reading it carefully during scoping, not writing new code. Also: a global `ValidationPipe` configured with `forbidNonWhitelisted: true` is a meaningfully different, higher-stakes thing to newly activate on a route than it sounds — until a route's body type becomes a real decorated class, that global config is silently inert for that route specifically, and turning it on is a behavior change with real regression surface (a payload shape that's tolerated today can start getting hard-rejected), not just "finally enabling validation that already runs everywhere else."**

### Cycle 182 (2026-07-24) — first full dependency/security re-audit in 22 cycles

User said "let's do one more scan," which was ambiguous enough (accessibility? circular deps? unused exports? dependencies?) to ask rather than guess — a genuine fork where picking wrong could burn a full cycle on the wrong target. User picked the dependency/security audit. Last real check was Cycle 160 (2026-07-23) — 22 cycles and a lot of new code ago, so treated this as a fresh baseline rather than a quick "still fine?" glance.

**Frontend (`npm audit`):** 3 vulnerabilities — `postcss` (high, path-traversal in sourcemap auto-loading, `GHSA-r28c-9q8g-f849`, transitive via `vite`), `react-router`/`react-router-dom` (2 moderate CVEs each). Ran `npm audit fix --dry-run` first rather than apply blind: it proposed bumping `postcss` 8.5.15→8.5.23 and `nanoid` 3.3.12→3.3.16 (both safe transitive patch bumps under `vite`), plus **removing `esbuild-wasm@0.28.1`** — investigated that before trusting it, since an unexplained package removal is exactly the kind of side effect worth checking rather than accepting on faith. Found `esbuild-wasm` physically present in `node_modules` but with **zero references anywhere in `package-lock.json`** — a stray, untracked package (plausibly a leftover from a past attempt to work around this session's long-documented `esbuild` Application Control sandbox block, since `esbuild-wasm` doesn't need a native binary spawn — but not actually wired into anything). Its removal is a safe reconciliation, not a functional change. Applied `npm audit fix` (non-force): postcss's high-severity finding closed cleanly.

`react-router`/`react-router-dom` needed real investigation before deciding whether to fix or defer: checked the npm registry directly and confirmed **6.30.4 (currently installed) is the latest 6.x release** — there is no patched 6.x version. Both CVEs (`GHSA-wrjc-x8rr-h8h6` open-redirect-via-backslash, `GHSA-337j-9hxr-rhxg` SSR-hydration constructor injection) are only fixed in the 7.x line (latest 7.18.1). React Router 7 merged Remix's data-loader/route-definition patterns — a real migration across this app's ~190+ routes, not a version-bump drive-by, and the SSR-hydration CVE likely doesn't even apply here (this app is a Vite SPA, not SSR). Deliberately left unfixed and opened as new roadmap item #19, rather than either silently skip it or attempt a rushed major-version migration mid-audit-cycle.

**Backend (`npm audit`):** 5 vulnerabilities — the already-tracked `@xenova/transformers`/`sharp` cluster (roadmap item #5, high), plus 2 new ones: `mongoose` (moderate, prototype pollution via `__proto__`-prefixed dotted paths in update casting, `GHSA-664h-wqgq-64gw`) and `tar`/`sqlite3` (moderate, uncontrolled-recursion DoS via crafted long-path tar archives, `GHSA-r292-9mhp-454m`). Same dry-run-first discipline: `mongoose` 9.7.0→9.8.0 and `tar` 7.5.19→7.5.22 were both offered as safe non-force fixes, though `mongoose`'s bump pulls in a newer `mongodb` driver (7.2.0→7.5.0) with its own sub-dependency churn (removed `bson`/`whatwg-url`/`tr46`/`mongodb-connection-string-url`, added updated equivalents) — a real dependency-tree change worth verifying, not assuming equivalent. Applied `npm audit fix` (non-force): both moderate findings closed.

Re-checked `@xenova/transformers`/`sharp` (item #5) with fresh evidence rather than carrying forward an assumed-still-true note: `npm audit fix --force --dry-run` still only offers downgrading `@xenova/transformers` to **1.4.2** — older than the currently-installed 2.17.2, not a real fix, matching the exact conclusion already on record since Cycle 160. Left untouched, roadmap entry updated to note the re-confirmation with a date rather than just leaving last cycle's note to imply it was never re-checked.

**Verification, deliberately thorough given this touches the Mongoose/MongoDB stack directly** (the off-by-default Express intake path documented in the Reception Desk audit, plus whatever else depends on `mongoose`): frontend `tsc --noEmit` 0 errors (only `package-lock.json` changed, no `package.json` version-range edits, so no source-level ripple expected — confirmed anyway). Backend `tsc` both configs 0 errors; full `eslint` sweep (whole `src` tree, not just changed files, matching the Cycle 176 lesson about per-file linting missing drift) 0 errors; **full jest suite 252/252 suites, 2034/2034 tests**, unchanged from before the dependency bump; **full e2e tier 9/9 real suites, 202/202 real tests**, unchanged, run specifically because this cycle touches the Mongoose/MongoDB dependency chain directly and the e2e tier is the closest thing this program has to an integration check on that stack. `git status` confirmed only `package-lock.json` (both trees) changed — no source files touched, nothing to conflict with the concurrent parallel session's ongoing `ocr-intake.service.ts`/`asset-access.service.ts` edits.

**Final state, all fresh this cycle:** frontend `tsc` 0 errors, frontend `npm audit` 2 moderate (was 3: 2 moderate + 1 high) — 1 high closed, 2 moderate newly found and correctly deferred (not previously on record). Backend `tsc`/`eslint` both 0 errors, backend `npm audit` 2 high (was 5: 3 moderate + 2 high) — both moderate closed, the 2 high (`@xenova/transformers`/`sharp`, one finding counted twice) re-confirmed unchanged with no safe fix. Main jest 252/252 (2034/2034) unchanged, e2e 9/9 (202/202) unchanged. Diff: 2 lockfiles only (`package-lock.json`, `backend/package-lock.json`), `SCORECARD.md`. Roadmap: item #5 re-confirmed with a fresh date; new item #19 opened for `react-router`/`react-router-dom`.

**Lesson: a "quick dependency audit" is only actually quick for the fixes that are genuinely safe — the discipline that made this cycle useful was running `--dry-run` before every `npm audit fix` and reading the exact diff (not just the vulnerability count) before applying it, twice catching something worth a second look before trusting it: a package the dry-run said it would silently remove (turned out to be safe — untracked stray weight, not a real dependency), and a "fix available: true" flag that turned out to require `--force` and a downgrade once actually attempted (turned out to not be a real fix at all, for react-router). `npm audit`'s summary-line vulnerability counts and its `fixAvailable` boolean are both worth treating as a starting hypothesis to verify, not a number to react to directly — the same "verify before trusting" discipline this program applies to code applies just as much to tooling output.**

### Cycle 183 (2026-07-24) — closed `AssetAccessService`, the roadmap item #17 candidate blocked longest by a concurrent session

Between cycles, the user separately asked to "commit all changes and push" — the working tree at that point held only 3 modified files, all belonging to the concurrent parallel session this program has been tracking since Cycle 174 (`ocr-intake.service.ts`+spec, `asset-access.service.ts`) plus 2 untracked `.tmp-*` scratch directories. Read the actual diffs before committing rather than trust the "concurrent session, leave alone" label uncritically after so many cycles of it sitting unchanged: all 3 turned out to be pure prettier line-wrap formatting, zero semantic change, and had been sitting stable and uncommitted for at least 9 cycles (Cy174 through Cy182) without ever actually being touched further — evidence this wasn't really an actively "concurrent" session by that point, just old uncommitted formatting nobody had swept up. Verified safe (`tsc`/`eslint` clean, all 16 relevant suites/60 tests passing) and committed them; deliberately left the 2 `.tmp-*` directories out (compiled `.js` scratch copies of `src/` files from some earlier ad-hoc test harness, not real project state) and flagged that exclusion to the user rather than silently deciding either way.

That commit had a direct effect on this cycle's target selection: with `asset-access.service.ts` now clean in git status, `AssetAccessService` — the single strongest evidence-backed candidate on the roadmap item #17 list since Cycle 171 found a real bug there (`recommendedAssetIds` vs. the entity's actual `preferredAssetIds`, silently zeroing every user's role-based asset recommendations) purely for lack of test coverage — was finally unblocked after 4+ cycles of deferral on concurrency grounds alone.

Read the full 272-line file before writing anything. `resolveAccess()` is a genuine access-control decision engine: a priority-ordered cascade of 9 checks (user-hidden, admin-only governance, draft/archived lifecycle, human-review-required, unsupported backend, demo/entitlement gating, workspace restriction, deprecated/beta lifecycle, default-allowed) that determines whether a given user can see/use a given platform asset — security/entitlement-relevant code in the same category this program has prioritized before (Biometric, Cache, DeviceToken). It's also unusually test-friendly: `resolveAccess` is public and takes fully explicit params, so the entire decision engine could be tested directly without mocking any of the 3 injected collaborator services at all.

**Wrote 39 tests** (`asset-access.service.spec.ts`), split into the pure decision-engine (no mocks) and the orchestration layer (3 mocked collaborators):

- **The full cascade, in priority order, including precedence checks, not just isolated branches**: confirmed `user-hidden` wins over an admin-only asset even for a student (the very first check short-circuits everything downstream); confirmed all 4 independent signal sources that trigger the admin-only gate (`governance.adminOnly`, `governance.visibility === 'admin-only'`, `governance.audience === 'admin-only'`, `permissionPolicy.adminOnly`) each work on their own; confirmed an admin passing the admin-only gate doesn't short-circuit straight to `ALLOWED` — it pushes an `'admin-lifecycle-override'` reason and keeps evaluating every check below it (verified by pairing it with a `BETA` lifecycle asset and confirming both reasons appear, in order); `DRAFT`/`ARCHIVED` lifecycle hides an asset from everyone including an admin; a `requiresHumanReview` asset blocks a student but not other roles.
- **The demo/entitlement sub-branch, the most conditionally nested part of the method**: a demo-labeled asset is `DEMO_ONLY` with no org entitlement gating in effect, but flips to `LOCKED` the moment the org has any entitlement packs configured and this specific asset isn't in them — and back to `DEMO_ONLY` once it is; for non-demo assets, confirmed the subtle "open-access org" distinction — `hasOrganization: true` with an *empty* entitled set is `ALLOWED` (no gating configured yet) unless `strictEntitlements` is also set, in which case an empty entitled set locks everything. This distinction is easy to get backwards when reading the code casually, so tested it explicitly rather than assume it from the variable names.
- **Priority ordering and its one real invariant**: pinned beats workspace-recommended beats role-recommended beats recent (tested by setting all four simultaneously and confirming only the highest-priority reason wins at each step down), and — the one genuinely important safety property — a `LOCKED` asset always gets priority 900/`'permitted'` regardless of whether it's pinned, since `resolvePriority`'s very first check short-circuits on any non-`ALLOWED`/non-`DEMO_ONLY` state. Without this test, a future refactor that moved the pinned-check earlier could silently let a user "pin" their way past an entitlement lock in the UI's sort order.
- **The full `effectiveAccessState` mapping table** (the internal `AssetAccessState` → external `SaasAssetAccessState` translation), via a parametrized `it.each`, plus 3 extra cases for the `ALLOWED` branch specifically: unpinned/unrecommended maps to `'visible'`, pinned maps to `'pinned'`, role-recommended maps to `'recommended'` — and, deliberately tested because it's easy to assume otherwise, a merely-*recent* asset still maps to `'visible'`, not `'recommended'`, since `toSaasAccessState` only checks the `pinned`/`*-recommended` priority reasons, not `recent`.
- **Orchestration** (`getUserAssetAccess`, 3 collaborators mocked): the default asset-id list is the union of entitled ids and every known asset when none are explicitly requested, vs. scoped to exactly the requested ids when they are; sorting is priority-first (a pinned asset out of alphabetical order still sorts first) with a genuine alphabetical tiebreaker case for equal-priority assets (not just asserted by test title — actually exercised with two unpinned assets); legacy workspace tool-id alias resolution verified against a real alias that expands to a *different* literal asset id (`medical-iot` → `['telemetry-monitoring', 'medical-iot']`), not just a self-mapping; pinned/hidden/recent ids correctly merge across all 3 legacy preference-field shapes (`pinnedAssetIds`/`pinnedToolIds`/`saasProfile.pinnedAssets`) rather than only reading the newest one.

**Verification:** new suite **1/1, 39/39**; backend `tsc` (both configs) 0 errors; backend `eslint` 0 errors (6 pure prettier line-wrap findings in the new spec file, fixed via `eslint --fix`, re-verified all 39 tests still pass after); full backend suite **253/253 suites, 2073/2073 tests** (was 252/252, 2034/2034 — +1 suite, +39 tests, zero regressions). No frontend files touched. `git status` confirmed clean of any concurrent-session state before staging — this cycle's own commit from the prior turn had already cleared it.

**Final state, all fresh this cycle:** backend `tsc`/`eslint` both 0 errors, main jest 253/253 (2073/2073, +1 suite/+39 tests). Diff: 1 new file (`asset-access.service.spec.ts`). Roadmap item #17: 6 of 9 closed, **3 remain** (`PersonalizationService`, `SentinelController` [still deferred, size], `ERPulseHandoverService`).

**Lesson: the "check `git status` before picking a target" discipline paid off in a new way this cycle — not by steering away from a blocked file (the usual case, Cy178/179/181), but by the blocker actually clearing between cycles as a side effect of an unrelated user request ("commit all changes"), which is exactly why re-checking fresh every cycle matters more than trusting last cycle's "still blocked" note. Also: when a file has looked "concurrently in flux" for many cycles running with an unchanged diff, it's worth actually reading that diff at some point rather than perpetually deferring on the same assumption — this one turned out to be inert, pre-existing formatting debt, not live work in progress, and the label had outlived its accuracy by several cycles.**

### Cycle 184 (2026-07-24) — closed `PersonalizationService`, the last unblocked roadmap item #17 candidate before `SentinelController`/`ERPulseHandoverService`

User said "let's go the next cycle." `git status` clean of any concurrent-session state (the last cycle's commit had already cleared it). Picked `PersonalizationService` (`backend/src/modules/personalization/personalization.service.ts`, 145 lines) — flagged since Cycle 168, the only remaining roadmap item #17 candidate not already deferred for size (`SentinelController`) or already partially covered (`ERPulseHandoverService`). Read the full file first: AI-preference storage (preferred behavior, suggested tools, recommended workflows, recent-prompt history) plus a full saved-prompt CRUD surface, backed by 2 TypeORM repositories.

**Wrote 16 tests** (`personalization.service.spec.ts`), following the established `Test.createTestingModule` + `getRepositoryToken` mocked-repository pattern:

- **`getForUser`'s defaulting logic, tested at both points it can trigger, not just the obvious one**: the first-ever call for a user creates a real default row (`preferredBehavior: 'clinical_copilot'`, a real 3-tool `suggestedTools` default, the literal `DEFAULT_RECOMMENDATIONS` array) — but also, separately, confirmed the *same* defaults apply when an existing row is found with `suggestedTools`/`recommendedWorkflows`/`recentPrompts` explicitly `null` (a genuinely different code path — `preference.suggestedTools || [...]` firing on a null value from an existing row, not the create-time default assignment). Easy to test only the create path and assume the `||` fallback is redundant; it isn't, since a real DB row could plausibly have these columns null from a schema migration or a partial write.
- **`getForUser`'s saved-prompts serialization**: confirmed the exact `find()` query shape (`userId` scope, `updatedAt DESC`, `take: 25`) and — a real security-relevant check — that `serializePrompt()`'s output genuinely has no `userId` field, not just an `undefined` one, since the raw entity passed into it does carry `userId` and a careless refactor (e.g. an object spread instead of an explicit field list) could leak it into an API response.
- **`updateForUser`'s partial-update semantics**: confirmed a dto with only `preferredBehavior` set leaves `suggestedTools`/`recommendedWorkflows` completely untouched (the `if (dto.x)` guards are per-field, not a merge-everything-or-nothing), and confirmed `suggestedTools` genuinely deduplicates via `[...new Set(...)]` rather than storing verbatim duplicates.
- **`recommendationsForUser`'s allow-list filtering**: the empty-list-means-unrestricted case (default param `[]`), the actual filtering case (both `suggestedTools` and workflow entries filtered down to the allowed set), and — the one non-obvious rule worth its own test — a workflow recommendation with no `toolId` at all always survives filtering regardless of the allow-list, since the filter condition is `!workflow.toolId || allowed.size===0 || allowed.has(...)` and a missing `toolId` short-circuits the whole check to true.
- **`savePrompt`/`deletePrompt`**: title trimming, `tags` defaulting to `[]`, `workspaceId` genuinely omitted (not stored as an empty string) when not given; `deletePrompt`'s `NotFoundException` guard and — the actually security-relevant assertion — that both the existence check and the delete itself are scoped to `{id, userId}` together, not just `{id}`, confirming one user can't delete another user's saved prompt by guessing an id.
- **`recordRecentPrompt`**: whitespace-collapsing + 160-character truncation, and the 10-entry cap (built a fixture with exactly 10 existing entries, recorded an 11th, and confirmed the oldest was evicted while the newest landed at index 0 — not just "length is still 10").

**Caught a wrong assumption in my own test before it ever ran, not after a red result**: the whitespace-truncation test's first draft asserted `promptPreview.startsWith('multi space text')` against an input that began with leading whitespace. Before trusting that assertion, ran the exact `.replace(/\s+/g, ' ')` logic standalone in Node against the literal test input — it revealed the collapsing regex leaves a single leading space rather than stripping it (`"  multi..."` → `" multi..."`, not `"multi..."`), which would have made the `startsWith` check fail against a string that started with a space. Fixed the test's input (removed the leading whitespace) rather than weaken the assertion — same "verify before trusting, including your own not-yet-run test code" discipline this program has applied to test authoring since Cycle 177's Pinecone metadata mistake, just caught pre-emptively this time instead of via a failing run.

**Verification:** new suite **1/1, 16/16**; backend `tsc` (both configs) 0 errors; backend `eslint` 0 errors (2 pure prettier findings in the new spec file, fixed via `eslint --fix`, re-verified all 16 tests still pass after); full backend suite **254/254 suites, 2089/2089 tests** (was 253/253, 2073/2073 — +1 suite, +16 tests, zero regressions). No frontend files touched. `git status` re-checked clean before staging.

**Final state, all fresh this cycle:** backend `tsc`/`eslint` both 0 errors, main jest 254/254 (2089/2089, +1 suite/+16 tests). Diff: 1 new file (`personalization.service.spec.ts`). Roadmap item #17: 7 of 9 closed, **2 remain** (`SentinelController` [still deferred, size], `ERPulseHandoverService`).

**Lesson: verifying a test's own logic against a quick standalone script before trusting it — not just before trusting the code under test — catches a real class of self-authored bug that a first "run it and see" pass would also have caught, but catching it *before* the first run means the eventual green result is actually informative (confirms the fixed assertion is correct) rather than ambiguous (a passing test whose author never independently checked whether the assertion itself matched reality is only as trustworthy as the author's mental model, which is exactly what this incident showed can be wrong even about a one-line regex).**

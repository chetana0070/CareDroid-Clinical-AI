# CareDroid Environment Configuration
# Copy to .env and update values

# ================================
# Application & Server
# ================================
# Do NOT set NODE_ENV here. Vite treats a `.env`-declared NODE_ENV=development
# as an explicit request to build in development mode -- even for `vite build`
# -- which silently ships React's dev runtime (larger, slower, with source
# paths baked into every chunk) instead of a real production build. Vite's
# own `mode`/`--mode` flag is the correct way to control this; the env-based
# .env files (.env.production etc.) already select on that automatically.
# Local dev defaults avoid :3000/:8000 (often taken by other apps). Override if needed.
FRONTEND_PORT=5190
BACKEND_PORT=3350
VITE_DEV_PORT=5190
# Documentation only — local app origin is http://localhost:5190 (Vite dev + preview). Nest API PORT lives in backend/.env (default 3350, proxied).
# Frontend: `npm run dev:fullstack` proxies `/api`, `/health`, and `/socket.io` to Nest (see vite.config.ts).
# Use empty VITE_API_URL for same-origin + Vite proxy; set a full URL only for a separate API host.
# Vercel frontend deploys are split API deploys by default and require VITE_API_URL.
# Leave empty in local dev so all API traffic goes through http://localhost:5190 (Vite proxy → Nest on :3350)
VITE_API_URL=
# Set true only when the deployed SPA origin has a verified edge/reverse proxy forwarding /api to Nest.
VITE_ALLOW_SAME_ORIGIN_API=false
# Required if VITE_ALLOW_SAME_ORIGIN_API=true in deployed environments.
VITE_SAME_ORIGIN_API_PROXY_VERIFIED=false
# WebSocket origin when split from the SPA (optional; defaults to same host as the page)
VITE_WS_URL=
# Vite dev/preview proxy target (Nest default :3000). Not used in production static builds.
VITE_API_PROXY_TARGET=http://localhost:3350
# Fetch timeout for apiClient (milliseconds)
VITE_API_TIMEOUT_MS=8000
# Emergency OS realtime SSE stream (token appended automatically)
VITE_ED_REALTIME_SSE_PATH=/api/emergency/realtime/stream
# Optional: WebSocket path or polling interval override (milliseconds)
VITE_ED_REALTIME_WS_PATH=
VITE_ED_REALTIME_POLL_MS=
VITE_APP_NAME=CareDroid-Clinical-AI
VITE_APP_VERSION=1.0.0
VITE_APP_ENVIRONMENT=development
VITE_APP_BUILD_DATE=2026-01-31
# Architect Mode: experimental AppShell engines (automation/OI/knowledge/living docs).
# Production default is OFF. Set true only for lab demos. Dev default is ON unless set false.
# VITE_ENABLE_EXPERIMENTAL_SHELL_ENGINES=false
# Optional explicit build metadata. Vite injects Git/Vercel metadata automatically when available.
# VITE_GIT_COMMIT is canonical; VITE_GIT_COMMIT_SHA is a documented alias.
VITE_GIT_COMMIT=
VITE_GIT_COMMIT_SHA=
VITE_GIT_BRANCH=
VITE_GIT_REPOSITORY=
VITE_BUILD_TIME=
# Optional deployment metadata (telemetry / version surfaces)
VITE_DEPLOYMENT_ID=
VITE_DEPLOYMENT_REGION=
VITE_DEPLOYED_AT=

# ================================
# Frontend Feature Flags (VITE_*)
# ================================
VITE_ENABLE_CRASH_REPORTING=true
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
VITE_SENTRY_PROFILES_SAMPLE_RATE=0.1
VITE_DEBUG=false
VITE_LOG_LEVEL=info

VITE_ENABLE_ANALYTICS=true
VITE_SEGMENT_WRITE_KEY=

VITE_ENABLE_PUSH_NOTIFICATIONS=true
VITE_ENABLE_OFFLINE_MODE=true
VITE_ENABLE_BIOMETRIC_AUTH=true
# Local/demo visible "Direct Sign In" button on `/` and `/auth`.
# Local Vite defaults to true. Hosted demo deployments must also set VITE_DEMO_MODE=true.
VITE_ENABLE_DEV_AUTH_BYPASS=true
# Shows the "Continue in Demo Mode" button on /auth for hosted demos.
VITE_DEMO_MODE=true
# Enables the platform Simulation / Training Mode toggle (mock data, no live patient feeds).
# Local dev defaults to true when unset; set false to exercise live backend wiring only.
VITE_SIMULATION_MODE=true
# Local fallback demo tokens are dev-only by default. Hosted demos should use backend /api/auth/dev-session.
VITE_ALLOW_LOCAL_DEMO_AUTH=true
VITE_DEV_BEARER_TOKEN=dev-token-for-testing
# Legacy flags retained for older builds; explicit dev/demo access now uses VITE_ENABLE_DEV_AUTH_BYPASS.
# Show "Demo / Local Dev Mode" quick sign-in on /auth even in production builds (e.g. staging/demo). Default: false.
VITE_SHOW_DEMO_AUTH=false
# Hide Division mode (instant bypass) on /auth. Production bundles default to hidden when unset.
VITE_HIDE_DIVISION_MODE=false

# ================================
# CareDroid SaaS / platform packaging (frontend)
# ================================
# Filter nav and launches by org entitlements and asset packs (default: true).
VITE_PLATFORM_ENTITLEMENTS=true
# Single primary workspace UX (default: true).
VITE_SINGLE_WORKSPACE_MODEL=true
# Show commercial/product discovery surfaces (default: true; pilot builds may hide).
VITE_COMMERCIAL_SURFACES=true
# Fail-closed entitlement checks — enable for staging/production tenants (default: false).
VITE_STRICT_SAAS_ENTITLEMENTS=false
# Nav respects entitled platform assets (default: true).
VITE_ASSET_AWARE_NAVIGATION=true
# Organization-scoped platform API reads (default: true).
VITE_ORG_SCOPED_PLATFORM_READS=true
# Dev-only: land on whiteboard instead of reception (default: unset/false).
# VITE_DEV_WHITEBOARD_LANDING=false

# ================================
# CareDroid single-application mode (ED)
# ================================
# One Vite app focused on emergency department operations. Extension URLs (fleet, cosmos, etc.)
# redirect into ED surfaces instead of rendering separate product shells.
VITE_ED_SINGLE_APPLICATION=true

# ================================
# Database (SQLite - Dev, PostgreSQL - Prod)
# ================================
DATABASE_CLIENT=sqlite
SQLITE_PATH=caredroid.dev.sqlite

# PostgreSQL (Production)
# DATABASE_CLIENT=postgres
# DATABASE_URL=postgresql://user:password@localhost:5432/caredroid

# ================================
# Security
# ================================
JWT_SECRET=change_this_to_32_character_random_string_in_production
ENCRYPTION_KEY=change_this_to_32_byte_hex_key_for_aes_256_0123456789ABCDEF

# ================================
# Authentication (Optional)
# ================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:5190/api/auth/google/callback

LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_CALLBACK_URL=http://localhost:5190/api/auth/linkedin/callback

# ================================
# Unified AI Client
# ================================
ANTHROPIC_API_KEY=
AI_MODEL=claude-sonnet-4-6
AI_PROVIDER=anthropic
# Optional Groq provider (AI_PROVIDER=groq). Not required for CI.
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
AI_TEMPERATURE=0.2
AI_MAX_TOKENS=2000
# Provider transport safety (timeout + circuit breaker). Hung provider calls fail with AI_TIMEOUT.
AI_REQUEST_TIMEOUT_MS=30000
AI_CIRCUIT_FAILURE_THRESHOLD=5
AI_CIRCUIT_COOLDOWN_MS=30000
AI_ENABLED=false
ED_COPILOT_AI_ENABLED=true
SMART_INTAKE_AI_ENABLED=false
REFERRAL_AI_ENABLED=false
ANALYTICS_AI_ENABLED=false
CLINICAL_WORKFLOW_AI_ENABLED=false
AI_AUDIT_LOGGING_ENABLED=true
AI_PATIENT_CONTEXT_ENABLED=false
# Browser builds never receive provider API keys or direct model names; frontend AI calls go through backend APIs.

# ================================
# RAG (Pinecone) — backend/.env also accepts these
# ================================
RAG_ENABLED=true
# Vector backend: pgvector | pinecone | memory (empty = auto)
RAG_VECTOR_BACKEND=
RAG_PGVECTOR_TABLE=caredroid_rag_vectors
RAG_AUTO_BOOTSTRAP_CORPUS=true
PINECONE_API_KEY=
PINECONE_INDEX_NAME=caredroid-medical-knowledge
PINECONE_ENVIRONMENT=us-east-1-aws
PINECONE_NAMESPACE=medical-docs
EMBEDDING_MODEL=Xenova/all-mpnet-base-v2
EMBEDDING_DIMENSION=768

# ================================
# NLU intent routing (TypeScript in-process on Nest /api/nlu)
# ================================
# Full NLU vars live in backend/.env — mirrored here for discoverability.
NLU_SERVICE_MODE=in-process
NLU_SERVICE_ENABLED=true
NLU_SERVICE_URL=http://127.0.0.1:3350/api/nlu
NLU_CONFIDENCE_THRESHOLD=0.7
# Retrain: cd backend && npm run nlu:pipeline

# ================================
# Clinical Reference APIs (Optional)
# ================================
VITE_FDA_API_KEY=
VITE_NIH_API_KEY=
VITE_PUBMED_API_KEY=

# ================================
# Firebase (Optional - for analytics in production)
# ================================
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=
VITE_FIREBASE_VAPID_KEY=

# ================================
# Legal URLs
# ================================
VITE_PRIVACY_POLICY_URL=http://localhost:5190/privacy
VITE_TERMS_OF_SERVICE_URL=http://localhost:5190/terms
VITE_SUPPORT_URL=http://localhost:5190/support
VITE_HIPAA_BAA_URL=http://localhost:5190/hipaa-baa
# ================================
# Monitoring & Observability (Batch 13)
# ================================

# Sentry Error Tracking
SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=1.0
SENTRY_PROFILES_SAMPLE_RATE=0.1

# Logging
LOG_LEVEL=debug
LOG_DIR=./logs
LOG_MAX_SIZE=20m
LOG_MAX_DAYS_COMBINED=7
LOG_MAX_DAYS_ERRORS=14
LOG_MAX_DAYS_PROD_COMBINED=30
LOG_MAX_DAYS_PROD_ERRORS=60

# Monitoring Stack (Docker Compose)
# Prometheus
PROMETHEUS_PORT=9090

# Grafana
GRAFANA_PASSWORD=admin
GRAFANA_USER=admin
GRAFANA_PORT=3001

# Kibana (Elasticsearch UI)
KIBANA_PORT=5601

# Sentry (Local Instance)
SENTRY_SECRET_KEY=your-secret-key-change-in-production
SENTRY_LOCAL_PORT=9000

# Redis (for Sentry and caching)
REDIS_PASSWORD=redis123
REDIS_PORT=6379

# Database (PostgreSQL for Sentry)
DB_NAME=caredroid
DB_USER=postgres
DB_PASSWORD=secure123
DB_PORT=5432

# Mail (for Sentry notifications)
MAIL_HOST=localhost
MAIL_PORT=1025

# ================================
# Alert Integrations (Batch 13 Phase 2)
# ================================

# Alertmanager Configuration
ALERTMANAGER_RESOLVE_TIMEOUT=5m

# Slack Alert Integration
# Get webhook URL from: https://api.slack.com/messaging/webhooks
ALERTMANAGER_SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Email Alert Integration (SMTP)
ALERTMANAGER_EMAIL_FROM=alerts@caredroid.local
ALERTMANAGER_EMAIL_TO=ops@caredroid.local
ALERTMANAGER_SMTP_HOST=localhost
ALERTMANAGER_SMTP_PORT=25
ALERTMANAGER_SMTP_USER=
ALERTMANAGER_SMTP_PASSWORD=

# PagerDuty Integration
# Get integration key: https://yoursubdomain.pagerduty.com/integrations
ALERTMANAGER_PAGERDUTY_KEY=

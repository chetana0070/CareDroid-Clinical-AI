import { LIGHT_THEME } from './light';
import { DARK_THEME } from './dark';

export { LIGHT_THEME } from './light';
export { DARK_THEME } from './dark';

/** Every theme currently registered with a real, working data-theme block. */
export const CDL_THEMES = Object.freeze([LIGHT_THEME, DARK_THEME]);

/**
 * Canonical header export — see `src/shell/ApplicationShell/ApplicationShell.tsx`
 * for why this re-export exists. The real implementation lives in
 * `src/components/Header.tsx` and is already mounted (once) inside AppShell.tsx.
 */
export { Header as ApplicationHeader } from '../../components/Header';
